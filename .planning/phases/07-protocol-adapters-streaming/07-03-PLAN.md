---
phase: "07-protocol-adapters-streaming"
plan: "03"
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - services/api-service/tests/relay/test_chat_endpoint.py
  - services/api-service/tests/relay/test_anthropic_endpoint.py
  - services/api-service/tests/relay/test_responses_endpoint.py
  - services/api-service/tests/relay/test_models_endpoint.py
  - services/api-service/tests/relay/test_streaming.py
autonomous: true
requirements: [RELAY-01, RELAY-02, RELAY-03, RELAY-04, RELAY-11, RELAY-12]

must_haves:
  truths:
    - "Chat completions 端点在 mock 上游下返回正确 OpenAI 格式响应"
    - "Anthropic messages 端点在 mock 上游下返回正确 Anthropic 格式响应"
    - "Responses 端点在 mock 上游下返回正确 Responses 格式响应"
    - "Models 端点返回过滤后的模型列表"
    - "SSE 流式响应格式正确（data: + [DONE]）"
    - "限流超限时返回 429"
  artifacts:
    - path: "services/api-service/tests/relay/test_chat_endpoint.py"
      provides: "RELAY-01 集成测试"
      contains: "def test_chat_completions"
    - path: "services/api-service/tests/relay/test_anthropic_endpoint.py"
      provides: "RELAY-02 集成测试"
      contains: "def test_anthropic_messages"
    - path: "services/api-service/tests/relay/test_responses_endpoint.py"
      provides: "RELAY-03 集成测试"
      contains: "def test_responses"
    - path: "services/api-service/tests/relay/test_models_endpoint.py"
      provides: "RELAY-04 集成测试"
      contains: "def test_list_models"
    - path: "services/api-service/tests/relay/test_streaming.py"
      provides: "RELAY-11 SSE 格式验证"
      contains: "def test_streaming"
  key_links:
    - from: "tests/relay/test_chat_endpoint.py"
      to: "controllers/relay/chat.py"
      via: "TestClient POST /v1/chat/completions"
      pattern: "client.post.*chat/completions"
    - from: "tests/relay/test_streaming.py"
      to: "relay/lifecycle/stream.py"
      via: "SSE format assertions"
      pattern: "text/event-stream"
---

<objective>
Phase 7 集成测试：验证四个 relay 端点 + SSE 流式 + 限流行为

Purpose: 通过 mock 上游 SDK 调用，验证完整请求链路（鉴权→限流→解析→路由→上游→响应格式化）在各协议下正确工作。确保 SSE 格式、限流 429、模型过滤等行为符合预期。
Output: 覆盖 RELAY-01~04, RELAY-11, RELAY-12 的集成测试套件
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-protocol-adapters-streaming/07-RESEARCH.md
@.planning/phases/07-protocol-adapters-streaming/07-01-SUMMARY.md

@services/api-service/tests/relay/conftest.py
@services/api-service/tests/conftest.py
@services/api-service/api_service/relay/auth.py
@services/api-service/api_service/relay/rate_limiter.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Chat + Anthropic + Responses 端点集成测试</name>
  <files>
    services/api-service/tests/relay/test_chat_endpoint.py
    services/api-service/tests/relay/test_anthropic_endpoint.py
    services/api-service/tests/relay/test_responses_endpoint.py
  </files>
  <read_first>
    services/api-service/api_service/controllers/relay/chat.py
    services/api-service/api_service/controllers/relay/anthropic.py
    services/api-service/api_service/controllers/relay/responses.py
    services/api-service/api_service/relay/lifecycle/orchestrator.py
    services/api-service/api_service/relay/auth.py
    services/api-service/api_service/relay/rate_limiter.py
    services/api-service/api_service/relay/dependencies.py
    services/api-service/tests/relay/conftest.py
    services/api-service/tests/conftest.py
  </read_first>
  <action>
    创建三个端点集成测试文件，使用 pytest + httpx AsyncClient + mock 策略：

    Mock 策略（所有测试共享）：
    - patch require_api_key 返回 ValidatedApiKey fixture（user_id=1, balance=100.0, user_rpm_limit=60, allowed_models="", api_key_id=1）
    - patch require_rate_limit 为 no-op
    - patch relay/dependencies 的 get_routing_config_cache 返回 mock config（含 model_channels, user_facing_aliases）
    - patch relay/dependencies 的 get_channel_selector 返回 mock selector（report_success/report_failure 为 no-op）
    - patch relay/dependencies 的 get_sdk_client_pool 返回 mock pool
    - patch upstream_dispatch.dispatch 返回 mock 响应

    1. test_chat_endpoint.py：
       - test_chat_completions_non_stream：POST /v1/chat/completions {model:"gpt-4", messages:[{"role":"user","content":"hi"}], stream:false}，mock dispatch 返回 OpenAIResponse，断言 200 + response.choices[0].message.content 存在
       - test_chat_completions_stream：stream=true，mock dispatch 返回 async iterator of OpenAIStreamChunk，断言 response.headers["content-type"] 包含 "text/event-stream"，body 包含 "data: " 行和 "data: [DONE]"
       - test_chat_completions_invalid_model：空 model 字段，断言 422
       - test_chat_completions_no_auth：不 mock require_api_key（让它 raise），断言 401

    2. test_anthropic_endpoint.py：
       - test_anthropic_messages_non_stream：POST /v1/anthropic/messages {model:"claude-3", messages:[{"role":"user","content":"hi"}], max_tokens:100, stream:false}，mock dispatch 返回 AnthropicAsOpenAIResponse，断言 200 + response 包含 "type":"message" 和 "content" 数组
       - test_anthropic_messages_stream：stream=true，断言 SSE 格式正确
       - test_anthropic_messages_missing_max_tokens：不传 max_tokens，断言 422
       - test_anthropic_dual_path：POST /v1/anthropic/v1/messages 也可达（不返回 404）

    3. test_responses_endpoint.py：
       - test_responses_non_stream：POST /v1/responses {model:"gpt-4", input:"hello", stream:false}，mock dispatch 返回 OpenAIResponse，断言 200 + response 包含 "output" 字段
       - test_responses_stream：stream=true，断言 SSE 格式
       - test_responses_invalid_request：缺少 model，断言 422

    所有测试使用 pytest.mark.asyncio 和 httpx.AsyncClient(transport=ASGITransport(app=app))。
  </action>
  <verify>
    <automated>cd /root/autodl-tmp/eucalAI_backend/services/api-service && python -m pytest tests/relay/test_chat_endpoint.py tests/relay/test_anthropic_endpoint.py tests/relay/test_responses_endpoint.py -x -q --timeout=30</automated>
  </verify>
  <acceptance_criteria>
    - test_chat_endpoint.py 包含至少 4 个测试函数，全部通过
    - test_anthropic_endpoint.py 包含至少 4 个测试函数，全部通过
    - test_responses_endpoint.py 包含至少 3 个测试函数，全部通过
    - 非流式测试断言响应 JSON 结构正确（OpenAI/Anthropic/Responses 各自格式）
    - 流式测试断言 content-type 为 text/event-stream 且 body 包含 SSE 格式数据
    - 无 auth 时返回 401，无效请求返回 422
  </acceptance_criteria>
  <done>三个协议端点的集成测试全部通过，覆盖非流式/流式/错误场景</done>
</task>

<task type="auto">
  <name>Task 2: Models 端点 + SSE 格式 + 限流 429 测试</name>
  <files>
    services/api-service/tests/relay/test_models_endpoint.py
    services/api-service/tests/relay/test_streaming.py
  </files>
  <read_first>
    services/api-service/api_service/controllers/relay/models.py
    services/api-service/api_service/relay/lifecycle/stream.py
    services/api-service/api_service/relay/rate_limiter.py
    services/api-service/api_service/relay/config_cache.py
    services/api-service/api_service/relay/auth.py
    services/api-service/tests/relay/conftest.py
  </read_first>
  <action>
    1. test_models_endpoint.py：
       - test_list_models_all：allowed_models="" 时返回全量 user_facing_aliases，断言 response["object"]=="list" 且 data 包含所有模型
       - test_list_models_filtered：allowed_models="gpt-4,claude-3" 时只返回交集，断言 data 只包含匹配模型
       - test_list_models_format：每个 model 对象包含 id, object="model", created=0, owned_by="eucal-ai"
       - test_list_models_sorted：返回的模型列表按 id 字母序排列
       - test_list_models_no_auth：不 mock auth，断言 401

       Mock 策略：patch require_api_key 返回 ValidatedApiKey，patch get_routing_config_cache().load() 返回 {"user_facing_aliases": ["gpt-4", "claude-3", "gemini-pro"]}

    2. test_streaming.py — SSE 格式深度验证：
       - test_sse_format_openai_chat：mock 完整流式链路，验证每行以 "data: " 开头，最后一行是 "data: [DONE]"，中间行是合法 JSON
       - test_sse_format_anthropic_native：mock Anthropic 原生流，验证输出包含 "event: message_start\ndata: " 格式
       - test_stream_usage_injected：验证 stream_options.include_usage=True 被注入到 forward_payload（D-11）
       - test_rate_limit_429：mock require_rate_limit 抛出 RateLimitExceeded，断言响应 429 + 包含 Retry-After header 或 error message

       Mock 策略：
       - SSE 测试需要 mock dispatch 返回 async generator（模拟 SDK 流式响应）
       - 限流测试 patch require_rate_limit 为抛出 RateLimitExceeded 的函数
  </action>
  <verify>
    <automated>cd /root/autodl-tmp/eucalAI_backend/services/api-service && python -m pytest tests/relay/test_models_endpoint.py tests/relay/test_streaming.py -x -q --timeout=30</automated>
  </verify>
  <acceptance_criteria>
    - test_models_endpoint.py 包含至少 5 个测试函数，全部通过
    - test_streaming.py 包含至少 4 个测试函数，全部通过
    - Models 测试验证 allowed_models 过滤逻辑（D-19, D-20）
    - SSE 测试验证 "data: " 前缀和 "[DONE]" 终止符
    - 限流测试验证 429 状态码
    - pytest tests/relay/ 全套通过（Plan 01 + Plan 03 共计 20+ 测试）
  </acceptance_criteria>
  <done>Models 端点过滤逻辑 + SSE 格式 + 限流 429 测试全部通过，Phase 7 测试覆盖完整</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test→app | 测试通过 mock 验证安全边界行为 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-10 | Elevation of Privilege | test_models_endpoint.py | mitigate | 测试验证 allowed_models 过滤确实生效 |
| T-07-11 | Denial of Service | test_streaming.py | mitigate | 测试验证限流 429 正确返回 |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | 无新增依赖 |
</threat_model>

<verification>
cd /root/autodl-tmp/eucalAI_backend/services/api-service && python -m pytest tests/relay/ -x -q --timeout=30
</verification>

<success_criteria>
- tests/relay/ 目录下所有测试通过（20+ 测试）
- 覆盖 RELAY-01（chat）、RELAY-02（anthropic）、RELAY-03（responses）、RELAY-04（models）、RELAY-11（streaming）、RELAY-12（rate limit 429）
- 非流式和流式路径均有测试覆盖
- 错误场景（401/422/429）均有测试覆盖
</success_criteria>

<output>
Create `.planning/phases/07-protocol-adapters-streaming/07-03-SUMMARY.md` when done
</output>

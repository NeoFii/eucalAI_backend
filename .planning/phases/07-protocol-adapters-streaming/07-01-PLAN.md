---
phase: "07-protocol-adapters-streaming"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - services/api-service/api_service/relay/sdk_clients.py
  - services/api-service/api_service/relay/retry_policy.py
  - services/api-service/api_service/relay/rate_limiter.py
  - services/api-service/api_service/relay/lua/token_bucket.lua
  - services/api-service/api_service/relay/backends/__init__.py
  - services/api-service/api_service/relay/backends/openai_backend.py
  - services/api-service/api_service/relay/backends/anthropic_backend.py
  - services/api-service/api_service/relay/upstream_dispatch.py
  - services/api-service/api_service/relay/adapters/__init__.py
  - services/api-service/api_service/relay/adapters/protocol.py
  - services/api-service/api_service/relay/schemas/chat.py
  - services/api-service/api_service/relay/schemas/anthropic.py
  - services/api-service/api_service/relay/schemas/responses.py
  - services/api-service/api_service/relay/schemas/__init__.py
  - services/api-service/api_service/relay/dependencies.py
  - services/api-service/api_service/core/lifespan.py
  - services/api-service/tests/relay/__init__.py
  - services/api-service/tests/relay/conftest.py
  - services/api-service/tests/relay/test_rate_limiter.py
  - services/api-service/tests/relay/test_sdk_clients.py
autonomous: true
requirements: [RELAY-05, RELAY-11, RELAY-12]

must_haves:
  truths:
    - "SdkClientPool 提供 LRU 复用的 AsyncOpenAI/AsyncAnthropic 实例"
    - "Token bucket 三级限流按 global→per-user→per-key 顺序检查"
    - "Redis 不可用时限流降级到 InMemory fallback"
    - "upstream_dispatch 根据 provider_slug 分派到 openai/anthropic 后端"
    - "ProtocolAdapter/StreamConverter Protocol 定义可被后续 adapter 实现"
  artifacts:
    - path: "services/api-service/api_service/relay/sdk_clients.py"
      provides: "SdkClientPool LRU 复用池"
      contains: "class SdkClientPool"
    - path: "services/api-service/api_service/relay/rate_limiter.py"
      provides: "RateLimiter + InMemoryRateLimiter + require_rate_limit Depends"
      contains: "class RateLimiter"
    - path: "services/api-service/api_service/relay/lua/token_bucket.lua"
      provides: "Token bucket Lua 脚本"
      contains: "redis.call"
    - path: "services/api-service/api_service/relay/upstream_dispatch.py"
      provides: "dispatch() SDK 路由分派"
      exports: ["dispatch"]
    - path: "services/api-service/api_service/relay/adapters/protocol.py"
      provides: "ProtocolAdapter + StreamConverter Protocol 定义"
      exports: ["ProtocolAdapter", "StreamConverter"]
    - path: "services/api-service/api_service/relay/backends/openai_backend.py"
      provides: "call_openai 上游调用"
      exports: ["call_openai"]
    - path: "services/api-service/api_service/relay/backends/anthropic_backend.py"
      provides: "call_anthropic_native + call_anthropic_from_openai"
      exports: ["call_anthropic_native", "call_anthropic_from_openai"]
  key_links:
    - from: "relay/rate_limiter.py"
      to: "relay/lua/token_bucket.lua"
      via: "redis.register_script"
      pattern: "register_script.*token_bucket"
    - from: "relay/upstream_dispatch.py"
      to: "relay/backends/"
      via: "provider_slug routing"
      pattern: "ANTHROPIC_NATIVE_SLUGS"
    - from: "relay/dependencies.py"
      to: "relay/sdk_clients.py"
      via: "get_sdk_client_pool singleton"
      pattern: "_sdk_client_pool"
---

<objective>
Phase 7 基础设施层：SdkClientPool、Token Bucket 限流、Backend 调用层、Protocol 定义、请求 Schema

Purpose: 为 Plan 02（lifecycle 编排器 + adapter 实现 + 端点挂载）提供所有底层依赖——SDK 客户端复用、限流、上游调用分派、协议接口定义、请求验证 schema。
Output: 可独立测试的 SdkClientPool + RateLimiter + backends + dispatch + schemas + protocol definitions
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-protocol-adapters-streaming/07-RESEARCH.md
@.planning/phases/07-protocol-adapters-streaming/07-PATTERNS.md

@services/api-service/api_service/relay/dependencies.py
@services/api-service/api_service/core/lifespan.py
@services/api-service/api_service/core/config.py
</context>

<interfaces>
<!-- Phase 6 existing contracts the executor needs -->

From relay/dependencies.py:
- init_relay_globals(*, config_cache, inference_client, channel_selector, affinity_store)
- get_routing_config_cache() -> RoutingConfigCache
- get_channel_selector() -> ChannelSelector
- shutdown_relay() -> None

From relay/auth.py:
- require_api_key -> ValidatedApiKey (has user_id, user_rpm_limit, allowed_models, balance)

From core/config.py (relevant settings):
- settings.RATE_LIMIT_ENABLED: bool = True
- settings.RATE_LIMIT_DEFAULT_USER_RPM: int = 20
- settings.RATE_LIMIT_GLOBAL_RPM: int = 0
- settings.SDK_CLIENT_POOL_MAX_SIZE: int = 64
- settings.ANTHROPIC_NATIVE_SLUGS: list[str] = ["anthropic"]
- settings.CHANNEL_MAX_RETRIES: int = 2
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: SdkClientPool + retry_policy + backends + upstream_dispatch + Protocol 定义 + schemas</name>
  <files>
    services/api-service/api_service/relay/sdk_clients.py
    services/api-service/api_service/relay/retry_policy.py
    services/api-service/api_service/relay/backends/__init__.py
    services/api-service/api_service/relay/backends/openai_backend.py
    services/api-service/api_service/relay/backends/anthropic_backend.py
    services/api-service/api_service/relay/upstream_dispatch.py
    services/api-service/api_service/relay/adapters/__init__.py
    services/api-service/api_service/relay/adapters/protocol.py
    services/api-service/api_service/relay/schemas/__init__.py
    services/api-service/api_service/relay/schemas/chat.py
    services/api-service/api_service/relay/schemas/anthropic.py
    services/api-service/api_service/relay/schemas/responses.py
  </files>
  <read_first>
    services/router-service/src/services/sdk_clients.py
    services/router-service/src/services/retry_policy.py
    services/router-service/src/services/upstream_dispatch.py
    services/router-service/src/services/protocol_adapter.py
    services/router-service/src/schemas/requests.py
    services/router-service/src/schemas/anthropic.py
    services/router-service/src/schemas/responses.py
    services/router-service/src/services/openai_backend.py
    services/router-service/src/services/anthropic_backend.py
    services/api-service/api_service/relay/dependencies.py
    services/api-service/api_service/core/config.py
  </read_first>
  <action>
    移植 router-service 的底层模块到 api-service/relay/：

    1. relay/sdk_clients.py — 原样移植 SdkClientPool（per D-05）：threading.Lock + LRU OrderedDict，max_size 参数化，get_openai(base_url, api_key) -> AsyncOpenAI，get_anthropic(base_url, api_key) -> AsyncAnthropic，close_all() async 关闭所有 client。

    2. relay/retry_policy.py — 原样移植：RETRYABLE_STATUS_CODES frozenset({429,500,502,503})，NEVER_RETRY_STATUS_CODES frozenset({400,401,403,404})，extract_status_code(exc) -> int|None，should_retry(exc, status_code=None) -> bool。

    3. relay/backends/__init__.py — 空 __init__。
       relay/backends/openai_backend.py — 移植 call_openai：接收 SdkClientPool + target_info + messages + forward_payload + stream + timeout，调用 pool.get_openai(target_info["api_base"], target_info["api_key"])，构建 kwargs（model=target_info["upstream_model"]），stream=True 时返回 async iterator（包装 OpenAIStreamChunk dataclass），非 stream 返回 OpenAIResponse dataclass。
       relay/backends/anthropic_backend.py — 移植 call_anthropic_native 和 call_anthropic_from_openai：native 路径直接传 anthropic_params 到 SDK，from_openai 路径先用 _openai_to_anthropic_params 转换再调用。非 stream 返回 AnthropicNativeResponse / AnthropicAsOpenAIResponse dataclass。stream 返回 SDK 原生 async iterator。

    4. relay/upstream_dispatch.py — 移植 dispatch() 函数（per D-07）：根据 target_info["provider_slug"] in settings.ANTHROPIC_NATIVE_SLUGS 分派。如果是 anthropic 上游且 incoming_protocol=="anthropic" 且 anthropic_request 不为 None，走 call_anthropic_native；否则走 call_anthropic_from_openai。非 anthropic 走 call_openai。

    5. relay/adapters/__init__.py — 空 __init__。
       relay/adapters/protocol.py — 原样移植 ProtocolAdapter 和 StreamConverter Protocol 定义（runtime_checkable）。ProtocolAdapter 方法：protocol_name property, parse_request, format_error, format_non_stream_response, create_stream_converter, get_timeout。StreamConverter 方法：convert_chunk, get_final_event。

    6. relay/schemas/__init__.py — 空 __init__。
       relay/schemas/chat.py — 移植 ChatCompletionRequest Pydantic v2 model：model(str, min_length=1, max_length=128), messages(list[dict], min_length=1, max_length=256), stream(bool=False), temperature, top_p, max_tokens, stop, stream_options, tools, tool_choice, response_format, frequency_penalty, presence_penalty, n, seed, user, logprobs, top_logprobs。加 model_validator 检查 total content size <= 512KB。ConfigDict(extra="allow") 允许未知字段透传。
       relay/schemas/anthropic.py — 移植 AnthropicMessagesRequest：model, messages, max_tokens(required), stream, system, temperature, top_p, top_k, stop_sequences, metadata, tools, tool_choice, thinking。ConfigDict(extra="allow")。
       relay/schemas/responses.py — 移植 ResponsesRequest：model, input(str|list), stream, instructions, tools, temperature, top_p, max_output_tokens, previous_response_id, store, metadata, truncation。ConfigDict(extra="allow")。
  </action>
  <verify>
    <automated>cd /root/autodl-tmp/eucalAI_backend/services/api-service && python -c "
from api_service.relay.sdk_clients import SdkClientPool
from api_service.relay.retry_policy import should_retry, extract_status_code
from api_service.relay.upstream_dispatch import dispatch
from api_service.relay.adapters.protocol import ProtocolAdapter, StreamConverter
from api_service.relay.backends.openai_backend import call_openai
from api_service.relay.backends.anthropic_backend import call_anthropic_native, call_anthropic_from_openai
from api_service.relay.schemas.chat import ChatCompletionRequest
from api_service.relay.schemas.anthropic import AnthropicMessagesRequest
from api_service.relay.schemas.responses import ResponsesRequest
print('All imports OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - relay/sdk_clients.py contains `class SdkClientPool` with get_openai, get_anthropic, close_all methods
    - relay/retry_policy.py contains should_retry and extract_status_code functions
    - relay/backends/openai_backend.py exports call_openai function
    - relay/backends/anthropic_backend.py exports call_anthropic_native and call_anthropic_from_openai
    - relay/upstream_dispatch.py exports dispatch function referencing settings.ANTHROPIC_NATIVE_SLUGS
    - relay/adapters/protocol.py defines ProtocolAdapter and StreamConverter as runtime_checkable Protocol classes
    - relay/schemas/chat.py defines ChatCompletionRequest with model_validator for 512KB limit
    - relay/schemas/anthropic.py defines AnthropicMessagesRequest with max_tokens required
    - relay/schemas/responses.py defines ResponsesRequest with input field
    - All imports succeed without error
  </acceptance_criteria>
  <done>所有底层模块（sdk_clients, retry_policy, backends, dispatch, protocol, schemas）可正常 import，类型和函数签名与 PATTERNS.md 一致</done>
</task>

<task type="auto">
  <name>Task 2: RateLimiter + Lua 脚本 + dependencies 扩展 + lifespan 注册 + 测试</name>
  <files>
    services/api-service/api_service/relay/rate_limiter.py
    services/api-service/api_service/relay/lua/token_bucket.lua
    services/api-service/api_service/relay/dependencies.py
    services/api-service/api_service/core/lifespan.py
    services/api-service/tests/relay/__init__.py
    services/api-service/tests/relay/conftest.py
    services/api-service/tests/relay/test_rate_limiter.py
    services/api-service/tests/relay/test_sdk_clients.py
  </files>
  <read_first>
    services/router-service/src/services/rate_limiter.py
    services/router-service/src/services/lua/token_bucket.lua
    services/api-service/api_service/relay/dependencies.py
    services/api-service/api_service/core/lifespan.py
    services/api-service/api_service/core/config.py
    services/api-service/api_service/relay/auth.py
    services/api-service/tests/conftest.py
  </read_first>
  <action>
    1. relay/lua/token_bucket.lua — 创建 lua/ 目录，原样移植 token bucket Lua 脚本（27 行）。KEYS[1] = hash key，ARGV[1]=capacity, ARGV[2]=refill_rate, ARGV[3]=cost。使用 redis.call('TIME') 获取微秒精度时间戳。返回 1=允许 / 0=拒绝。

    2. relay/rate_limiter.py — 实现 RateLimiter 类（per D-13~D-18）：
       - RateLimitExceeded 异常类（含 retry_after: int 属性）
       - InMemoryRateLimiter 类：使用 cachetools.TTLCache(maxsize=4096, ttl=60) 做 fallback，check(key, capacity) -> bool 方法用简单计数器
       - RateLimiter 类：__init__(redis, settings) 注册 Lua 脚本；check_all(user_id, key_rpm, user_rpm) 按 D-14 顺序检查 global→per-user→per-key；_check(key, capacity) 调用 Lua 脚本，失败时 fallback
       - require_rate_limit FastAPI Depends 函数（per D-17）：依赖 require_api_key 返回的 principal，读取 settings.RATE_LIMIT_ENABLED 开关，调用 get_rate_limiter().check_all()
       - per-key Redis key 格式：rl:key:{principal.api_key_id}（数字 ID，非 hash）

    3. relay/dependencies.py — 扩展现有文件：
       - 新增 _sdk_client_pool: SdkClientPool | None = None 和 _rate_limiter: RateLimiter | None = None 模块级变量
       - 新增 get_sdk_client_pool() -> SdkClientPool 和 get_rate_limiter() -> RateLimiter getter
       - 扩展 init_relay_globals 签名增加 sdk_client_pool 和 rate_limiter 参数
       - 扩展 shutdown_relay 增加 sdk_client_pool.close_all() 调用
       - 更新 __all__ 列表

    4. core/lifespan.py — 扩展 register_relay_resources 中的 _init_relay 闭包：
       - 在现有初始化之后创建 SdkClientPool(max_size=settings.SDK_CLIENT_POOL_MAX_SIZE)
       - 创建 RateLimiter(redis=cache_redis, settings=settings)
       - 将 sdk_client_pool 和 rate_limiter 传入 init_relay_globals
       - 在 _shutdown_relay 中增加 sdk_client_pool.close_all() 调用（存入 _state）

    5. tests/relay/__init__.py — 空文件。
       tests/relay/conftest.py — 共享 fixtures：mock_redis（AsyncMock），mock_sdk_client_pool（SdkClientPool 实例），mock_settings（带 RATE_LIMIT_* 配置的 mock）。
       tests/relay/test_rate_limiter.py — 测试：
         - test_check_all_global_limit_exceeded：global RPM=1，第二次调用 raises RateLimitExceeded
         - test_check_all_passes_when_under_limit：所有限制宽松时通过
         - test_inmemory_fallback_when_redis_none：redis=None 时使用 InMemoryRateLimiter
         - test_check_order_global_first：mock Redis 验证 global key 先于 user key 被检查
       tests/relay/test_sdk_clients.py — 测试：
         - test_get_openai_returns_same_instance：相同 key 返回同一 client
         - test_lru_eviction：超过 max_size 后最旧 client 被淘汰
         - test_close_all_clears_pool：close_all 后 pool 为空
  </action>
  <verify>
    <automated>cd /root/autodl-tmp/eucalAI_backend/services/api-service && python -m pytest tests/relay/ -x -q --timeout=30</automated>
  </verify>
  <acceptance_criteria>
    - relay/lua/token_bucket.lua 存在且包含 redis.call('TIME') 和 HSET/HGET/EXPIRE 操作
    - relay/rate_limiter.py 包含 class RateLimiter, class InMemoryRateLimiter, class RateLimitExceeded, async def require_rate_limit
    - RateLimiter.check_all 按 global→per-user→per-key 顺序检查（D-14）
    - relay/dependencies.py 包含 get_sdk_client_pool 和 get_rate_limiter 函数
    - core/lifespan.py 的 _init_relay 创建 SdkClientPool 和 RateLimiter 并传入 init_relay_globals
    - pytest tests/relay/ 全部通过（至少 7 个测试）
  </acceptance_criteria>
  <done>RateLimiter 三级限流 + SdkClientPool 单例管理 + lifespan 注册完成，测试全绿</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→relay endpoint | 不可信的 API Key + 请求体跨越此边界 |
| relay→upstream SDK | 内部 API Key 用于调用上游 LLM 提供商 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Denial of Service | rate_limiter.py | mitigate | Token bucket 三级限流 + InMemory fallback 防止 Redis 故障时无限流 |
| T-07-02 | Information Disclosure | backends/*.py | mitigate | 上游 API Key 不出现在错误响应中，error sanitization 在 lifecycle 层处理 |
| T-07-03 | Denial of Service | sdk_clients.py | mitigate | max_size=64 LRU 上限防止内存无限增长 |
| T-07-04 | Tampering | schemas/*.py | mitigate | Pydantic 512KB content limit + 256 message limit 防止超大请求 |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | 无新增依赖，所有库已在 pyproject.toml 中 |
</threat_model>

<verification>
cd /root/autodl-tmp/eucalAI_backend/services/api-service && python -m pytest tests/relay/ -x -q --timeout=30
cd /root/autodl-tmp/eucalAI_backend/services/api-service && python -c "from api_service.relay.sdk_clients import SdkClientPool; from api_service.relay.rate_limiter import RateLimiter, require_rate_limit; from api_service.relay.upstream_dispatch import dispatch; print('OK')"
</verification>

<success_criteria>
- SdkClientPool LRU 复用池可实例化并通过测试
- RateLimiter 三级限流按正确顺序检查并通过测试
- Token bucket Lua 脚本存在
- upstream_dispatch 可正确路由到 openai/anthropic backend
- ProtocolAdapter/StreamConverter Protocol 定义可 import
- 三个请求 Schema（Chat/Anthropic/Responses）可正常验证
- dependencies.py 扩展了 get_sdk_client_pool + get_rate_limiter
- lifespan 注册了 SdkClientPool 和 RateLimiter 初始化/关闭
</success_criteria>

<output>
Create `.planning/phases/07-protocol-adapters-streaming/07-01-SUMMARY.md` when done
</output>

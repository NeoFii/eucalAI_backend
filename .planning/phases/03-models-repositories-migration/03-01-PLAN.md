---
plan: 03-01
title: ORM Models Consolidation
phase: 3
wave: 1
depends_on: []
files_modified:
  - services/api-service/api_service/models/__init__.py
  - services/api-service/api_service/models/user.py
  - services/api-service/api_service/models/user_session.py
  - services/api-service/api_service/models/email_verification_code.py
  - services/api-service/api_service/models/user_api_key.py
  - services/api-service/api_service/models/balance_transaction.py
  - services/api-service/api_service/models/topup_order.py
  - services/api-service/api_service/models/api_call_log.py
  - services/api-service/api_service/models/usage_stat.py
  - services/api-service/api_service/models/voucher_redemption_code.py
  - services/api-service/api_service/models/admin_user.py
  - services/api-service/api_service/models/admin_audit_log.py
  - services/api-service/api_service/models/audit_action_definition.py
  - services/api-service/api_service/models/model_catalog.py
  - services/api-service/api_service/models/pool.py
  - services/api-service/api_service/models/routing_setting.py
  - services/api-service/api_service/models/enums.py
  - services/api-service/tests/test_models_import.py
autonomous: true
requirements:
  - USER-02
  - USER-03
  - ADMIN-02
---

# Plan 03-01: ORM Models Consolidation

## 目标

将 user-service 的 9 个 ORM model 和 admin-service 的 7 个 ORM model（含 10 个类）迁移到 `api_service/models/` 包下，统一继承 `api_service.common.infra.db.base.Base`，按 D-01 决策重命名 3 个类，确保所有 model 可无循环依赖导入。

## 任务

<task id="03-01-01">
<title>创建 enums 模块</title>
<read_first>
- services/admin-service/src/core/enums.py
- services/api-service/api_service/common/infra/db/base.py
</read_first>
<action>
创建 `api_service/models/enums.py`，包含三个 IntEnum 类：
- `AdminRole`（ADMIN=0, SUPER_ADMIN=1）
- `AdminStatus`（DISABLED=0, ACTIVE=1）
- `PoolAccountStatus`（ACTIVE=0, DISABLED=1, EXHAUSTED=2, ERROR=3）

从 `services/admin-service/src/core/enums.py` 复制定义，保持值不变。
</action>
<acceptance_criteria>
- `from api_service.models.enums import AdminRole, AdminStatus, PoolAccountStatus` 成功
- `AdminRole.SUPER_ADMIN == 1`
- `PoolAccountStatus.ERROR == 3`
</acceptance_criteria>
</task>

<task id="03-01-02">
<title>迁移 User model</title>
<read_first>
- services/user-service/src/models/user.py
- services/api-service/api_service/common/infra/db/base.py
- services/api-service/api_service/common/utils/timezone.py
</read_first>
<action>
创建 `api_service/models/user.py`，从 user-service 复制 `User` 类。调整 import：
- `from api_service.common.infra.db.base import Base, SnowflakeIdMixin, TimestampMixin`
- `from api_service.common.utils.timezone import now`
- relationship 引用字符串 `"UserSession"` 保持不变
- `__tablename__ = "users"` 保持不变
</action>
<acceptance_criteria>
- 文件存在于 `services/api-service/api_service/models/user.py`
- 类名为 `User`，`__tablename__ == "users"`
- 继承链为 `(Base, SnowflakeIdMixin, TimestampMixin)`
- 包含 `uid`, `email`, `password_hash`, `status`, `balance`, `frozen_amount`, `used_amount`, `total_requests`, `total_tokens`, `rpm_limit` 列
- `is_active`, `is_email_verified`, `is_login_locked` 属性存在
</acceptance_criteria>
</task>

<task id="03-01-03">
<title>迁移 UserSession model</title>
<read_first>
- services/user-service/src/models/user_session.py
- services/api-service/api_service/common/infra/db/base.py
</read_first>
<action>
创建 `api_service/models/user_session.py`，从 user-service 复制 `UserSession` 类。调整 import：
- `from api_service.common.infra.db.base import Base, SnowflakeIdMixin, TimestampMixin`
- `from api_service.common.utils.timezone import now`
- `__tablename__ = "user_sessions"` 保持不变
- relationship `"User"` 保持字符串引用
</action>
<acceptance_criteria>
- 类名为 `UserSession`，`__tablename__ == "user_sessions"`
- 包含 `session_id`, `user_id`, `token_jti`, `refresh_token_hash`, `expires_at`, `revoked_at` 列
- `is_revoked`, `is_expired`, `is_valid` 属性存在
- `ForeignKey("users.id", ondelete="CASCADE")` 存在
</acceptance_criteria>
</task>

<task id="03-01-04">
<title>迁移 EmailVerificationCode model</title>
<read_first>
- services/user-service/src/models/email_verification_code.py
- services/api-service/api_service/common/infra/db/base.py
</read_first>
<action>
创建 `api_service/models/email_verification_code.py`，从 user-service 复制 `EmailVerificationCode` 类。调整 import：
- `from api_service.common.infra.db.base import Base`
- `from api_service.common.utils.timezone import now`
- 该 model 使用 autoincrement id（不用 SnowflakeIdMixin），保持原样
- `__tablename__ = "email_verification_codes"` 保持不变
- `__table_args__` 中的 3 个 Index 保持不变
</action>
<acceptance_criteria>
- 类名为 `EmailVerificationCode`，`__tablename__ == "email_verification_codes"`
- `id` 列为 `BigInteger, primary_key=True, autoincrement=True`
- 包含 `email`, `code_hash`, `purpose`, `expires_at`, `used_at`, `error_count`, `locked_until` 列
- `__table_args__` 包含 3 个 Index
</acceptance_criteria>
</task>

<task id="03-01-05">
<title>迁移 UserApiKey model</title>
<read_first>
- services/user-service/src/models/user_api_key.py
- services/api-service/api_service/common/infra/db/base.py
</read_first>
<action>
创建 `api_service/models/user_api_key.py`，从 user-service 复制 `UserApiKey` 类。调整 import：
- `from api_service.common.infra.db.base import Base, SnowflakeIdMixin, SoftDeleteMixin, TimestampMixin`
- `__tablename__ = "user_api_keys"` 保持不变
- 类常量 STATUS_ACTIVE=1, STATUS_DISABLED=2, STATUS_EXPIRED=3, STATUS_EXHAUSTED=4, MODE_UNLIMITED=1, MODE_LIMITED=2 保持不变
</action>
<acceptance_criteria>
- 类名为 `UserApiKey`，`__tablename__ == "user_api_keys"`
- 继承链包含 `SoftDeleteMixin`
- 包含 `key_hash`, `key_prefix`, `name`, `status`, `quota_mode`, `quota_limit`, `quota_used`, `allowed_models`, `allow_ips`, `expires_at`, `last_used_at` 列
- `is_exhausted` 属性存在
- `ForeignKey("users.id", ondelete="CASCADE")` 存在
</acceptance_criteria>
</task>

<task id="03-01-06">
<title>迁移 BalanceTransaction model</title>
<read_first>
- services/user-service/src/models/balance_transaction.py
- services/api-service/api_service/common/infra/db/base.py
</read_first>
<action>
创建 `api_service/models/balance_transaction.py`，从 user-service 复制 `BalanceTransaction` 类。调整 import：
- `from api_service.common.infra.db.base import Base, SnowflakeIdMixin`
- `from api_service.common.utils.timezone import now`
- 保持 `__init__` 中的 ref_type/ref_id 校验逻辑
- 7 个 TYPE_ 常量保持不变
</action>
<acceptance_criteria>
- 类名为 `BalanceTransaction`，`__tablename__ == "balance_transactions"`
- 包含 `TYPE_TOPUP=1` 到 `TYPE_VOUCHER_REDEEM=7` 共 7 个常量
- `__init__` 中 `ref_type` 和 `ref_id` 为 None 时抛出 `ValueError`
- `ForeignKey("users.id", ondelete="CASCADE")` 存在
</acceptance_criteria>
</task>

<task id="03-01-07">
<title>迁移 TopupOrder model</title>
<read_first>
- services/user-service/src/models/topup_order.py
- services/api-service/api_service/common/infra/db/base.py
</read_first>
<action>
创建 `api_service/models/topup_order.py`，从 user-service 复制 `TopupOrder` 类。调整 import：
- `from api_service.common.infra.db.base import Base, SnowflakeIdMixin, TimestampMixin`
- `__tablename__ = "topup_orders"` 保持不变
- 4 个 STATUS_ 常量保持不变
</action>
<acceptance_criteria>
- 类名为 `TopupOrder`，`__tablename__ == "topup_orders"`
- 包含 `STATUS_PENDING=1` 到 `STATUS_REFUNDED=4`
- 包含 `order_no`, `user_id`, `amount`, `status`, `payment_channel`, `payment_no`, `payment_raw`, `paid_at` 列
</acceptance_criteria>
</task>

<task id="03-01-08">
<title>迁移 ApiCallLog model</title>
<read_first>
- services/user-service/src/models/api_call_log.py
- services/api-service/api_service/common/infra/db/base.py
</read_first>
<action>
创建 `api_service/models/api_call_log.py`，从 user-service 复制 `ApiCallLog` 类。调整 import：
- `from api_service.common.infra.db.base import Base, SnowflakeIdMixin`
- `from api_service.common.utils.timezone import now`
- relationship 引用 `"UserApiKey"` 保持字符串
- `__tablename__ = "api_call_logs"` 保持不变
- 所有 30+ 列保持不变
</action>
<acceptance_criteria>
- 类名为 `ApiCallLog`，`__tablename__ == "api_call_logs"`
- 包含 `request_id`, `user_id`, `api_key_id`, `model_name`, `selected_model`, `provider_slug`, `prompt_tokens`, `completion_tokens`, `cost`, `status`, `duration_ms` 等列
- `STATUS_SUCCESS = 200` 常量存在
- `is_success` 和 `is_error` 静态方法存在
- `ForeignKey("users.id")` 和 `ForeignKey("user_api_keys.id")` 存在
</acceptance_criteria>
</task>

<task id="03-01-09">
<title>迁移 UsageStat model</title>
<read_first>
- services/user-service/src/models/usage_stat.py
- services/api-service/api_service/common/infra/db/base.py
</read_first>
<action>
创建 `api_service/models/usage_stat.py`，从 user-service 复制 `UsageStat` 类。调整 import：
- `from api_service.common.infra.db.base import Base, SnowflakeIdMixin, TimestampMixin`
- `__tablename__ = "usage_stats"` 保持不变
- `__table_args__` 中的 2 个 UniqueConstraint 和 3 个 Index 保持不变
</action>
<acceptance_criteria>
- 类名为 `UsageStat`，`__tablename__ == "usage_stats"`
- `__table_args__` 包含 `uk_usage_stats_bucket` 和 `uk_usage_stats_bucket_effective` UniqueConstraint
- 包含 `user_id`, `api_key_id`, `account_api_key_id`, `model_name`, `stat_hour` 列
</acceptance_criteria>
</task>

<task id="03-01-10">
<title>迁移 VoucherRedemptionCode model</title>
<read_first>
- services/user-service/src/models/voucher_redemption_code.py
- services/api-service/api_service/common/infra/db/base.py
</read_first>
<action>
创建 `api_service/models/voucher_redemption_code.py`，从 user-service 复制 `VoucherRedemptionCode` 类。调整 import：
- `from api_service.common.infra.db.base import Base, SnowflakeIdMixin, TimestampMixin`
- `__tablename__ = "voucher_redemption_codes"` 保持不变
- relationship 引用 `"User"` 保持字符串
- 3 个 STATUS_ 常量保持不变
</action>
<acceptance_criteria>
- 类名为 `VoucherRedemptionCode`，`__tablename__ == "voucher_redemption_codes"`
- 包含 `STATUS_ACTIVE=1`, `STATUS_REDEEMED=2`, `STATUS_DISABLED=3`
- 包含 `code_hash`, `code_prefix`, `code_suffix`, `amount`, `status`, `starts_at`, `expires_at`, `redeemed_user_id` 列
- `ForeignKey("users.id", ondelete="SET NULL")` 存在
</acceptance_criteria>
</task>

<task id="03-01-11">
<title>迁移 AdminUser model</title>
<read_first>
- services/admin-service/src/models/admin_user.py
- services/api-service/api_service/common/infra/db/base.py
- services/api-service/api_service/common/utils/timezone.py
</read_first>
<action>
创建 `api_service/models/admin_user.py`，从 admin-service 复制 `AdminUser` 类。调整 import：
- `from api_service.common.infra.db.base import Base, SnowflakeIdMixin, TimestampMixin`
- `from api_service.models.enums import AdminRole, AdminStatus`
- `from api_service.common.utils.timezone import now`（用于 `is_login_locked` 属性）
- `__tablename__ = "admin_users"` 保持不变
- 3 个自引用 relationship 和 2 个 audit_logs relationship 保持字符串引用
</action>
<acceptance_criteria>
- 类名为 `AdminUser`，`__tablename__ == "admin_users"`
- 包含 `uid`, `email`, `password_hash`, `name`, `status`, `role`, `is_root` 列
- `is_active`, `is_super_admin`, `is_login_locked` 属性存在
- 3 个 `ForeignKey("admin_users.id")` 自引用存在
- `from api_service.models.enums import AdminRole, AdminStatus` 在文件中
</acceptance_criteria>
</task>

<task id="03-01-12">
<title>迁移 AdminAuditLog 和 AuditActionDefinition models</title>
<read_first>
- services/admin-service/src/models/admin_audit_log.py
- services/admin-service/src/models/audit_action_definition.py
- services/api-service/api_service/common/infra/db/base.py
</read_first>
<action>
创建两个文件：

1. `api_service/models/audit_action_definition.py` — 从 admin-service 复制 `AuditActionDefinition` 类。调整 import：
   - `from api_service.common.infra.db.base import Base`
   - `from api_service.common.utils.timezone import now`
   - `__tablename__ = "audit_action_definitions"` 保持不变
   - `code` 为 primary_key（非 Snowflake ID）

2. `api_service/models/admin_audit_log.py` — 从 admin-service 复制 `AdminAuditLog` 类。调整 import：
   - `from api_service.common.infra.db.base import Base`
   - `from api_service.common.utils.timezone import now`
   - `__tablename__ = "admin_audit_logs"` 保持不变
   - `id` 为 autoincrement BigInteger（非 Snowflake ID）
   - relationship 引用 `"AdminUser"` 和 `"AuditActionDefinition"` 保持字符串
</action>
<acceptance_criteria>
- `AuditActionDefinition` 类存在，`__tablename__ == "audit_action_definitions"`，`code` 为 primary_key
- `AdminAuditLog` 类存在，`__tablename__ == "admin_audit_logs"`
- `AdminAuditLog` 包含 `actor_admin_id`, `target_admin_id`, `action`, `resource_type`, `before_data`, `after_data` 列
- `ForeignKey("admin_users.id")` 和 `ForeignKey("audit_action_definitions.code")` 存在
- `__table_args__` 包含 `ix_admin_audit_logs_created_at` Index
</acceptance_criteria>
</task>

<task id="03-01-13">
<title>迁移 ModelCatalog 相关 models（含重命名）</title>
<read_first>
- services/admin-service/src/models/model_catalog.py
- services/api-service/api_service/common/infra/db/base.py
</read_first>
<action>
创建 `api_service/models/model_catalog.py`，从 admin-service 复制 4 个类并按 D-01 重命名：
- `ModelVendor` — 保持不变，`__tablename__ = "model_vendors"`
- `ModelCategory` — 保持不变，`__tablename__ = "model_categories"`
- `SupportedModel` → 重命名为 `ModelCatalog`，`__tablename__ = "model_catalog"` 保持不变
- `SupportedModelCategoryMap` → 重命名为 `ModelCatalogCategoryMap`，`__tablename__ = "model_catalog_category_map"` 保持不变

调整 import：
- `from api_service.common.infra.db.base import Base, SnowflakeIdMixin, TimestampMixin`

relationship 字符串引用更新：
- `ModelVendor.models` → `relationship("ModelCatalog", ...)`
- `ModelCategory.model_links` → `relationship("ModelCatalogCategoryMap", ...)`
- `ModelCatalog.category_links` → `relationship("ModelCatalogCategoryMap", ...)`
- `ModelCatalogCategoryMap.model` → `relationship("ModelCatalog", ...)`
- `ModelCatalogCategoryMap.category` → `relationship("ModelCategory", ...)`
- `ModelCatalog.vendor` → `relationship("ModelVendor", ...)`

UniqueConstraint name 保持 `uk_supported_model_category`（与 DDL 一致）。
</action>
<acceptance_criteria>
- 文件包含 4 个类：`ModelVendor`, `ModelCategory`, `ModelCatalog`, `ModelCatalogCategoryMap`
- `ModelCatalog.__tablename__ == "model_catalog"`
- `ModelCatalogCategoryMap.__tablename__ == "model_catalog_category_map"`
- `ModelCatalog` 包含 `slug`, `routing_slug`, `name`, `vendor_id`, `sale_input_per_million`, `is_active` 列
- relationship 字符串引用使用新类名 `"ModelCatalog"` 和 `"ModelCatalogCategoryMap"`
- 不存在名为 `SupportedModel` 或 `SupportedModelCategoryMap` 的类
</acceptance_criteria>
</task>

<task id="03-01-14">
<title>迁移 Pool 相关 models（含重命名）</title>
<read_first>
- services/admin-service/src/models/pool.py
- services/api-service/api_service/common/infra/db/base.py
- services/api-service/api_service/models/enums.py
</read_first>
<action>
创建 `api_service/models/pool.py`，从 admin-service 复制 3 个类并按 D-01 重命名：
- `Pool` — 保持不变，`__tablename__ = "pools"`
- `PoolModel` → 重命名为 `PoolModelConfig`，`__tablename__ = "pool_model_configs"` 保持不变
- `PoolAccount` — 保持不变，`__tablename__ = "pool_accounts"`

调整 import：
- `from api_service.common.infra.db.base import Base, SnowflakeIdMixin, TimestampMixin`
- `from api_service.models.enums import PoolAccountStatus`

relationship 字符串引用更新：
- `Pool.models` → `relationship("PoolModelConfig", ...)`
- `Pool.accounts` → `relationship("PoolAccount", ...)`
- `PoolModelConfig.pool` → `relationship("Pool", ...)`
- `PoolAccount.pool` → `relationship("Pool", ...)`
</action>
<acceptance_criteria>
- 文件包含 3 个类：`Pool`, `PoolModelConfig`, `PoolAccount`
- `PoolModelConfig.__tablename__ == "pool_model_configs"`
- 不存在名为 `PoolModel` 的类
- `Pool.models` relationship 引用 `"PoolModelConfig"`
- `PoolAccount.status` 使用 `PoolAccountStatus.ACTIVE` 作为 default
- `ForeignKey("pools.id", ondelete="CASCADE")` 在 PoolModelConfig 和 PoolAccount 中存在
</acceptance_criteria>
</task>

<task id="03-01-15">
<title>迁移 RoutingSetting model</title>
<read_first>
- services/admin-service/src/models/routing_setting.py
- services/api-service/api_service/common/infra/db/base.py
</read_first>
<action>
创建 `api_service/models/routing_setting.py`，从 admin-service 复制 `RoutingSetting` 类。调整 import：
- `from api_service.common.infra.db.base import Base`
- `from api_service.common.utils.timezone import now`
- `__tablename__ = "routing_settings"` 保持不变
- `key` 为 primary_key（非 Snowflake ID）
</action>
<acceptance_criteria>
- 类名为 `RoutingSetting`，`__tablename__ == "routing_settings"`
- `key` 列为 `String(64), primary_key=True`
- 包含 `value`, `value_type`, `group_name`, `label`, `sort_order`, `updated_by`, `updated_at` 列
- `ForeignKey("admin_users.id", ondelete="SET NULL")` 存在
</acceptance_criteria>
</task>

<task id="03-01-16">
<title>创建 models/__init__.py 统一导出</title>
<read_first>
- services/user-service/src/models/__init__.py
- services/admin-service/src/models/__init__.py
- services/api-service/api_service/models/__init__.py
</read_first>
<action>
重写 `api_service/models/__init__.py`，导出所有 16 个 model 类 + enums：

导出列表（按域分组）：
- User domain: User, UserSession, EmailVerificationCode, UserApiKey, BalanceTransaction, TopupOrder, ApiCallLog, UsageStat, VoucherRedemptionCode
- Admin domain: AdminUser, AdminAuditLog, AuditActionDefinition, ModelVendor, ModelCategory, ModelCatalog, ModelCatalogCategoryMap, Pool, PoolModelConfig, PoolAccount, RoutingSetting
- Enums: AdminRole, AdminStatus, PoolAccountStatus

定义 `__all__` 列表包含所有导出名称。
</action>
<acceptance_criteria>
- `from api_service.models import User, AdminUser, ModelCatalog, PoolModelConfig, ModelCatalogCategoryMap` 成功
- `from api_service.models import AdminRole, AdminStatus, PoolAccountStatus` 成功
- `__all__` 包含 22 个名称（19 个 model 类 + 3 个 enum）
- 不存在 `SupportedModel`, `SupportedModelCategoryMap`, `PoolModel` 名称
</acceptance_criteria>
</task>

<task id="03-01-17">
<title>编写 model 导入测试</title>
<read_first>
- services/api-service/api_service/models/__init__.py
- services/api-service/tests/test_models_import.py
</read_first>
<action>
创建 `services/api-service/tests/test_models_import.py`，验证：
1. 所有 19 个 model 类可从 `api_service.models` 导入
2. 无循环依赖（import 不抛异常）
3. 每个 model 的 `__tablename__` 与预期一致
4. `Base.metadata.tables` 包含全部 22 张表名
5. 重命名类不存在于导出中（SupportedModel, SupportedModelCategoryMap, PoolModel）

使用 pytest 参数化测试覆盖所有 model。
</action>
<acceptance_criteria>
- `pytest services/api-service/tests/test_models_import.py -v` 全部通过
- 测试覆盖 19 个 model 类的 `__tablename__` 断言
- 测试验证 `Base.metadata.tables` 包含 22 张表
- 测试验证旧类名不在 `api_service.models.__all__` 中
</acceptance_criteria>
</task>

## 验证标准

```bash
# 1. 所有 model 可导入
python -c "from api_service.models import User, AdminUser, ModelCatalog, PoolModelConfig, ModelCatalogCategoryMap, RoutingSetting"

# 2. 无循环依赖
python -c "import api_service.models"

# 3. metadata 包含所有表
python -c "from api_service.common.infra.db.base import Base; import api_service.models; assert len(Base.metadata.tables) == 22, f'Expected 22, got {len(Base.metadata.tables)}'"

# 4. 测试通过
cd services/api-service && python -m pytest tests/test_models_import.py -v
```

## must_haves

- 所有 19 个 ORM model 类可从 `api_service.models` 无错误导入
- 3 个类按 D-01 正确重命名（ModelCatalog, ModelCatalogCategoryMap, PoolModelConfig）
- `Base.metadata.tables` 包含全部 22 张表名
- 无循环 import 错误
- 所有 model 继承 `api_service.common.infra.db.base.Base`

---
phase: 08-inference-service-update
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/api-service/api_service/services/admin/routing_setting_service.py
  - services/api-service/api_service/controllers/internal.py
  - services/api-service/api_service/core/router.py
  - services/api-service/tests/test_routing_setting_service.py
  - services/api-service/tests/test_internal_endpoint.py
autonomous: true
requirements: [INTL-01]

must_haves:
  truths:
    - "GET /api/v1/internal/routing-config/active/inference returns 200 with valid HMAC headers"
    - "Request without HMAC headers returns 403"
    - "Request with wrong caller (not inference-service) returns 403"
    - "Response contains version, status, route_order, weights, score_bands, tier_model_map fields"
  artifacts:
    - path: "services/api-service/api_service/controllers/internal.py"
      provides: "Internal HMAC-protected endpoint for inference-service"
      exports: ["router"]
    - path: "services/api-service/api_service/services/admin/routing_setting_service.py"
      provides: "resolve_for_internal static method"
      contains: "resolve_for_internal"
    - path: "services/api-service/tests/test_internal_endpoint.py"
      provides: "Integration test for internal endpoint"
  key_links:
    - from: "services/api-service/api_service/controllers/internal.py"
      to: "api_service.services.admin.routing_setting_service"
      via: "RoutingSettingService.resolve_for_internal(db)"
      pattern: "RoutingSettingService\\.resolve_for_internal"
    - from: "services/api-service/api_service/controllers/internal.py"
      to: "api_service.common.http.internal_auth"
      via: "build_internal_auth_dependency"
      pattern: "build_internal_auth_dependency"
    - from: "services/api-service/api_service/core/router.py"
      to: "api_service.controllers.internal"
      via: "include_router"
      pattern: "internal\\.router"
---

<objective>
Port `resolve_for_internal` to api-service RoutingSettingService, create the HMAC-protected `/api/v1/internal/routing-config/active/inference` endpoint, and mount it on the api_router.

Purpose: Provide inference-service with a signed internal endpoint to fetch routing configuration from api-service (replacing admin-service).
Output: Working internal endpoint with HMAC auth, tested.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<interfaces>
<!-- Key types and contracts the executor needs -->

From api_service/common/http/internal_auth.py:
```python
def build_internal_auth_dependency(
    expected_secret: str,
    *,
    request_ttl_seconds: int = 30,
    allowed_callers: set[str] | None = None,
) -> Callable
```

From api_service/core/config.py:
```python
settings = get_settings()  # has .INTERNAL_SECRET, .INTERNAL_REQUEST_TTL_SECONDS
```

From api_service/core/db.py:
```python
async def get_db() -> AsyncGenerator[AsyncSession, None]
```

From admin-service source (resolve_for_internal return shape):
```python
{
    "router_alias": str,
    "user_facing_aliases": list[str],
    "route_order": list[str],
    "weights": dict[str, float],
    "score_bands": str,
    "tier_model_map": dict[str, str],
    "default_user_rpm": int,
    "system_rpm_cap": int,
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Port resolve_for_internal and update test</name>
  <files>
    services/api-service/api_service/services/admin/routing_setting_service.py,
    services/api-service/tests/test_routing_setting_service.py
  </files>
  <read_first>
    services/admin-service/src/services/routing_setting_service.py (lines 186-240, source implementation),
    services/api-service/api_service/services/admin/routing_setting_service.py (current state),
    services/api-service/tests/test_routing_setting_service.py (test to update),
    services/api-service/api_service/repositories/routing_setting_repository.py (get_all method signature)
  </read_first>
  <action>
    Port `resolve_for_internal` static method from admin-service `RoutingSettingService` (lines 186-240) into api-service `RoutingSettingService` class. The method signature is `async def resolve_for_internal(db: AsyncSession) -> dict[str, Any]`. It calls `RoutingSettingRepository(db).get_all()`, builds a dict with keys: router_alias, user_facing_aliases, route_order, weights, score_bands, tier_model_map, default_user_rpm, system_rpm_cap. Use the existing `FIVEWAY_ROUTE_ORDER` constant already defined in the file.

    Add `from typing import Any` import if not present.

    Update the module docstring to remove the "NOT ported" language about resolve_for_internal (Phase 8 now ports it).

    In the test file, replace `test_resolve_for_internal_not_present` with `test_resolve_for_internal_present` that asserts `hasattr(RoutingSettingService, "resolve_for_internal")` is True. Also add a test `test_resolve_for_internal_returns_expected_keys` that mocks `RoutingSettingRepository.get_all()` returning a list of mock setting objects and verifies the returned dict has all 8 expected keys.
  </action>
  <verify>
    <automated>cd /root/autodl-tmp/eucalAI_backend/services/api-service && python -m pytest tests/test_routing_setting_service.py -x -q 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `RoutingSettingService.resolve_for_internal` exists as a staticmethod accepting `(db: AsyncSession) -> dict[str, Any]`
    - The method body matches admin-service lines 186-240 logic (builds weights from weight_{route}, tier_model_map from tier_{1-5}_model, parses router_alias, user_facing_aliases, default_user_rpm, system_rpm_cap, score_bands)
    - `test_resolve_for_internal_not_present` is removed
    - `test_resolve_for_internal_present` passes asserting the method exists
    - `test_resolve_for_internal_returns_expected_keys` passes verifying all 8 keys in return dict
    - All existing tests in the file still pass
  </acceptance_criteria>
  <done>resolve_for_internal ported, old negative test replaced with positive tests, all tests green</done>
</task>

<task type="auto">
  <name>Task 2: Create internal controller and mount on router</name>
  <files>
    services/api-service/api_service/controllers/internal.py,
    services/api-service/api_service/core/router.py,
    services/api-service/tests/test_internal_endpoint.py
  </files>
  <read_first>
    services/admin-service/src/controllers/internal.py (lines 66-156, reference implementation),
    services/api-service/api_service/common/http/internal_auth.py (build_internal_auth_dependency),
    services/api-service/api_service/core/router.py (current state, Phase 8 placeholder),
    services/api-service/api_service/core/config.py (settings import pattern)
  </read_first>
  <action>
    Create `services/api-service/api_service/controllers/internal.py` with:
    1. `router = APIRouter(prefix="/internal", tags=["internal"])`
    2. `verify_routing_config_inference = build_internal_auth_dependency(settings.INTERNAL_SECRET, request_ttl_seconds=settings.INTERNAL_REQUEST_TTL_SECONDS, allowed_callers={"inference-service"})` — per D-03
    3. `InternalRoutingConfigInference` Pydantic BaseModel with fields: version (int), status (str), route_order (list[str]), weights (dict[str, float]), score_bands (str), tier_model_map (dict[str, str])
    4. GET endpoint at `/routing-config/active/inference` with response_model=InternalRoutingConfigInference, depends on verify_routing_config_inference and get_db. Calls `RoutingSettingService.resolve_for_internal(db)` and constructs response with version=0, status="active", plus route_order/weights/score_bands/tier_model_map from the base dict.

    Imports: `from api_service.core.config import settings`, `from api_service.core.db import get_db`, `from api_service.common.http.internal_auth import build_internal_auth_dependency`, `from api_service.services.admin.routing_setting_service import RoutingSettingService`.

    Update `services/api-service/api_service/core/router.py`:
    - Add import: `from api_service.controllers import internal`
    - Replace `# Phase 8: Internal routes` comment with `api_router.include_router(internal.router)  # Phase 8: Internal HMAC endpoints`

    Create test file `services/api-service/tests/test_internal_endpoint.py`:
    - Test that the endpoint returns 403 when no HMAC headers are provided (use TestClient)
    - Test that the endpoint returns 200 with correct HMAC signature (mock RoutingSettingService.resolve_for_internal to return a valid dict, use the signing primitives from internal_signing.py to generate valid headers)
    - Verify response body has all 6 InternalRoutingConfigInference fields
  </action>
  <verify>
    <automated>cd /root/autodl-tmp/eucalAI_backend/services/api-service && python -m pytest tests/test_internal_endpoint.py -x -q 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File `api_service/controllers/internal.py` exists with `router` exported
    - Endpoint path is `/api/v1/internal/routing-config/active/inference` (prefix /api/v1 from api_router + /internal from internal router + /routing-config/active/inference) — per D-01
    - `allowed_callers={"inference-service"}` — per D-03
    - `core/router.py` imports and includes `internal.router`
    - Test confirms 403 without HMAC headers
    - Test confirms 200 with valid HMAC headers and response contains keys: version, status, route_order, weights, score_bands, tier_model_map
    - `python -c "from api_service.controllers.internal import router"` succeeds
  </acceptance_criteria>
  <done>Internal endpoint live at /api/v1/internal/routing-config/active/inference, HMAC-protected, tests pass</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| inference-service → api-service | Internal HMAC-signed request crosses service boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Spoofing | /internal/routing-config/active/inference | mitigate | HMAC signature verification via build_internal_auth_dependency with 30s TTL |
| T-08-02 | Tampering | HMAC signature | mitigate | hmac.compare_digest prevents timing attacks; TTL prevents replay |
| T-08-03 | Information Disclosure | routing config response | accept | Internal network only; config data is not PII; HMAC prevents unauthorized access |
| T-08-04 | Elevation of Privilege | allowed_callers bypass | mitigate | allowed_callers={"inference-service"} restricts to single known caller |
</threat_model>

<verification>
```bash
cd /root/autodl-tmp/eucalAI_backend/services/api-service
python -m pytest tests/test_routing_setting_service.py tests/test_internal_endpoint.py -x -q
python -c "from api_service.controllers.internal import router; print('OK')"
```
</verification>

<success_criteria>
- resolve_for_internal method exists and returns correct dict shape
- Internal endpoint responds 200 to valid HMAC requests from inference-service
- Internal endpoint responds 403 to unauthenticated or wrong-caller requests
- All tests pass
</success_criteria>

<output>
Create `.planning/phases/08-inference-service-update/08-01-SUMMARY.md` when done
</output>

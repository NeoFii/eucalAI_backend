---
phase: 04-user-domain-controllers
plan: 04-01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/api-service/api_service/core/config.py
  - services/api-service/api_service/main.py
  - services/api-service/api_service/core/router.py
  - services/api-service/api_service/common/utils/email.py
  - services/api-service/api_service/common/utils/password_policy.py
  - services/api-service/api_service/common/utils/api_key_policy.py
  - services/api-service/api_service/core/policies.py
  - services/api-service/api_service/core/arq_pool.py
  - services/api-service/api_service/core/worker.py
  - services/api-service/api_service/core/jobs.py
  - services/api-service/api_service/schemas/__init__.py
  - services/api-service/api_service/schemas/common.py
  - services/api-service/api_service/schemas/auth.py
  - services/api-service/api_service/services/__init__.py
  - services/api-service/api_service/services/email_service.py
  - services/api-service/api_service/services/auth_service.py
  - services/api-service/api_service/controllers/__init__.py
  - services/api-service/api_service/controllers/auth.py
  - services/api-service/pyproject.toml
  - services/api-service/tests/conftest.py
  - services/api-service/tests/test_auth_register.py
  - services/api-service/tests/test_auth_login.py
  - services/api-service/tests/test_auth_logout.py
  - services/api-service/tests/test_auth_refresh.py
  - services/api-service/tests/test_auth_me.py
  - services/api-service/tests/test_auth_change.py
  - services/api-service/tests/test_auth_reset.py
  - services/api-service/tests/test_email_send.py
  - services/api-service/tests/test_email_verify.py
autonomous: true
requirements: [USER-01, USER-06]
requirements_addressed: [USER-01, USER-06]
tags: [auth, arq, foundation, schemas, settings, worker]

must_haves:
  truths:
    - "D-01: no `internal_*.py` controllers/services/schemas are migrated to api-service — Phase 4 scope explicitly excludes the 7 internal user-service files (Phase 5 admin will call services directly in-process)"
    - "D-02: email_service.send_verification_code writes the code row + commits, then calls `pool.enqueue_job('send_verification_email', ...)` — NO synchronous SMTP send remains on the request thread"
    - "D-03: this plan ports `services/user-service/src/schemas/auth.py` and `schemas/common.py` 1:1 (request/response field-for-field) — no schema consolidation, no admin schemas (deferred to Phase 5 per D-10)"
    - "D-08: this is plan 1 of exactly 3 plans for Phase 4 (auth + foundations; 04-02 = keys+billing; 04-03 = model catalog + final wiring)"
    - "D-12: Wave 0 foundations (settings keys, common/utils/{email,password_policy,api_key_policy}.py, ARQ pool, worker scaffold, schemas/common.py, conftest, pytest config) land in Task 1 of THIS plan BEFORE any service/controller code in Task 2/3 — no other plan touches these"
    - "POST /api/v1/auth/register creates a user, sets HttpOnly+SameSite user_access_token/user_refresh_token cookies, returns 201, and enqueues an ARQ job (no synchronous SMTP send)"
    - "POST /api/v1/auth/login returns 200, sets the two cookies, locks the account after settings.LOGIN_MAX_FAILURES failures"
    - "POST /api/v1/auth/logout revokes the user_sessions row by token_jti and clears both cookies"
    - "POST /api/v1/auth/refresh rotates BOTH access and refresh tokens, updates session.token_jti + refresh_token_hash + expires_at in place"
    - "GET /api/v1/auth/me returns user_uid (never user_id), rpm_limit, default_rpm=settings.DEFAULT_USER_RPM, current_tpm via BillingRepository.stat_get_user_tpm_last_minute"
    - "POST /api/v1/auth/send-email-code enqueues the ARQ job 'send_verification_email' and rate-limits at settings.CODE_DAILY_SEND_LIMIT per (email,purpose) per UTC day"
    - "POST /api/v1/auth/verify-email increments error_count on wrong code; after settings.MAX_CODE_ERRORS, sets locked_until = now + CODE_ERROR_LOCK_HOURS and commits via the inner db.commit() preserved per D-11"
    - "POST /api/v1/auth/change-password revokes all active sessions for the user"
    - "ARQ pool lifecycle is bound to the FastAPI lifespan (init priority=40, shutdown reverse-order); get_arq_pool() returns the live ArqRedis instance after startup"
  artifacts:
    - path: "services/api-service/api_service/core/arq_pool.py"
      provides: "init_arq_pool / close_arq_pool / get_arq_pool / _build_redis_settings"
      contains: "create_pool"
    - path: "services/api-service/api_service/core/worker.py"
      provides: "WorkerSettings class for `arq api_service.core.worker.WorkerSettings`"
      contains: "WorkerSettings"
    - path: "services/api-service/api_service/core/jobs.py"
      provides: "send_verification_email + 4 cron jobs (aggregate_usage_stats, cleanup_expired_verification_codes, cleanup_expired_sessions, reconcile_balance_ledger) + get_worker_settings_kwargs"
      contains: "send_verification_email"
    - path: "services/api-service/api_service/core/policies.py"
      provides: "require_active_user dependency wrapping get_current_user"
      contains: "require_active_user"
    - path: "services/api-service/api_service/common/utils/email.py"
      provides: "normalize_email"
      min_lines: 5
    - path: "services/api-service/api_service/common/utils/password_policy.py"
      provides: "check_password_strength"
      contains: "check_password_strength"
    - path: "services/api-service/api_service/common/utils/api_key_policy.py"
      provides: "normalize_allowed_models, normalize_allow_ips, is_model_allowed, is_ip_allowed"
      contains: "normalize_allowed_models"
    - path: "services/api-service/api_service/schemas/common.py"
      provides: "ApiResponse[T], DateTimeModel, AuthBaseResponse, AuthErrorResponse"
      contains: "class ApiResponse"
    - path: "services/api-service/api_service/schemas/auth.py"
      provides: "all 16 request/response models from user-service/src/schemas/auth.py"
      contains: "RegisterRequest"
    - path: "services/api-service/api_service/services/email_service.py"
      provides: "EmailService (staticmethod class — no module instance, no __init__) with send_verification_code/get_valid_code_or_raise/mark_code_used"
      contains: "class EmailService"
    - path: "services/api-service/api_service/services/auth_service.py"
      provides: "AuthService.register/login/login_with_code/logout/refresh/change_password/reset_password + session helpers"
      contains: "class AuthService"
    - path: "services/api-service/api_service/controllers/auth.py"
      provides: "10 /auth/* endpoints with cookie set/clear helpers"
      contains: "_set_auth_cookies"
    - path: "services/api-service/tests/conftest.py"
      provides: "shared pytest fixtures: mock_user, mock_db, arq_pool_mock"
      contains: "mock_user"
  key_links:
    - from: "services/api-service/api_service/main.py"
      to: "services/api-service/api_service/core/arq_pool.py"
      via: "LifespanRegistry.register('arq_pool', ..., priority=40)"
      pattern: "registry\\.register\\(\"arq_pool\""
    - from: "services/api-service/api_service/services/email_service.py"
      to: "services/api-service/api_service/core/arq_pool.py"
      via: "get_arq_pool() + pool.enqueue_job('send_verification_email', ...)"
      pattern: "enqueue_job\\(\\s*_JOB_SEND_VERIFICATION_EMAIL"
    - from: "services/api-service/api_service/core/jobs.py"
      to: "services/api-service/api_service/core/worker.py"
      via: "get_worker_settings_kwargs() exposes 'functions' list including send_verification_email"
      pattern: "send_verification_email"
    - from: "services/api-service/api_service/controllers/auth.py"
      to: "services/api-service/api_service/services/auth_service.py"
      via: "await AuthService.register(db, request) etc"
      pattern: "AuthService\\.(register|login|logout|refresh|change_password|reset_password)"
    - from: "services/api-service/api_service/controllers/auth.py"
      to: "services/api-service/api_service/repositories/billing_repository.py"
      via: "/auth/me uses BillingRepository.stat_get_user_tpm_last_minute (NOT UsageStatRepository)"
      pattern: "stat_get_user_tpm_last_minute"
    - from: "services/api-service/api_service/core/router.py"
      to: "services/api-service/api_service/controllers/auth.py"
      via: "api_router.include_router(auth.router)"
      pattern: "include_router\\(auth\\.router\\)"
---

<objective>
Lay the Phase 4 foundation (Wave 0 inside this plan) and deliver the user-auth surface (Wave 1) so that all `/auth/*` endpoints work identically to user-service except email sending is asynchronous via ARQ.

Purpose: Unblock 04-02 (keys/billing) and 04-03 (model catalog/email) by landing every shared dependency they need — settings keys, utility modules, schemas/common.py, ARQ pool wiring, worker scaffolding, pytest infrastructure — and then port the largest service (auth_service.py, 393 lines) plus controller, schema and email service together.

Output: 10 working /auth/* endpoints, an ARQ worker module runnable via `arq api_service.core.worker.WorkerSettings`, 9 new pytest files + conftest.py covering USER-01 and USER-06, and the foundational scaffolding (`core/arq_pool.py`, `core/worker.py`, `core/jobs.py`, `core/policies.py`, `common/utils/{email,password_policy,api_key_policy}.py`, `schemas/common.py`, `schemas/auth.py`, expanded ApiServiceSettings) required by 04-02 and 04-03.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-user-domain-controllers/04-CONTEXT.md
@.planning/phases/04-user-domain-controllers/04-RESEARCH.md
@.planning/phases/04-user-domain-controllers/04-PATTERNS.md
@.planning/phases/04-user-domain-controllers/04-VALIDATION.md

# Project conventions
@CLAUDE.md
@services/user-service/CLAUDE.md

# Current target state (read before modifying)
@services/api-service/api_service/main.py
@services/api-service/api_service/core/router.py
@services/api-service/api_service/core/config.py
@services/api-service/api_service/core/db.py
@services/api-service/api_service/core/dependencies/user.py
@services/api-service/api_service/common/config.py
@services/api-service/api_service/common/security/jwt.py
@services/api-service/api_service/common/security/password.py
@services/api-service/api_service/common/core/exceptions.py
@services/api-service/api_service/common/observability.py
@services/api-service/api_service/common/infra/cache.py
@services/api-service/api_service/common/infra/redis.py
@services/api-service/api_service/common/api/pagination.py
@services/api-service/api_service/repositories/user_repository.py
@services/api-service/api_service/repositories/billing_repository.py
@services/api-service/api_service/models/__init__.py
@services/api-service/pyproject.toml
@services/api-service/tests/test_auth_dependencies.py

# Migration sources (truth-of-source per CONTEXT canonical_refs)
@services/user-service/src/controllers/auth.py
@services/user-service/src/services/auth_service.py
@services/user-service/src/services/email_service.py
@services/user-service/src/schemas/auth.py
@services/user-service/src/schemas/common.py
@services/user-service/src/schemas/__init__.py
@services/user-service/src/core/policies.py
@services/user-service/src/core/worker.py
@services/user-service/src/core/jobs.py
@services/user-service/src/core/config.py
@services/user-service/src/utils/email.py
@services/user-service/src/utils/password.py
@services/user-service/src/utils/api_key_policy.py

<interfaces>
<!-- Existing api-service exports — executor uses these directly. No exploration needed. -->

From api_service/repositories/user_repository.py (Phase 3 merged repo — used by auth_service + email_service):
- get_by_email(email: str) -> User | None
- get_by_uid(uid: str) -> User | None
- get_by_id(user_id: int, *, for_update: bool = False) -> User | None
- add(user: User) -> None
- get_session_by_token_jti(jti: str) -> UserSession | None
- list_active_sessions_for_user(user_id: int) -> list[UserSession]
- revoke_session(session: UserSession) -> None
- add_session(session: UserSession) -> None
- email_code_count_created_since(email: str, purpose: str, since: datetime) -> int
- email_code_latest_for_email(email: str, purpose: str) -> EmailVerificationCode | None
- email_code_latest_unused_for_email(email: str, purpose: str, *, for_update: bool = False) -> EmailVerificationCode | None
- email_code_list_unused_for_email(email: str, purpose: str) -> list[EmailVerificationCode]
- email_code_add(record: EmailVerificationCode) -> None
- async email_code_delete(record: EmailVerificationCode) -> None  # NOTE: async (per RESEARCH A5)

From api_service/repositories/billing_repository.py (used by /auth/me only in this plan):
- stat_get_user_tpm_last_minute(user_id: int) -> int

From api_service/common/security/jwt.py:
- create_access_token(subject: str, expires_minutes: int) -> str
- create_refresh_token(subject: str, expires_days: int) -> str
- decode_token(token: str) -> dict
- get_token_jti(token: str) -> str

From api_service/common/security/password.py:
- async hash_password_async(plain: str) -> str
- async verify_password_async(plain: str, hashed: str) -> bool
- hash_password(plain: str) -> str  # sync (used inside ARQ job startup)

From api_service/common/core/exceptions.py (verified):
- AuthenticationException, EmailAlreadyExistsException, EmailNotVerifiedException, InvalidCredentialsException, InvalidTokenException, SessionExpiredException, SessionNotFoundException, SessionRevokedException, TokenExpiredException, UserDisabledException, UserNotFoundException, WeakPasswordException, ServiceUnavailableException, CodeExpiredException, CodeNotFoundException, InvalidCodeException, ValidationException, NotFoundException

From api_service/core/dependencies/user.py (Phase 3 D-06/D-08):
- async get_current_user(request, credentials, access_token, db) -> User  # NO blacklist check (user domain)

From api_service/core/db.py:
- async get_db() -> AsyncGenerator[AsyncSession, None]  # rollback-on-exception only
- get_db_context() -> async context manager (used by ARQ jobs)
- create_engine(url, ...), init_session_factory(), close_db()
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 foundations — settings, utils, schemas/common.py, policies, pytest config, conftest, ARQ pool + worker scaffold + 4 cron jobs (no send_verification_email yet)</name>
  <files>
    services/api-service/api_service/core/config.py,
    services/api-service/api_service/common/utils/email.py,
    services/api-service/api_service/common/utils/password_policy.py,
    services/api-service/api_service/common/utils/api_key_policy.py,
    services/api-service/api_service/core/policies.py,
    services/api-service/api_service/core/arq_pool.py,
    services/api-service/api_service/core/worker.py,
    services/api-service/api_service/core/jobs.py,
    services/api-service/api_service/schemas/__init__.py,
    services/api-service/api_service/schemas/common.py,
    services/api-service/api_service/main.py,
    services/api-service/pyproject.toml,
    services/api-service/tests/conftest.py
  </files>
  <read_first>
    # Target files (current state — see what exists before modifying)
    services/api-service/api_service/core/config.py,
    services/api-service/api_service/main.py,
    services/api-service/api_service/common/config.py,
    services/api-service/api_service/common/infra/cache.py,
    services/api-service/api_service/core/db.py,
    services/api-service/api_service/core/dependencies/user.py,
    services/api-service/api_service/common/utils/timezone.py,
    services/api-service/api_service/common/observability.py,
    services/api-service/pyproject.toml,
    services/api-service/tests/test_auth_dependencies.py,
    # Source analogs (truth-of-source for the port)
    services/user-service/src/utils/email.py,
    services/user-service/src/utils/password.py,
    services/user-service/src/utils/api_key_policy.py,
    services/user-service/src/core/policies.py,
    services/user-service/src/core/worker.py,
    services/user-service/src/core/jobs.py,
    services/user-service/src/core/config.py,
    services/user-service/src/schemas/common.py,
    # Migration playbook
    .planning/phases/04-user-domain-controllers/04-RESEARCH.md,
    .planning/phases/04-user-domain-controllers/04-PATTERNS.md,
    .planning/phases/04-user-domain-controllers/04-CONTEXT.md
  </read_first>
  <action>
    Land the entire Wave 0 foundation in one task (per D-12). Do this in order:

    1. **Settings (D-09):** Append the 10 missing keys to `ApiServiceSettings` in `api_service/core/config.py` per RESEARCH.md § Settings Gap. Exact defaults (do not invent):
       `LOGIN_LOCK_DURATION_HOURS: int = 1`, `MAX_CODE_ERRORS: int = 5`, `CODE_ERROR_LOCK_HOURS: int = 24`, `CODE_DAILY_SEND_LIMIT: int = 3`,
       `VERIFICATION_CODE_RETENTION_DAYS: int = 7`, `MIN_TOPUP_AMOUNT: int = 1_000_000`, `MAX_TOPUP_AMOUNT: int = 10_000_000_000`,
       `USER_WORKER_CONCURRENCY: int = 5`, `USER_JOB_TIMEOUT_SECONDS: int = 300`, `DEFAULT_USER_RPM: int = 20` (per D-09 — drop-in for system_settings_gateway; add `# TODO(phase-5): read from system_settings table` comment).
       Do NOT touch the existing RATE_LIMIT_DEFAULT_USER_RPM (Relay scope — different setting).

    2. **Common utils** — port verbatim per PATTERNS.md analog rows:
       - `common/utils/email.py`: copy from `services/user-service/src/utils/email.py` (single function `normalize_email`).
       - `common/utils/password_policy.py`: copy from `services/user-service/src/utils/password.py` (function `check_password_strength`) but rename the file to avoid clash with existing `common/security/password.py`. Rewrite `from core.config import settings` → `from api_service.core.config import settings`. Per RESEARCH § Import Translation Table.
       - `common/utils/api_key_policy.py`: copy from `services/user-service/src/utils/api_key_policy.py` verbatim (stdlib `ipaddress` only — zero import rewrites needed).

    3. **Policies:** Create `core/policies.py` with `require_active_user(current_user: User = Depends(get_current_user)) -> User` raising `UserDisabledException` for status==0 and `EmailNotVerifiedException` for status==2, per PATTERNS.md § core/policies.py. Imports: `from api_service.common.core.exceptions import EmailNotVerifiedException, UserDisabledException`, `from api_service.core.dependencies.user import get_current_user`, `from api_service.models import User`.

    4. **Schemas common.py + __init__.py:**
       - `schemas/__init__.py`: empty package marker for now (re-exports get added in later tasks).
       - `schemas/common.py`: copy verbatim from `services/user-service/src/schemas/common.py` (40 lines: `ApiResponse[T]`, `DateTimeModel`, `AuthBaseResponse`, `AuthErrorResponse`). Rewrite the single import `from common.utils.timezone import format_iso` → `from api_service.common.utils.timezone import format_iso`. **CRITICAL — Pitfall 7:** keep the `list(data.items())` copy in `DateTimeModel.serialize_model` exactly as-is — do NOT lint-clean to iterate `data.items()` directly (will RuntimeError on dicts with many datetime fields).

    5. **ARQ pool (`core/arq_pool.py`)** — new module composed from two analogs per PATTERNS.md. Mirror the `init_/get_/close_` accessor shape of `api_service/common/infra/cache.py:13-43`, but use `arq.create_pool(_build_redis_settings())` instead of `aioredis.from_url`. The `_build_redis_settings()` helper parses `settings.WORKER_QUEUE_REDIS_URL` (NOT `USER_QUEUE_REDIS_URL`) into `arq.connections.RedisSettings` (host/port/database/username/password/ssl). `close_arq_pool()` calls `_arq_pool.close()` (no `aclose` on `ArqRedis`). `get_arq_pool()` raises `RuntimeError("ARQ pool not initialised — call init_arq_pool() first")` if `_arq_pool is None`.

    6. **Worker entrypoint (`core/worker.py`)** — port from `services/user-service/src/core/worker.py` with import rewrites: `from api_service.common.observability import configure_logging_from_settings`, `from api_service.core.config import settings`, `from api_service.core.jobs import get_worker_settings_kwargs`, `import api_service.models  # noqa: F401`. Class `WorkerSettings: pass`, then `setattr` loop applying `get_worker_settings_kwargs()`.

    7. **Jobs module (`core/jobs.py`)** — port from `services/user-service/src/core/jobs.py` per PATTERNS.md § core/jobs.py:
       - Port `build_redis_settings()` (same parsing logic as `arq_pool._build_redis_settings`; both live side-by-side as in source).
       - Port `on_worker_startup(ctx)` and `on_worker_shutdown(ctx)` verbatim with import rewrites; they call `create_engine`/`init_session_factory`/`close_db` from `api_service.core.db`.
       - Port the 4 existing cron jobs verbatim: `aggregate_usage_stats`, `cleanup_expired_verification_codes`, `cleanup_expired_sessions`, `reconcile_balance_ledger`. Rewrite all `from services.X import Y` to `from api_service.services.X import Y` and any `from repositories import …` to specific module imports.
       - **Do NOT add `send_verification_email` in this task** — it is added in Task 2 alongside `email_service.py` to keep the diff focused.
       - `get_worker_settings_kwargs()` returns `{"functions": [aggregate_usage_stats, cleanup_expired_verification_codes, cleanup_expired_sessions, reconcile_balance_ledger], "redis_settings": build_redis_settings(), "max_jobs": settings.USER_WORKER_CONCURRENCY, "job_timeout": settings.USER_JOB_TIMEOUT_SECONDS, "on_startup": on_worker_startup, "on_shutdown": on_worker_shutdown, "cron_jobs": [...]}` — copy `cron_jobs` list verbatim from source.

    8. **Lifespan registration in `main.py`:** Append `_init_arq_pool` and `_shutdown_arq_pool` async helpers that call `init_arq_pool()` / `close_arq_pool()`, then `registry.register("arq_pool", init_fn=_init_arq_pool, shutdown_fn=_shutdown_arq_pool, priority=40)` immediately after the existing `cache_redis` (priority=30) registration block. Priority 40 ensures ARQ starts after Redis(30)+DB(20) and shuts down before them (reverse-priority order — verified in `core/lifespan.py`).

    9. **pyproject.toml — pytest config:** Add `[tool.pytest.ini_options]` block with `asyncio_mode = "auto"`, `asyncio_default_fixture_loop_scope = "function"`, `testpaths = ["tests"]`. Per VALIDATION.md § Wave 0 Requirements and STATE.md note about pytest-asyncio strict mode.

    10. **tests/conftest.py:** Create shared fixtures per VALIDATION.md § Wave 0:
        - `mock_user`: MagicMock with `id=1`, `uid="u_test01"`, `status=1`, `email="test@example.com"`, `email_verified_at=now()`, `rpm_limit=20`.
        - `mock_db`: `AsyncMock()` (no real DB — tests in this phase use mocked repos, mirroring `tests/test_auth_dependencies.py:80-91`).
        - `arq_pool_mock`: `AsyncMock()` exposing `.enqueue_job` for assertion. Use `@pytest.fixture` (sync def, returns AsyncMock).
        - `redis_mock`: `AsyncMock()` for cache layer.

    Use `log_event(logger, logging.INFO, "arqPoolInitialised")` in `init_arq_pool` and `log_event(logger, logging.INFO, "arqPoolClosed")` in `close_arq_pool` — mirror the cache.py logging discipline (no string interpolation).

    DO NOT use heredoc/`echo` for any file creation — use Write/Edit tools only.
  </action>
  <verify>
    <automated>
      cd services/api-service &&
      python -c "from api_service.core.config import settings; assert settings.DEFAULT_USER_RPM == 20; assert settings.CODE_DAILY_SEND_LIMIT == 3; assert settings.MAX_CODE_ERRORS == 5; assert settings.MIN_TOPUP_AMOUNT == 1_000_000; print('settings OK')" &&
      python -c "from api_service.common.utils.email import normalize_email; assert normalize_email('  Foo@BAR.COM ') == 'foo@bar.com'" &&
      python -c "from api_service.common.utils.password_policy import check_password_strength; ok, _ = check_password_strength('Abcdefg1!', lang='en'); assert ok is True" &&
      python -c "from api_service.common.utils.api_key_policy import normalize_allowed_models, normalize_allow_ips; assert normalize_allowed_models('a, b, a') == 'a,b'" &&
      python -c "from api_service.core.policies import require_active_user; assert callable(require_active_user)" &&
      python -c "from api_service.core.arq_pool import init_arq_pool, close_arq_pool, get_arq_pool, _build_redis_settings; rs = _build_redis_settings(); assert rs.host" &&
      python -c "from api_service.core import worker; assert hasattr(worker, 'WorkerSettings')" &&
      python -c "from api_service.core.jobs import get_worker_settings_kwargs, aggregate_usage_stats, cleanup_expired_verification_codes, cleanup_expired_sessions, reconcile_balance_ledger; kw = get_worker_settings_kwargs(); assert 'functions' in kw and len(kw['functions']) == 4" &&
      python -c "from api_service.schemas.common import ApiResponse, DateTimeModel, AuthBaseResponse, AuthErrorResponse; r = ApiResponse[int](code=200, message='ok', data=1); assert r.data == 1" &&
      grep -n "registry.register(\"arq_pool\"" services/api-service/api_service/main.py | grep -q "priority=40" &&
      grep -q "asyncio_mode" services/api-service/pyproject.toml &&
      grep -q "list(data.items())" services/api-service/api_service/schemas/common.py &&
      grep -rEn "from common\\.(utils|core|infra|api|cache|db|observability)\\." services/api-service/api_service/{core/policies.py,core/arq_pool.py,core/worker.py,core/jobs.py,common/utils/email.py,common/utils/password_policy.py,common/utils/api_key_policy.py,schemas/common.py} | { ! grep -q "from common\\." ; } &&
      cd services/api-service && pytest tests/ --collect-only -q | head -40 &&
      cd services/api-service && pytest tests/test_auth_dependencies.py tests/test_health.py tests/test_models_import.py tests/test_repositories_import.py tests/test_snowflake_worker.py -x -q
    </automated>
  </verify>
  <acceptance_criteria>
    - `services/api-service/api_service/core/config.py` defines all 10 new settings keys with exact defaults from RESEARCH.md § Settings Gap (D-09: DEFAULT_USER_RPM=20).
    - `services/api-service/api_service/common/utils/email.py`, `password_policy.py`, `api_key_policy.py` exist and import without raising (`python -c "import api_service.common.utils.email as m; import api_service.common.utils.password_policy as m; import api_service.common.utils.api_key_policy as m"` exits 0).
    - `core/policies.py` exports `require_active_user`; it imports `EmailNotVerifiedException`, `UserDisabledException` from `api_service.common.core.exceptions` and `get_current_user` from `api_service.core.dependencies.user`.
    - `core/arq_pool.py` exposes `init_arq_pool`, `close_arq_pool`, `get_arq_pool`, `_build_redis_settings` and parses `settings.WORKER_QUEUE_REDIS_URL` (grep finds `WORKER_QUEUE_REDIS_URL` in the file; grep does NOT find `USER_QUEUE_REDIS_URL`).
    - `core/worker.py` defines `WorkerSettings` class, imports `get_worker_settings_kwargs` from `api_service.core.jobs`.
    - `core/jobs.py` defines exactly 4 cron job coroutines (`aggregate_usage_stats`, `cleanup_expired_verification_codes`, `cleanup_expired_sessions`, `reconcile_balance_ledger`) and `get_worker_settings_kwargs()` returns `{"functions": [...], ...}` with `len(functions) == 4`. `send_verification_email` MUST NOT yet be present in this file (added in Task 2).
    - `schemas/common.py` contains `class ApiResponse(BaseModel, Generic[T])` and the `list(data.items())` iteration pattern in `DateTimeModel.serialize_model` (Pitfall 7 — grep `list(data.items())` returns a match).
    - `main.py` contains `registry.register("arq_pool", init_fn=_init_arq_pool, shutdown_fn=_shutdown_arq_pool, priority=40)` immediately after the `cache_redis` registration.
    - `pyproject.toml` has `[tool.pytest.ini_options]` with `asyncio_mode = "auto"` and `testpaths = ["tests"]`.
    - `tests/conftest.py` exports fixtures `mock_user`, `mock_db`, `arq_pool_mock`, `redis_mock` (grep finds each `@pytest.fixture` decorator above the matching function).
    - `grep -r "from common\\.\\(utils\\|core\\|infra\\|api\\|cache\\|db\\|observability\\)\\." services/api-service/api_service/{core,common/utils,schemas}/` returns no matches (Import Rewrite Map enforced — Pitfall 2).
    - All existing tests (`pytest tests/test_auth_dependencies.py tests/test_health.py tests/test_models_import.py tests/test_repositories_import.py tests/test_snowflake_worker.py -x -q`) still pass (Phase 3 regression gate).
    - VALIDATION slot T-04-01 (conftest fixtures + asyncio mode green) covered: `pytest tests/ --collect-only` exits 0.
  </acceptance_criteria>
  <done>
    Wave 0 foundations complete. All 10 settings keys present. Three utility modules exist with verbatim ports. `core/policies.py`, `core/arq_pool.py`, `core/worker.py`, `core/jobs.py` (with 4 cron jobs) created. `schemas/common.py` ports the verbatim `DateTimeModel` (Pitfall 7 preserved). ARQ pool registered in lifespan at priority 40. pytest config + conftest in place. All existing Phase 3 tests still pass. Subsequent tasks in 04-01/02/03 can now import everything they need.
  </done>
</task>

<task type="auto">
  <name>Task 2: schemas/auth.py + email_service.py + send_verification_email ARQ job + auth_service.py (port from user-service with D-02/D-09/D-11 divergences)</name>
  <files>
    services/api-service/api_service/schemas/auth.py,
    services/api-service/api_service/schemas/__init__.py,
    services/api-service/api_service/services/__init__.py,
    services/api-service/api_service/services/email_service.py,
    services/api-service/api_service/services/auth_service.py,
    services/api-service/api_service/core/jobs.py,
    services/api-service/tests/test_email_send.py,
    services/api-service/tests/test_email_verify.py
  </files>
  <read_first>
    # Source analogs (truth-of-source — port verbatim except where divergences are documented)
    services/user-service/src/schemas/auth.py,
    services/user-service/src/schemas/__init__.py,
    services/user-service/src/services/auth_service.py,
    services/user-service/src/services/email_service.py,
    # Phase 3 repositories (call signatures used by the ports)
    services/api-service/api_service/repositories/user_repository.py,
    services/api-service/api_service/repositories/billing_repository.py,
    services/api-service/api_service/common/security/jwt.py,
    services/api-service/api_service/common/security/password.py,
    services/api-service/api_service/common/utils/timezone.py,
    services/api-service/api_service/common/utils/nanoid_uid.py,
    services/api-service/api_service/common/utils/snowflake.py,
    services/api-service/api_service/common/core/exceptions.py,
    services/api-service/api_service/common/observability.py,
    services/api-service/api_service/models/__init__.py,
    # Just-created Wave 0 files
    services/api-service/api_service/core/arq_pool.py,
    services/api-service/api_service/core/jobs.py,
    services/api-service/api_service/schemas/common.py,
    services/api-service/api_service/common/utils/email.py,
    services/api-service/api_service/common/utils/password_policy.py,
    # Test mocking style
    services/api-service/tests/test_auth_dependencies.py,
    services/api-service/tests/conftest.py,
    # Playbook
    .planning/phases/04-user-domain-controllers/04-RESEARCH.md,
    .planning/phases/04-user-domain-controllers/04-PATTERNS.md
  </read_first>
  <action>
    Port schemas/auth.py + the two largest services (auth_service.py ≈ 393 LOC, email_service.py ≈ 172 LOC) plus the new ARQ job. Order:

    1. **schemas/auth.py:** Port verbatim from `services/user-service/src/schemas/auth.py` (~225 lines, 16 request/response models). Rewrite ONLY these imports per RESEARCH § Import Translation Table:
       - `from schemas.common import AuthBaseResponse, DateTimeModel` → `from api_service.schemas.common import AuthBaseResponse, DateTimeModel`
       - `from utils.email import normalize_email` → `from api_service.common.utils.email import normalize_email`
       - `from utils.password import check_password_strength` → `from api_service.common.utils.password_policy import check_password_strength` (renamed file)
       Keep ALL field validators (`@field_validator("email", mode="before")` normalizing email, `@model_validator(mode="after")` enforcing password strength, `@field_validator("confirm_password")` matching). Keep `UserInfoResponseData` fields exact: `id` (NOTE: source returns `id` AS uid — verify against source; root CLAUDE.md says responses use `user_uid` — port the source `id` field name as-is since source already complied per RESEARCH § Anti-Patterns, and verify in tests). Keep all 16 models.

    2. **services/__init__.py and schemas/__init__.py re-exports:**
       - `services/__init__.py`: empty package marker.
       - `schemas/__init__.py`: append re-exports for auth + common per PATTERNS.md § schemas/__init__.py:
         `from api_service.schemas.auth import (RegisterRequest, RegisterResponse, LoginRequest, LoginResponse, ...)` and `from api_service.schemas.common import (ApiResponse, AuthBaseResponse, AuthErrorResponse, DateTimeModel)`. Copy the exact `__all__` shape from source `services/user-service/src/schemas/__init__.py`, omitting `internal_*` entries (per D-01).

    3. **services/email_service.py (D-02 + D-11 divergences):**
       - Class `EmailService` with NO `__init__` and NO module-level `email_service = EmailService()` (per Pitfall 4 — convert from module singleton to staticmethod class).
       - Methods (all `@staticmethod`): `generate_code()`, `send_verification_code(db, email, purpose='register') -> tuple[bool, str]`, `get_valid_code_or_raise(db, email, code, purpose='register') -> EmailVerificationCode`, `mark_code_used(record)`.
       - **Repository method renames** per RESEARCH § Repository Method Translation Table:
         `EmailCodeRepository(db).count_created_since(...)` → `UserRepository(db).email_code_count_created_since(...)`
         `EmailCodeRepository(db).latest_for_email(...)` → `UserRepository(db).email_code_latest_for_email(...)`
         `EmailCodeRepository(db).latest_unused_for_email(..., for_update=True)` → `UserRepository(db).email_code_latest_unused_for_email(..., for_update=True)`
         `EmailCodeRepository(db).list_unused_for_email(...)` → `UserRepository(db).email_code_list_unused_for_email(...)`
         `repo.delete(record)` → `await UserRepository(db).email_code_delete(record)` (now ASYNC — Pitfall A5)
         `repo.add(verification)` → `UserRepository(db).email_code_add(verification)` (sync — wraps `session.add`)
       - **D-02 divergence:** Inside `send_verification_code`, after writing the row + `await db.commit()`, call `pool = get_arq_pool()` then `await pool.enqueue_job(_JOB_SEND_VERIFICATION_EMAIL, email, code, purpose)`. Define module-level constant `_JOB_SEND_VERIFICATION_EMAIL = "send_verification_email"` (Pitfall 9 — name MUST match the function `__name__` registered in jobs.py). Return `(True, "queued")`. The instance-method `_send_email` and `_send_mock` from source are NOT ported (moved to ARQ job in step 4).
       - **D-11 divergence:** Inside `get_valid_code_or_raise`, when `record.error_count >= settings.MAX_CODE_ERRORS`, set `record.locked_until = now() + timedelta(hours=settings.CODE_ERROR_LOCK_HOURS)` and call `await db.commit()` BEFORE raising `InvalidCodeException(detail="Too many invalid verification attempts")`. Add comment `# D-11: preserve inner db.commit() for 1:1 source parity` above the commit call.
       - Convert any `email_service.X(...)` callsite hint to `EmailService.X(...)` in the file (used by auth_service.py in step 5).

    4. **core/jobs.py — append `send_verification_email` ARQ job + register it:**
       - Add the module constant `_JOB_SEND_VERIFICATION_EMAIL = "send_verification_email"` (Pitfall 9 — must literally equal the function name).
       - Add `def _send_smtp_sync(email, code, purpose)` synchronous helper reading SMTP_HOST/PORT/USER/PASSWORD/TLS/FROM from settings and using stdlib `smtplib.SMTP` + `MIMEMultipart` per RESEARCH § Code Examples. If `not settings.SMTP_HOST or not settings.SMTP_USER` → log debug `"emailSendMock email=%s purpose=%s"` and return without raising (preserves source mock behavior).
       - Add `async def send_verification_email(ctx, email, code, purpose) -> None`: wraps `await asyncio.to_thread(_send_smtp_sync, email, code, purpose)`; on Exception, if `ctx.get('job_try', 1) < 3` → `raise Retry(defer=job_try * 5) from exc`; else `log_event(logger, logging.ERROR, "emailSendFailedPermanently", email=email, purpose=purpose)` and return without re-raising (D-02 acceptable tradeoff per CONTEXT.md).
       - Append `send_verification_email` to the `functions` list returned by `get_worker_settings_kwargs()` (the list now has 5 entries: 4 crons + the new job).
       - Add subject/body builder `_build_message(email, code, purpose) -> tuple[str, str]` covering purposes: `register`, `login`, `verify`, `reset` per RESEARCH § Code Examples (zh-CN copy preserved from source if present, English fallback otherwise — port source verbatim if it exists).

    5. **services/auth_service.py:** Port from `services/user-service/src/services/auth_service.py`. Apply these rewrites:
       - **Import rewrites** per RESEARCH § Import Translation Table:
         `from common.utils.jwt import …` → `from api_service.common.security.jwt import create_access_token, create_refresh_token, decode_token, get_token_jti`
         `from common.utils.password import …` → `from api_service.common.security.password import hash_password, hash_password_async, verify_password_async`
         `from common.utils.nanoid_uid import …` → `from api_service.common.utils.nanoid_uid import generate_nanoid_uid`
         `from common.utils.snowflake import …` → `from api_service.common.utils.snowflake import generate_snowflake_id`
         `from common.utils.timezone import now` → `from api_service.common.utils.timezone import now`
         `from common.observability import log_event` → `from api_service.common.observability import log_event`
         `from common.core.exceptions import …` → `from api_service.common.core.exceptions import …`
         `from utils.email import normalize_email` → `from api_service.common.utils.email import normalize_email`
         `from repositories import SessionRepository, UserRepository` → `from api_service.repositories.user_repository import UserRepository`
         `from services.email_service import email_service` → `from api_service.services.email_service import EmailService`
         **DELETE entirely:** `from gateways.system_settings import system_settings_gateway` (D-09 — use `settings.DEFAULT_USER_RPM` constant).
       - **Repository method renames** at all 5+ session_* call sites per RESEARCH § Repository Method Translation Table:
         `SessionRepository(db).get_by_token_jti(jti)` → `UserRepository(db).get_session_by_token_jti(jti)`
         `SessionRepository(db).list_active_for_user(uid)` → `UserRepository(db).list_active_sessions_for_user(uid)`
         `SessionRepository(db).revoke(session)` → `UserRepository(db).revoke_session(session)`
         `session_repo.add(session)` → `user_repo.add_session(session)`
       - **D-09 divergence in `register`:** Replace the `try/except` block that calls `await system_settings_gateway.get_default_user_rpm()` with a single line `snapshot_rpm = settings.DEFAULT_USER_RPM` followed by a comment `# TODO(phase-5): re-introduce dynamic DB read when admin domain ships`.
       - **All `email_service.X(...)` callsites → `EmailService.X(...)`** (class method calls, not module-instance).
       - Preserve verbatim (DO NOT modify): `_get_dummy_hash()` constant-time timing equalizer (security domain — Pitfall: email enumeration timing leak), refresh-token rotation block (source lines 236-240 — `session.token_jti = get_token_jti(new_refresh_token); session.refresh_token_hash = await hash_password_async(new_refresh_token); session.expires_at = now() + timedelta(days=settings.JWT_REFRESH_TOKEN_EXPIRE_DAYS); await db.commit()`), session creation (source lines 287-324), and all `await db.commit()` calls in their existing locations.

    6. **tests/test_email_send.py** — covers VALIDATION T-04-21, T-04-22 (USER-06):
       - `test_daily_limit`: mock `UserRepository.email_code_count_created_since` to return `settings.CODE_DAILY_SEND_LIMIT`; expect `send_verification_code` returns `(False, ...)` and `pool.enqueue_job` NOT called.
       - `test_enqueues_arq` (T-04-22 — D-02 behavior change): patch `api_service.services.email_service.get_arq_pool` to return an `AsyncMock`; call `await EmailService.send_verification_code(db, "user@example.com", "register")` with mocked repo returning 0 sends; assert `pool.enqueue_job.assert_called_once_with("send_verification_email", "user@example.com", ANY, "register")`. Per PATTERNS.md test pattern + RESEARCH § Code Examples.

    7. **tests/test_email_verify.py** — covers VALIDATION T-04-23 (USER-06 + D-11):
       - `test_error_count`: mock repo to return an `EmailVerificationCode` with `error_count=0`; call `get_valid_code_or_raise` with wrong code; expect `InvalidCodeException` AND `record.error_count` was incremented AND `db.commit` was awaited (D-11 inner commit verified).
       - `test_lockout_at_max_errors`: pre-set `error_count = settings.MAX_CODE_ERRORS - 1`; expect `record.locked_until` set and `db.commit` awaited then `InvalidCodeException` raised.

    Mocking style: follow `tests/test_auth_dependencies.py:80-91` — `@pytest.mark.asyncio` + `@patch("api_service.services.email_service.UserRepository")` + `AsyncMock` for repo methods.

    DO NOT add controllers/auth.py here — that is Task 3.
  </action>
  <verify>
    <automated>
      cd services/api-service &&
      python -c "from api_service.schemas.auth import RegisterRequest, LoginRequest, RegisterResponse, LoginResponse, RefreshTokenResponse, UserInfoResponseData, ChangePasswordRequest, ResetPasswordRequest, SendEmailCodeRequest, VerifyEmailRequest; print('schemas.auth OK')" &&
      python -c "from api_service.services.email_service import EmailService, _JOB_SEND_VERIFICATION_EMAIL; assert _JOB_SEND_VERIFICATION_EMAIL == 'send_verification_email'; assert not hasattr(EmailService, '__init__') or EmailService.__init__ is object.__init__; print('email_service OK')" &&
      python -c "import api_service.services.email_service as m; assert not hasattr(m, 'email_service'), 'module instance must be removed (Pitfall 4)'" &&
      python -c "from api_service.services.auth_service import AuthService; assert hasattr(AuthService, 'register') and hasattr(AuthService, 'login') and hasattr(AuthService, 'logout') and hasattr(AuthService, 'refresh') and hasattr(AuthService, 'change_password') and hasattr(AuthService, 'reset_password')" &&
      python -c "from api_service.core.jobs import send_verification_email, get_worker_settings_kwargs; kw = get_worker_settings_kwargs(); assert send_verification_email in kw['functions']; assert len(kw['functions']) == 5; assert send_verification_email.__name__ == 'send_verification_email'" &&
      grep -c "system_settings_gateway" services/api-service/api_service/services/auth_service.py | grep -q "^0$" &&
      grep -c "await db.commit()" services/api-service/api_service/services/email_service.py | { read n; [ "$n" -ge 2 ] ; } &&
      grep -E "D-11|preserve inner db.commit" services/api-service/api_service/services/email_service.py &&
      grep -q "settings.DEFAULT_USER_RPM" services/api-service/api_service/services/auth_service.py &&
      grep -q "EmailService\\." services/api-service/api_service/services/auth_service.py &&
      grep -c "email_service\\." services/api-service/api_service/services/auth_service.py | grep -q "^0$" &&
      grep -rEn "from (common|repositories|services|utils|core|schemas|gateways)\\." services/api-service/api_service/services/ services/api-service/api_service/schemas/auth.py | { ! grep -vE "api_service\\." ; } &&
      cd services/api-service && pytest tests/test_email_send.py tests/test_email_verify.py -x -q
    </automated>
  </verify>
  <acceptance_criteria>
    - `schemas/auth.py` defines all 16 request/response models from source. `from api_service.schemas.auth import RegisterRequest, LoginRequest, RegisterResponse, LoginResponse, RefreshTokenResponse, UserInfoResponseData, ChangePasswordRequest, ResetPasswordRequest, SendEmailCodeRequest, VerifyEmailRequest` succeeds.
    - `services/email_service.py` defines `class EmailService` with NO `__init__` (or only object.__init__), has staticmethod methods `generate_code`, `send_verification_code`, `get_valid_code_or_raise`, `mark_code_used`, and exports module constant `_JOB_SEND_VERIFICATION_EMAIL = "send_verification_email"`. Module does NOT export `email_service` symbol (Pitfall 4 — `grep -E "^email_service\\s*=" services/api-service/api_service/services/email_service.py` returns nothing).
    - `services/email_service.py` contains `await db.commit()` inside `get_valid_code_or_raise` with a `D-11` or `preserve inner db.commit` comment above it (D-11 enforcement — grep verifies).
    - `services/email_service.py` calls `pool.enqueue_job(_JOB_SEND_VERIFICATION_EMAIL, ...)` inside `send_verification_code`. `pool = get_arq_pool()` imported from `api_service.core.arq_pool`.
    - `services/email_service.py` uses `await UserRepository(db).email_code_*(...)` instead of `EmailCodeRepository(db).*` — `grep -c "EmailCodeRepository" services/api-service/api_service/services/email_service.py` returns 0.
    - `services/email_service.py` uses `await UserRepository(db).email_code_delete(...)` (now async — Pitfall A5).
    - `services/auth_service.py` does NOT reference `system_settings_gateway` (D-09 — `grep -c "system_settings_gateway"` returns 0).
    - `services/auth_service.py` uses `snapshot_rpm = settings.DEFAULT_USER_RPM` in the register flow.
    - `services/auth_service.py` calls `EmailService.X(...)` (capital E, class method), not `email_service.X(...)` (lowercase, module instance) — `grep -c "email_service\\." services/api-service/api_service/services/auth_service.py` returns 0.
    - `services/auth_service.py` uses `UserRepository(db).get_session_by_token_jti(...)`, `.list_active_sessions_for_user(...)`, `.revoke_session(...)`, `.add_session(...)` (RESEARCH § Repository Method Translation Table verified by grep).
    - `core/jobs.py` defines `send_verification_email(ctx, email, code, purpose)` whose `__name__` literally equals `"send_verification_email"` (Pitfall 9). `get_worker_settings_kwargs()["functions"]` has length 5 and contains the new function.
    - `core/jobs.py` calls `_send_smtp_sync` via `await asyncio.to_thread(...)` and uses `Retry(defer=job_try * 5)` for retries.
    - No `from common\\.…` or `from utils\\.…` or `from gateways\\.…` imports remain in any new file under `services/api-service/api_service/services/` or `schemas/auth.py` (Import Rewrite Map enforced).
    - VALIDATION slot T-04-21 (`test_daily_limit`), T-04-22 (`test_enqueues_arq`), T-04-23 (`test_error_count`) pass: `pytest tests/test_email_send.py tests/test_email_verify.py -x -q` exits 0.
  </acceptance_criteria>
  <done>
    schemas/auth.py + email_service.py (D-02 ARQ enqueue + D-11 inner commit) + auth_service.py (D-09 settings constant) + send_verification_email ARQ job all in place and importable. T-04-21, T-04-22, T-04-23 green. auth_service has no `system_settings_gateway`, no `email_service.` module-instance call. Task 3 can now mount the controller.
  </done>
</task>

<task type="auto">
  <name>Task 3: controllers/auth.py with cookie helpers + 10 endpoints + register/mount in core/router.py + 7 auth integration tests</name>
  <files>
    services/api-service/api_service/controllers/__init__.py,
    services/api-service/api_service/controllers/auth.py,
    services/api-service/api_service/core/router.py,
    services/api-service/tests/test_auth_register.py,
    services/api-service/tests/test_auth_login.py,
    services/api-service/tests/test_auth_logout.py,
    services/api-service/tests/test_auth_refresh.py,
    services/api-service/tests/test_auth_me.py,
    services/api-service/tests/test_auth_change.py,
    services/api-service/tests/test_auth_reset.py
  </files>
  <read_first>
    # Source analog (truth-of-source for endpoint signatures)
    services/user-service/src/controllers/auth.py,
    # Target wiring
    services/api-service/api_service/core/router.py,
    services/api-service/api_service/core/db.py,
    services/api-service/api_service/core/policies.py,
    services/api-service/api_service/core/dependencies/user.py,
    services/api-service/api_service/common/config.py,
    services/api-service/api_service/common/core/exceptions.py,
    services/api-service/api_service/repositories/billing_repository.py,
    # Just-created Task 2 outputs
    services/api-service/api_service/services/auth_service.py,
    services/api-service/api_service/services/email_service.py,
    services/api-service/api_service/schemas/auth.py,
    services/api-service/api_service/schemas/common.py,
    # Test mocking style
    services/api-service/tests/test_auth_dependencies.py,
    services/api-service/tests/conftest.py,
    # Playbook
    .planning/phases/04-user-domain-controllers/04-RESEARCH.md,
    .planning/phases/04-user-domain-controllers/04-PATTERNS.md,
    .planning/phases/04-user-domain-controllers/04-VALIDATION.md
  </read_first>
  <action>
    Port `services/user-service/src/controllers/auth.py` (390 lines) and wire it into the API router. Order:

    1. **controllers/__init__.py:** empty package marker.

    2. **controllers/auth.py:** Port verbatim from source with these rewrites (all sourced from RESEARCH § Import Translation Table + PATTERNS.md § controllers/auth.py):
       - **Imports rewrite (Pitfall 3 — `get_db_session` → `get_db`):**
         `from common.core.exceptions import ...` → `from api_service.common.core.exceptions import AuthenticationException, SessionNotFoundException, ServiceUnavailableException`
         `from core.config import settings` → `from api_service.core.config import settings`
         `from core.dependencies import get_db_session` → `from api_service.core.db import get_db`
         `from core.policies import require_active_user` → `from api_service.core.policies import require_active_user`
         `from models import User` → `from api_service.models import User`
         `from schemas import ...` → `from api_service.schemas.auth import (RegisterRequest, RegisterResponse, RegisterResponseData, LoginRequest, LoginResponse, LoginResponseData, ChangePasswordRequest, RefreshTokenResponse, RefreshTokenResponseData, UserInfoResponseData, ResetPasswordRequest, SendEmailCodeRequest, VerifyEmailRequest, ...)` plus `from api_service.schemas.common import ApiResponse, AuthBaseResponse, AuthErrorResponse`
         `from services.auth_service import AuthService` → `from api_service.services.auth_service import AuthService`
         `from services.email_service import email_service` → `from api_service.services.email_service import EmailService` (CLASS — not module instance)
         **D-04 (Phase 3) divergence:** `from repositories.usage_stat_repository import UsageStatRepository` (source) → `from api_service.repositories.billing_repository import BillingRepository` and replace `UsageStatRepository(db).get_user_tpm_last_minute(int(current_user.id))` with `BillingRepository(db).stat_get_user_tpm_last_minute(int(current_user.id))` at the `/auth/me` callsite.
         **DELETE entirely:** `from gateways.system_settings import system_settings_gateway` (D-09).

       - **Cookie helpers** (port verbatim from source lines 55-89 per PATTERNS.md § controllers/auth.py — Pitfall 6: do NOT change `USER_COOKIE_PATH = "/"`):
         Module-level constants: `USER_ACCESS_COOKIE = "user_access_token"`, `USER_REFRESH_COOKIE = "user_refresh_token"`, `USER_COOKIE_PATH = "/"`.
         Functions: `_set_auth_cookies(response, access_token, refresh_token)` and `_clear_auth_cookies(response)` using `response.set_cookie/delete_cookie` with `httponly=True`, `secure=settings.COOKIE_SECURE`, `samesite=settings.COOKIE_SAMESITE`, `max_age` from `JWT_ACCESS_TOKEN_EXPIRE_MINUTES * 60` / `JWT_REFRESH_TOKEN_EXPIRE_DAYS * 86400`, `path=USER_COOKIE_PATH`.

       - **Router declaration:** `router = APIRouter(tags=["认证"])` (per source). Per-endpoint paths include `/auth/*`. Claude's Discretion per CONTEXT.md permits prefix-style; **choose the path-on-decorator form to match source verbatim** (reduces port risk).

       - **10 endpoints — port verbatim with the import/method rewrites above:**
         1. `POST /auth/register` — calls `AuthService.register` + auto-login, sets cookies, returns `RegisterResponse(code=201, ...)`. Logs `"用户注册失败"` on exception.
         2. `POST /auth/login` — `AuthService.login(db, email, password, user_agent, ip_address)`, sets cookies, returns `LoginResponse`.
         3. `POST /auth/login-with-code` — `AuthService.login_with_code`.
         4. `POST /auth/logout` — calls `AuthService.logout`, then `_clear_auth_cookies(response)`. Reads `user_access_token` cookie via `Cookie(default=None, alias=USER_ACCESS_COOKIE)`.
         5. `POST /auth/refresh` — `AuthService.refresh(db, refresh_token)`, sets new cookies via `_set_auth_cookies`.
         6. `GET /auth/me` — depends on `require_active_user`, returns `UserInfoResponseData` with `current_tpm = await BillingRepository(db).stat_get_user_tpm_last_minute(int(current_user.id))` and `default_rpm = settings.DEFAULT_USER_RPM` (D-09). Returns `user_uid` only (NEVER `user_id` — root CLAUDE.md 用户标识规范).
         7. `POST /auth/change-password` — `AuthService.change_password`.
         8. `POST /auth/reset-password` — `AuthService.reset_password`, sets cookies after success.
         9. `POST /auth/send-email-code` — calls `await EmailService.send_verification_code(db, request.email, request.purpose)` (CLASS method, D-02 ARQ enqueue).
         10. `POST /auth/verify-email` — calls `EmailService.get_valid_code_or_raise` + business action.

         For each endpoint preserve the source `try/except Exception: logger.exception(...); raise` pattern (per PATTERNS.md § Error Handling) and the `response_model=ApiResponse[...]` / `status_code=...` decorators verbatim.

    3. **Router mount in `core/router.py`:** Add inside the existing `api_router` block, immediately after the `api_router = APIRouter(prefix="/api/v1")` line:
       `from api_service.controllers import auth`
       `api_router.include_router(auth.router)  # Phase 4: 10 /auth/* endpoints`
       Do NOT include `keys`, `billing`, `model_catalog` — those mount in their respective plans.

    4. **7 integration test files — covers VALIDATION T-04-02 through T-04-09 (USER-01):**

       Use `httpx.AsyncClient(app=app, base_url="http://test")` for integration calls; mock the service-layer methods via `@patch("api_service.controllers.auth.AuthService.<method>")` returning `AsyncMock` results. Pattern mirrors `tests/test_auth_dependencies.py`.

       - `test_auth_register.py::test_register_success` (T-04-02): mock `AuthService.register` + `.login` returning `(user, access, refresh)`; POST `/api/v1/auth/register`; assert status 201, response cookies include `user_access_token` (HttpOnly, SameSite from settings.COOKIE_SAMESITE), and response body has `data.uid` but NO `data.id` (用户标识规范 — root CLAUDE.md).

       - `test_auth_login.py::test_login_success` (T-04-03) + `test_login_lockout` (T-04-04): success path mocks `.login`, asserts cookies + 200. Lockout path mocks `.login` raising `InvalidCredentialsException` repeatedly — assert eventually `UserDisabledException` or 401 (verify against source behavior).

       - `test_auth_logout.py::test_logout_revokes_session` (T-04-05): POST `/api/v1/auth/logout` with cookie; mock `AuthService.logout` (assert called with extracted jti); assert response `Set-Cookie` headers include `user_access_token=; Max-Age=0` (cookie-clear) for both cookies.

       - `test_auth_refresh.py::test_refresh_rotates` (T-04-06): POST `/api/v1/auth/refresh`; mock `AuthService.refresh` returning new tokens; assert response sets new cookies AND the response body returns new access token.

       - `test_auth_me.py::test_me_excludes_id` (T-04-07): GET `/api/v1/auth/me`; mock `require_active_user` returning a user fixture; mock `BillingRepository.stat_get_user_tpm_last_minute` returning 42; assert response body has `data.uid`, `data.default_rpm == settings.DEFAULT_USER_RPM`, `data.current_tpm == 42`, and `"user_id"` field is ABSENT from `data` (assert `"user_id" not in resp.json()["data"]`).

       - `test_auth_change.py::test_change_revokes_sessions` (T-04-08): POST `/api/v1/auth/change-password`; mock `AuthService.change_password` — assert it is awaited with the user and old/new password; on success assert no cookies cleared (source behavior — change-password keeps user logged in via new session).

       - `test_auth_reset.py::test_reset_with_code` (T-04-09): POST `/api/v1/auth/reset-password`; mock `AuthService.reset_password` returning `(user, access, refresh)`; assert cookies set on response.

    Use the `arq_pool_mock` and `mock_db` fixtures from Wave 0 conftest where applicable. Where a test needs a real FastAPI app instance with mocked services, override `app.dependency_overrides[get_db] = lambda: mock_db` and `app.dependency_overrides[require_active_user] = lambda: mock_user`.
  </action>
  <verify>
    <automated>
      cd services/api-service &&
      python -c "from api_service.controllers.auth import router, _set_auth_cookies, _clear_auth_cookies, USER_ACCESS_COOKIE, USER_REFRESH_COOKIE, USER_COOKIE_PATH; assert USER_ACCESS_COOKIE == 'user_access_token'; assert USER_REFRESH_COOKIE == 'user_refresh_token'; assert USER_COOKIE_PATH == '/'; routes = [r.path for r in router.routes]; expected = ['/auth/register','/auth/login','/auth/login-with-code','/auth/logout','/auth/refresh','/auth/me','/auth/change-password','/auth/reset-password','/auth/send-email-code','/auth/verify-email']; missing = [p for p in expected if p not in routes]; assert not missing, f'missing routes: {missing}'; print('OK')" &&
      python -c "from api_service.core.router import api_router; routes = [r.path for r in api_router.routes]; assert any(p.endswith('/auth/register') for p in routes), routes" &&
      grep -c "system_settings_gateway" services/api-service/api_service/controllers/auth.py | grep -q "^0$" &&
      grep -c "get_db_session" services/api-service/api_service/controllers/auth.py | grep -q "^0$" &&
      grep -q "BillingRepository" services/api-service/api_service/controllers/auth.py &&
      grep -q "stat_get_user_tpm_last_minute" services/api-service/api_service/controllers/auth.py &&
      grep -q "EmailService\\." services/api-service/api_service/controllers/auth.py &&
      grep -c "email_service\\." services/api-service/api_service/controllers/auth.py | grep -q "^0$" &&
      grep -q "USER_COOKIE_PATH = \"/\"" services/api-service/api_service/controllers/auth.py &&
      cd services/api-service && pytest tests/test_auth_register.py tests/test_auth_login.py tests/test_auth_logout.py tests/test_auth_refresh.py tests/test_auth_me.py tests/test_auth_change.py tests/test_auth_reset.py -x -q &&
      cd services/api-service && pytest tests/ -x -q
    </automated>
  </verify>
  <acceptance_criteria>
    - `controllers/auth.py` defines a `router: APIRouter` whose routes contain exactly these paths: `/auth/register`, `/auth/login`, `/auth/login-with-code`, `/auth/logout`, `/auth/refresh`, `/auth/me`, `/auth/change-password`, `/auth/reset-password`, `/auth/send-email-code`, `/auth/verify-email` (10 endpoints — `grep -cE '@router\\.(get|post)' services/api-service/api_service/controllers/auth.py` returns at least 10).
    - Cookie constants `USER_ACCESS_COOKIE = "user_access_token"`, `USER_REFRESH_COOKIE = "user_refresh_token"`, `USER_COOKIE_PATH = "/"` are defined verbatim (Pitfall 6 enforced — `grep -q 'USER_COOKIE_PATH = "/"' services/api-service/api_service/controllers/auth.py`).
    - `_set_auth_cookies` and `_clear_auth_cookies` helpers exist. Both reference `settings.COOKIE_SECURE` and `settings.COOKIE_SAMESITE`.
    - No `system_settings_gateway` import or reference (D-09 — grep returns 0).
    - No `get_db_session` reference (Pitfall 3 — grep returns 0).
    - `/auth/me` endpoint uses `BillingRepository(db).stat_get_user_tpm_last_minute(...)` instead of `UsageStatRepository(...).get_user_tpm_last_minute(...)` (Phase 3 D-04 — grep verifies).
    - `/auth/me` endpoint uses `settings.DEFAULT_USER_RPM` for `default_rpm` field (D-09 — grep verifies).
    - All `email_service.X(...)` callsites replaced with `EmailService.X(...)` (CLASS, not module instance — `grep -c "email_service\\." services/api-service/api_service/controllers/auth.py` returns 0).
    - `core/router.py` includes `from api_service.controllers import auth` and `api_router.include_router(auth.router)`.
    - VALIDATION slots T-04-02 through T-04-09 pass: `pytest tests/test_auth_register.py tests/test_auth_login.py tests/test_auth_logout.py tests/test_auth_refresh.py tests/test_auth_me.py tests/test_auth_change.py tests/test_auth_reset.py -x -q` exits 0.
    - `test_auth_me.py::test_me_excludes_id` asserts `"user_id" not in resp.json()["data"]` (用户标识规范 enforced).
    - Full test suite stays green: `pytest tests/ -x -q` exits 0 (Wave 0 tests + email tests from Task 2 + 7 new auth tests + 5 existing Phase 3 tests).
  </acceptance_criteria>
  <done>
    All 10 `/auth/*` endpoints mounted at `/api/v1/auth/*`. Cookie helpers preserve source semantics. D-09 (settings constant), D-04 (BillingRepository), Pitfall 3 (get_db), Pitfall 4 (EmailService class), Pitfall 6 (cookie path) all enforced. T-04-02 through T-04-09 + T-04-21..23 from Task 2 green. Test suite fully green. Plan 04-02 (keys/billing) is now unblocked.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → FastAPI | Cookie-based JWT (`user_access_token`, `user_refresh_token`) and request bodies cross here. All inputs validated by Pydantic v2 schemas. |
| FastAPI → Service | `int(current_user.id)` passed from controller; service layer trusts caller scoping. |
| Service → DB | All wallet/session/code mutations use explicit `await db.commit()` and (for wallet) `SELECT ... FOR UPDATE`. |
| api-service ↔ Redis (ARQ db/1) | Verification code plaintext travels in the ARQ job payload — Redis must be on the trusted internal network. |
| Worker process → SMTP | Plaintext code sent to user email; SMTP credentials in env vars. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-S1 | Spoofing | `/auth/login` cookie issuance | mitigate | HttpOnly + Secure + SameSite (settings.COOKIE_SAMESITE=strict default — verified `common/config.py:57-58`); refresh-token rotation on every `/auth/refresh` (source lines 236-240 preserved per PATTERNS § auth_service); bcrypt hash via passlib<4 (already wired). |
| T-04-S2 | Spoofing | Email enumeration via `/auth/login` timing | mitigate | Port `_get_dummy_hash()` constant-time equalizer verbatim from source auth_service.py:46-53 (preserved per PATTERNS § auth_service.py). |
| T-04-T1 | Tampering | Code brute-force on `/auth/verify-email` | mitigate | `error_count` + `locked_until` via D-11 inner `db.commit()` in `EmailService.get_valid_code_or_raise`; `CODE_DAILY_SEND_LIMIT=3` per (email, purpose) per UTC day; `MAX_CODE_ERRORS=5` then `CODE_ERROR_LOCK_HOURS=24` lock. All Wave 0 settings keys. |
| T-04-T2 | Tampering | Session hijacking via refresh-token replay | mitigate | Refresh-token hash stored in `user_sessions.refresh_token_hash` (bcrypt); jti rotation; revocation row-level on logout (`UserRepository.revoke_session`). |
| T-04-R1 | Repudiation | Login/logout audit trail | mitigate | `log_event(logger, level, 'userLoginAttempt'/'userLoginLocked'/'userLogout', uid=...)` — observability layer auto-masks PII per RESEARCH § Security Domain. |
| T-04-I1 | Information disclosure | Returning `user_id: int` in any response | mitigate | Test `test_auth_me.py::test_me_excludes_id` asserts `"user_id" not in resp.json()["data"]` (用户标识规范 — root CLAUDE.md). Source schemas already use `uid`. |
| T-04-I2 | Information disclosure | Plaintext verification code logged | mitigate | `_send_smtp_sync` debug log uses `email=%s purpose=%s` only — code never logged. ARQ worker logs `emailSendFailedPermanently` without code. |
| T-04-I3 | Information disclosure | Email enumeration on `/auth/register` | accept | Source returns generic `EmailAlreadyExistsException` (zh message). Constant-time alternative considered but out of scope per RESEARCH § Security Domain (preserves source behavior). |
| T-04-D1 | Denial of service | `/auth/send-email-code` flood | mitigate | `CODE_DAILY_SEND_LIMIT=3` per (email, purpose); ARQ retry capped at 3 tries with linear backoff (RESEARCH § Code Examples + Pitfall 9). |
| T-04-D2 | Denial of service | ARQ Redis queue exhaustion | accept | Low traffic + bounded retries (max_tries=3) cap unbounded growth; cron job `cleanup_expired_verification_codes` already in place. |
| T-04-E1 | Elevation of privilege | CSRF on `/auth/logout` or `/auth/refresh` | mitigate | SameSite=strict default (settings.COOKIE_SAMESITE); refresh cookie path "/" with HttpOnly (Pitfall 6 — preserved). Source already handles this. |
</threat_model>

<verification>
**Per-task verification** runs inside each `<verify><automated>` block.

**Plan-level gate** before merging to 04-02:
- `cd services/api-service && pytest tests/ -x -q` exits 0 (all Wave 0 + Phase 3 regression + auth + email tests green).
- `cd services/api-service && uvicorn api_service.main:app --reload --port 9999` starts without ImportError (smoke test optional but recommended).
- `arq api_service.core.worker.WorkerSettings --check` exits 0 (worker boots in isolation per RESEARCH § Pitfall 5 — manual run; not gated in CI).
- No `from common\\.…` / `from utils\\.…` / `from gateways\\.…` / `from repositories import …` imports in any new file (Import Rewrite Map enforced).
- Tests in VALIDATION T-04-01 through T-04-09 + T-04-21..T-04-23 are all `✅` (10 of 23 slots in this plan).
</verification>

<success_criteria>
- USER-01 fully covered: register, login, login-with-code, logout, refresh, /auth/me, change-password, reset-password endpoints function with cookie-based JWT, with the matching tests green.
- USER-06 partially covered (send-email-code + verify-email endpoints + ARQ enqueue verified). The full USER-06 — model catalog email flows — completes in 04-03.
- D-02 verified: `pool.enqueue_job("send_verification_email", ...)` called in `EmailService.send_verification_code` (no synchronous SMTP send in the request path).
- D-09 verified: `settings.DEFAULT_USER_RPM` used as the only source of default RPM; `system_settings_gateway` never imported.
- D-11 verified: inner `await db.commit()` preserved in `EmailService.get_valid_code_or_raise` with the marker comment present.
- D-12 verified: Wave 0 foundations (settings, utils, schemas/common.py, ARQ pool, worker, jobs, policies, conftest) all in place before any controller/service code was written.
- 用户标识规范 enforced: `/auth/me` returns `uid`, never `user_id` (test verified).
- ARQ pool registered in lifespan (priority=40); ARQ worker module runnable in isolation.
- 10 `/auth/*` endpoints mounted at `/api/v1/auth/*`; full test suite green; no Phase 3 regression.
</success_criteria>

<output>
Create `.planning/phases/04-user-domain-controllers/04-01-SUMMARY.md` when done, summarizing:
- Files created/modified (use the `files_modified` list from frontmatter).
- VALIDATION slots covered: T-04-01..09, T-04-21..23 (10 slots in this plan).
- Requirements addressed: USER-01 (full), USER-06 (partial — completes in 04-03).
- Decisions enacted: D-01 (no internal_* migrated), D-02 (ARQ async email), D-09 (settings.DEFAULT_USER_RPM), D-11 (inner db.commit preserved), D-12 (Wave 0 foundations landed).
- Pitfalls addressed: P3 (`get_db_session` → `get_db`), P4 (EmailService class — no module instance), P6 (cookie path "/"), P7 (DateTimeModel list() copy), P9 (job name `__name__` matches enqueue arg).
- Unblocked: 04-02 (keys/billing) and 04-03 (model catalog/email) can now run.
</output>
</content>
</invoke>
---
phase: 04-user-domain-controllers
plan: 04-02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - services/api-service/api_service/schemas/keys.py
  - services/api-service/api_service/schemas/billing.py
  - services/api-service/api_service/schemas/__init__.py
  - services/api-service/api_service/services/api_key_service.py
  - services/api-service/api_service/services/balance_service.py
  - services/api-service/api_service/services/topup_order_service.py
  - services/api-service/api_service/services/voucher_service.py
  - services/api-service/api_service/services/usage_stat_service.py
  - services/api-service/api_service/controllers/keys.py
  - services/api-service/api_service/controllers/billing.py
  - services/api-service/api_service/core/router.py
  - services/api-service/tests/test_keys.py
  - services/api-service/tests/test_api_key_service.py
  - services/api-service/tests/test_billing_balance.py
  - services/api-service/tests/test_billing_tx.py
  - services/api-service/tests/test_voucher.py
  - services/api-service/tests/test_topup.py
  - services/api-service/tests/test_usage.py
  - services/api-service/tests/test_usage_stat_service.py
autonomous: true
requirements: [USER-04]
requirements_addressed: [USER-04]
tags: [api-keys, billing, balance, usage, voucher, topup]

must_haves:
  truths:
    - "D-01: no `internal_*.py` files are in scope — this plan only ports user-facing /keys + /billing controllers (internal admin-proxy endpoints are explicitly excluded by D-01 and will not be migrated)"
    - "D-03: this plan ports `services/user-service/src/schemas/keys.py` (83 lines) and `schemas/billing.py` (190 lines) 1:1 — request/response field-for-field, no consolidation, no admin schemas (deferred to Phase 5 per D-10)"
    - "D-08: this is plan 2 of exactly 3 plans for Phase 4 (api keys + billing wave; depends on Wave 1 = 04-01 auth foundations)"
    - "GET /api/v1/keys returns the authenticated user's API keys with `key_prefix` only (NEVER raw `key`, NEVER `key_hash`)"
    - "POST /api/v1/keys returns the plaintext `sk-…` key EXACTLY ONCE in `data.key`; subsequent reads via GET never expose plaintext"
    - "DELETE /api/v1/keys/{id} is a soft delete (sets `deleted_at`, no row removal)"
    - "GET /api/v1/billing/balance returns int fields `balance`, `frozen_amount`, `used_amount`, `total_requests`, `total_tokens`, and computed `available_balance = balance - frozen_amount`"
    - "GET /api/v1/billing/transactions paginates and filters by type via ListParams (time range capped at 90 days)"
    - "POST /api/v1/billing/vouchers/redeem is idempotent on duplicate code: a second call with the same code does NOT double-credit (enforced via `BillingRepository.exists_by_ref(tx_type=VOUCHER_REDEEM, ref_type='voucher_code', ref_id=str(code.id))`)"
    - "Every wallet mutation method (`consume_for_call_log`, `freeze`, `settle`, `refund`, `topup`, `redeem_code`, `admin_adjust`) acquires the user row via `UserRepository.get_by_id(user_id, for_update=True)` BEFORE mutating `user.balance` or `user.frozen_amount`"
    - "GET /api/v1/billing/usage with `start`/`end` enforces max 90-day window via `ListParams.validate_time_range`"
    - "GET /api/v1/billing/usage/analytics with `range=24h` uses hourly granularity; explicit start/end >48h uses daily granularity"
    - "All services use merged `BillingRepository` methods (`add_tx`, `exists_by_ref`, `list_tx_for_user`, `topup_*`, `stat_*`) — NO references to deleted `BalanceTxRepository`, `TopupOrderRepository`, `UsageStatRepository` classes"
  artifacts:
    - path: "services/api-service/api_service/schemas/keys.py"
      provides: "ApiKeyItem (key_prefix only), ApiKeyCreateRequest, ApiKeyCreateData (plaintext key only on create), ApiKeyUpdateRequest, ApiKeyListResponse, ApiKeyCreateResponse"
      contains: "key_prefix"
    - path: "services/api-service/api_service/schemas/billing.py"
      provides: "BalanceResponseData with computed available_balance, BalanceTransactionItem, TopupOrderItem, VoucherRedeemRequest/Response, UsageStatItem, UsageAnalyticsData, UsageAnalyticsRange Literal, ApiCallLogItem with from_orm_instance"
      contains: "available_balance"
    - path: "services/api-service/api_service/services/api_key_service.py"
      provides: "list, create (returns (key_obj, raw_plaintext)), update, disable, delete (soft), verify_key_ownership, validate_by_hash, _refresh_status"
      contains: "class ApiKeyService"
    - path: "services/api-service/api_service/services/balance_service.py"
      provides: "consume_for_call_log, freeze, settle, refund, topup, redeem_code, admin_adjust, get_balance, list_transactions — all SELECT FOR UPDATE + ref_id idempotency"
      contains: "class BalanceService"
    - path: "services/api-service/api_service/services/topup_order_service.py"
      provides: "create_order, list_orders, _generate_order_no"
      contains: "TP"
    - path: "services/api-service/api_service/services/voucher_service.py"
      provides: "normalize_code, hash_code, redeem (SELECT FOR UPDATE + ref_id), list_for_user, list_for_admin"
      contains: "class VoucherService"
    - path: "services/api-service/api_service/services/usage_stat_service.py"
      provides: "get_user_stats, list_usage_logs, get_usage_analytics, list_analytics_logs, _build_usage_analytics_window (8h/24h/7d/30d), granularity switch at 48h"
      contains: "class UsageStatService"
    - path: "services/api-service/api_service/controllers/keys.py"
      provides: "5 endpoints under /keys (GET list, POST create, PATCH update, POST disable, DELETE soft-delete)"
      contains: "APIRouter(prefix=\"/keys\""
    - path: "services/api-service/api_service/controllers/billing.py"
      provides: "8 /billing endpoints: balance, transactions, topup-orders, topup-orders create, vouchers redeem, vouchers list-redemptions, usage, usage/analytics, usage/logs"
      contains: "APIRouter(prefix=\"/billing\""
  key_links:
    - from: "services/api-service/api_service/services/balance_service.py"
      to: "services/api-service/api_service/repositories/billing_repository.py"
      via: "BillingRepository(db).add_tx(...) + .exists_by_ref(...) + .list_tx_for_user(...)"
      pattern: "BillingRepository\\(db\\)\\.(add_tx|exists_by_ref|list_tx)"
    - from: "services/api-service/api_service/services/balance_service.py"
      to: "services/api-service/api_service/repositories/user_repository.py"
      via: "UserRepository(db).get_by_id(user_id, for_update=True)"
      pattern: "get_by_id\\([^)]*for_update=True"
    - from: "services/api-service/api_service/services/voucher_service.py"
      to: "services/api-service/api_service/repositories/voucher_repository.py"
      via: "VoucherRepository (renamed from VoucherRedemptionCodeRepository in Phase 3)"
      pattern: "from api_service.repositories.voucher_repository import VoucherRepository"
    - from: "services/api-service/api_service/controllers/keys.py"
      to: "services/api-service/api_service/services/api_key_service.py"
      via: "ApiKeyService.list/create/update/disable/delete"
      pattern: "ApiKeyService\\.(list|create|update|disable|delete)"
    - from: "services/api-service/api_service/controllers/billing.py"
      to: "services/api-service/api_service/services/balance_service.py"
      via: "BalanceService.get_balance / list_transactions / redeem_code etc"
      pattern: "(BalanceService|VoucherService|UsageStatService|TopupOrderService)\\."
    - from: "services/api-service/api_service/core/router.py"
      to: "services/api-service/api_service/controllers/keys.py"
      via: "api_router.include_router(keys.router)"
      pattern: "include_router\\(keys\\.router\\)"
---

<objective>
Port the 5 `/keys` endpoints and 8 `/billing` endpoints plus their 5 supporting services from user-service into api-service. All wallet mutations preserve `SELECT ... FOR UPDATE` row locking and `ref_id` idempotency, but call against the Phase 3 merged `BillingRepository` (which absorbed `BalanceTxRepository`, `TopupOrderRepository`, `UsageStatRepository`).

Purpose: Deliver USER-04 (balance/transactions/voucher/usage) and the API-key CRUD half of USER-03 functionality at the controller surface, while preserving every security-critical invariant: plaintext API key returned only on create, soft-delete only, row-locked wallet operations, idempotent voucher redemption.

Output: 13 endpoints mounted at `/api/v1/keys/*` (5) and `/api/v1/billing/*` (8), 5 services with full repo-method translations applied, 7 test files covering VALIDATION slots T-04-10 through T-04-17.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-user-domain-controllers/04-CONTEXT.md
@.planning/phases/04-user-domain-controllers/04-RESEARCH.md
@.planning/phases/04-user-domain-controllers/04-PATTERNS.md
@.planning/phases/04-user-domain-controllers/04-VALIDATION.md
@.planning/phases/04-user-domain-controllers/04-01-PLAN.md

# Project conventions
@CLAUDE.md
@services/user-service/CLAUDE.md

# 04-01 outputs we build on (must exist before this plan starts)
@services/api-service/api_service/core/config.py
@services/api-service/api_service/core/policies.py
@services/api-service/api_service/core/db.py
@services/api-service/api_service/core/router.py
@services/api-service/api_service/schemas/common.py
@services/api-service/api_service/schemas/__init__.py
@services/api-service/api_service/common/utils/api_key_policy.py
@services/api-service/api_service/controllers/auth.py
@services/api-service/api_service/services/auth_service.py
@services/api-service/api_service/services/email_service.py
@services/api-service/tests/conftest.py

# Repositories (Phase 3 merged classes — call signatures the executor needs)
@services/api-service/api_service/repositories/api_key_repository.py
@services/api-service/api_service/repositories/billing_repository.py
@services/api-service/api_service/repositories/user_repository.py
@services/api-service/api_service/repositories/voucher_repository.py

# Common utilities
@services/api-service/api_service/common/utils/timezone.py
@services/api-service/api_service/common/utils/snowflake.py
@services/api-service/api_service/common/infra/db/query.py
@services/api-service/api_service/common/api/pagination.py
@services/api-service/api_service/common/core/exceptions.py
@services/api-service/api_service/common/observability.py
@services/api-service/api_service/models/__init__.py

# Migration sources (truth-of-source per CONTEXT canonical_refs)
@services/user-service/src/controllers/keys.py
@services/user-service/src/controllers/billing.py
@services/user-service/src/services/api_key_service.py
@services/user-service/src/services/balance_service.py
@services/user-service/src/services/topup_order_service.py
@services/user-service/src/services/voucher_service.py
@services/user-service/src/services/usage_stat_service.py
@services/user-service/src/schemas/keys.py
@services/user-service/src/schemas/billing.py

<interfaces>
<!-- Phase 3 merged repositories (BillingRepository absorbed 3 source repos) — exact method names the executor needs. -->

From api_service/repositories/billing_repository.py (verified):
- add_tx(tx: BalanceTransaction) -> None  # session.add wrapper
- exists_by_ref(*, tx_type: int, ref_type: str, ref_id: str) -> bool
- list_tx_for_user(user_id: int, *, params: ListParams, type_filter: int | None = None) -> PaginatedResult[BalanceTransaction]
- list_tx_all(*, params: ListParams, ...) -> PaginatedResult[BalanceTransaction]
- topup_add(order: TopupOrder) -> None
- topup_get_for_user_by_order_no(user_id: int, order_no: str) -> TopupOrder | None
- topup_list_for_user(user_id: int, *, params: ListParams) -> PaginatedResult[TopupOrder]
- topup_list_all(*, params: ListParams) -> PaginatedResult[TopupOrder]
- stat_get_user_tpm_last_minute(user_id: int) -> int
- stat_get_user_stats(user_id: int, *, params: ListParams) -> PaginatedResult[UsageStat]
- stat_get_all_stats(*, params: ListParams) -> PaginatedResult[UsageStat]
- stat_list_usage_logs(*, params: ListParams, user_id: int, ...) -> PaginatedResult[ApiCallLog]
- stat_list_analytics_logs(*, user_id: int, start, end, ...) -> list  # _exclude_invalid_model already embedded
- stat_list_logs_for_hour(*, user_id: int, hour_start) -> list  # _exclude_invalid_model already embedded
- stat_get_bucket(user_id: int, bucket_start: datetime) -> UsageStat | None
- session (attribute) -> AsyncSession  # for per-row add fallback in usage_stat_service (RESEARCH § translation note)

From api_service/repositories/user_repository.py:
- get_by_id(user_id: int, *, for_update: bool = False) -> User | None  # SELECT ... FOR UPDATE when for_update=True

From api_service/repositories/api_key_repository.py (unchanged from Phase 3):
- list_for_user(user_id: int) -> list[UserApiKey]
- count_for_user(user_id: int) -> int
- get_owned_key(key_id: int, user_id: int) -> UserApiKey | None
- get_by_hash(key_hash: str) -> UserApiKey | None
- disable_all_for_user(user_id: int) -> None
- add(api_key: UserApiKey) -> None

From api_service/repositories/voucher_repository.py (renamed class — methods same as source):
- get_by_id(code_id: int, *, for_update: bool = False) -> VoucherRedemptionCode | None
- get_by_hash(code_hash: str) -> VoucherRedemptionCode | None
- list_for_admin(*, params: ListParams) -> PaginatedResult[VoucherRedemptionCode]
- list_for_user_redemptions(user_id: int, *, params: ListParams) -> PaginatedResult[VoucherRedemptionCode]

From api_service/common/infra/db/query.py:
- class ListParams(page, page_size, order_by, time_field, start, end, max_span_days)
- ListParams.validate_time_range(default_end: datetime, default_days: int) -> None

From api_service/common/api/pagination.py:
- class PaginatedResponse[T] with from_result(...) helper

From api_service/common/utils/api_key_policy.py (Wave 0 / 04-01):
- normalize_allowed_models(value: str | None) -> str | None
- normalize_allow_ips(value: str | None) -> str | None
- is_model_allowed(model: str, allowed_models: str | None) -> bool
- is_ip_allowed(ip: str, allow_ips: str | None) -> bool

Module-level constants used by services:
- BalanceTransaction.TYPE_CONSUME, TYPE_FREEZE, TYPE_SETTLE, TYPE_REFUND, TYPE_TOPUP, TYPE_VOUCHER_REDEEM, TYPE_ADMIN_ADJUST (defined on the model — verify via models/__init__.py)
- UserApiKey.STATUS_ACTIVE / STATUS_DISABLED / STATUS_EXPIRED / STATUS_EXHAUSTED
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: schemas/keys.py + schemas/billing.py + ApiKeyService (no repo renames — preserves source) + TopupOrderService + VoucherService + UsageStatService (with billing_repo method renames)</name>
  <files>
    services/api-service/api_service/schemas/keys.py,
    services/api-service/api_service/schemas/billing.py,
    services/api-service/api_service/schemas/__init__.py,
    services/api-service/api_service/services/api_key_service.py,
    services/api-service/api_service/services/topup_order_service.py,
    services/api-service/api_service/services/voucher_service.py,
    services/api-service/api_service/services/usage_stat_service.py
  </files>
  <read_first>
    services/user-service/src/schemas/keys.py,
    services/user-service/src/schemas/billing.py,
    services/user-service/src/schemas/__init__.py,
    services/user-service/src/services/api_key_service.py,
    services/user-service/src/services/topup_order_service.py,
    services/user-service/src/services/voucher_service.py,
    services/user-service/src/services/usage_stat_service.py,
    services/api-service/api_service/repositories/api_key_repository.py,
    services/api-service/api_service/repositories/billing_repository.py,
    services/api-service/api_service/repositories/voucher_repository.py,
    services/api-service/api_service/repositories/user_repository.py,
    services/api-service/api_service/common/utils/api_key_policy.py,
    services/api-service/api_service/common/utils/timezone.py,
    services/api-service/api_service/common/infra/db/query.py,
    services/api-service/api_service/common/api/pagination.py,
    services/api-service/api_service/common/core/exceptions.py,
    services/api-service/api_service/models/__init__.py,
    services/api-service/api_service/schemas/common.py,
    services/api-service/api_service/schemas/__init__.py,
    .planning/phases/04-user-domain-controllers/04-RESEARCH.md,
    .planning/phases/04-user-domain-controllers/04-PATTERNS.md
  </read_first>
  <action>
    Land the 2 schemas + 4 of 5 services (BalanceService is large enough to split into Task 2). Order:

    1. **schemas/keys.py:** Port verbatim from `services/user-service/src/schemas/keys.py`. Rewrite ONLY these imports:
       `from common.utils.timezone import to_shanghai_naive` → `from api_service.common.utils.timezone import to_shanghai_naive`
       `from schemas.common import DateTimeModel` → `from api_service.schemas.common import DateTimeModel`
       `from utils.api_key_policy import normalize_allow_ips, normalize_allowed_models` → `from api_service.common.utils.api_key_policy import normalize_allow_ips, normalize_allowed_models`
       **CRITICAL — security invariant:** `ApiKeyItem` exposes ONLY `key_prefix`, NEVER `key` or `key_hash`. `ApiKeyCreateData` is the ONLY shape that includes `key: str` (plaintext, returned once on create). Verify the source class definitions, port verbatim.

    2. **schemas/billing.py:** Port verbatim from `services/user-service/src/schemas/billing.py` (~190 lines). Rewrite imports:
       `from schemas.common import DateTimeModel` → `from api_service.schemas.common import DateTimeModel`
       Any `from common.utils.timezone` → `from api_service.common.utils.timezone`.
       Preserve verbatim:
       - `BalanceResponseData.available_balance` `@computed_field` (`balance - frozen_amount`)
       - `UsageAnalyticsRange = Literal["8h", "24h", "7d", "30d"]`
       - `ApiCallLogItem.from_orm_instance(cls, obj)` classmethod (loads `api_key.name` from the eager-loaded relationship — used by `/billing/usage/logs` endpoint).

    3. **schemas/__init__.py:** Extend the existing re-exports added in 04-01 by appending `from api_service.schemas.keys import ApiKeyCreateData, ApiKeyCreateRequest, ApiKeyItem, ApiKeyUpdateRequest, ApiKeyListResponse, ApiKeyCreateResponse, ApiKeyUpdateResponse, ApiKeyDeleteResponse, ApiKeyDisableResponse` and `from api_service.schemas.billing import ApiCallLogItem, BalanceResponseData, BalanceTransactionItem, BalanceTransactionListResponse, BalanceResponse, TopupOrderItem, TopupOrderListResponse, TopupOrderResponse, UsageAnalyticsBucket, UsageAnalyticsBucketCost, UsageAnalyticsData, UsageAnalyticsRange, UsageAnalyticsResponse, UsageStatItem, UsageStatListResponse, UsageLogsResponse, VoucherRedeemRequest, VoucherRedeemResponse, VoucherRedeemResponseData, VoucherRedemptionItem`. Use the source `services/user-service/src/schemas/__init__.py` as the canonical `__all__` shape; omit anything tied to `internal_*` modules (per D-01).

    4. **services/api_key_service.py:** Port from `services/user-service/src/services/api_key_service.py` (~196 lines). Per PATTERNS.md § api_key_service.py: NO repository method renames needed — `ApiKeyRepository` is preserved as-is in Phase 3. Only import paths change:
       `from common.utils.timezone` → `from api_service.common.utils.timezone`
       `from utils.api_key_policy` → `from api_service.common.utils.api_key_policy`
       `from common.observability import log_event` → `from api_service.common.observability import log_event`
       `from repositories.api_key_repository import ApiKeyRepository` → `from api_service.repositories.api_key_repository import ApiKeyRepository`
       `from repositories.user_repository import UserRepository` → `from api_service.repositories.user_repository import UserRepository`
       `from models import UserApiKey` → `from api_service.models import UserApiKey`
       Preserve verbatim:
       - `create(db, user_id, name, ...)` returning tuple `(api_key_obj, raw_plaintext_key)` where `raw_key = "sk-" + 46 chars from secrets.choice(_KEY_ALPHABET)` and `key_hash = hashlib.sha256(raw_key.encode("utf-8")).hexdigest()`. Cap by `settings.MAX_API_KEYS_PER_USER` before insert.
       - `_refresh_status(api_key)` (DISABLED early-return, EXPIRED if `expires_at <= now()`, EXHAUSTED if `is_exhausted`, else ACTIVE).
       - `delete(db, key_id, user_id)`: soft delete — set `api_key.deleted_at = now()` and `await db.commit()`. DO NOT issue a SQL DELETE.
       - `verify_key_ownership` and `validate_by_hash` (used by Phase 6 relay — must compile but no callers in this plan).

    5. **services/topup_order_service.py:** Port from `services/user-service/src/services/topup_order_service.py` (82 lines). Imports per RESEARCH § Import Translation Table:
       `from common.db import ListParams, PaginatedResult` → `from api_service.common.infra.db.query import ListParams, PaginatedResult`
       `from repositories import TopupOrderRepository` → `from api_service.repositories.billing_repository import BillingRepository`
       `from models import TopupOrder` → `from api_service.models import TopupOrder`
       Repo method renames per RESEARCH § Repository Method Translation Table:
       `TopupOrderRepository(db).add(order)` → `BillingRepository(db).topup_add(order)`
       `TopupOrderRepository(db).list_for_user(...)` → `BillingRepository(db).topup_list_for_user(...)`
       `TopupOrderRepository(db).list_all(...)` → `BillingRepository(db).topup_list_all(...)`
       Preserve verbatim:
       - `_generate_order_no()` returning `"TP" + now().strftime("%Y%m%d") + 8 random chars from _ORDER_ALPHABET`.
       - `MIN_TOPUP_AMOUNT` / `MAX_TOPUP_AMOUNT` range validation via `settings.MIN_TOPUP_AMOUNT` / `settings.MAX_TOPUP_AMOUNT` (Wave 0 settings keys verified present).

    6. **services/voucher_service.py:** Port from `services/user-service/src/services/voucher_service.py` (185 lines). Imports:
       `from common.db import ListParams, PaginatedResult` → `from api_service.common.infra.db.query import ListParams, PaginatedResult`
       `from repositories import BalanceTxRepository, UserRepository, VoucherRedemptionCodeRepository` → split into:
         `from api_service.repositories.billing_repository import BillingRepository`
         `from api_service.repositories.user_repository import UserRepository`
         `from api_service.repositories.voucher_repository import VoucherRepository`  # class renamed in Phase 3 (PATTERNS.md verified)
       Class rename in code: every `VoucherRedemptionCodeRepository(db)` → `VoucherRepository(db)`. Method names unchanged (`get_by_id`, `get_by_hash`, `list_for_admin`, `list_for_user_redemptions`).
       Repo method renames per RESEARCH § Repository Method Translation Table:
       `BalanceTxRepository(db).add(BalanceTransaction(...))` → `BillingRepository(db).add_tx(BalanceTransaction(...))`
       `BalanceTxRepository(db).exists_by_ref(...)` → `BillingRepository(db).exists_by_ref(...)` (same kwargs)
       Preserve verbatim (security invariants):
       - `normalize_code(raw)` — `return raw.strip().lower()` (single source of truth per Pitfall 10).
       - `hash_code(raw)` — sha256 of normalized code.
       - In `redeem`: acquire `user = await UserRepository(db).get_by_id(user_id, for_update=True)` BEFORE mutation; check `await BillingRepository(db).exists_by_ref(tx_type=BalanceTransaction.TYPE_VOUCHER_REDEEM, ref_type="voucher_code", ref_id=str(code.id))` for idempotency; `BillingRepository(db).add_tx(BalanceTransaction(..., ref_type="voucher_code", ref_id=str(code.id), ...))` for the audit row; finally `await db.commit()`.

    7. **services/usage_stat_service.py:** Port from `services/user-service/src/services/usage_stat_service.py` (338 lines). Imports:
       `from common.db import ListParams, PaginatedResult` → `from api_service.common.infra.db.query import ListParams, PaginatedResult`
       `from repositories import UsageStatRepository` → `from api_service.repositories.billing_repository import BillingRepository`
       `from schemas.billing import UsageAnalyticsBucket, UsageAnalyticsBucketCost, ...` → `from api_service.schemas.billing import UsageAnalyticsBucket, UsageAnalyticsBucketCost, UsageAnalyticsData, UsageAnalyticsRange, UsageStatItem, ...`
       `from models import UsageStat, ApiCallLog` → `from api_service.models import UsageStat, ApiCallLog`
       Repo method renames at all 9 call sites per RESEARCH § Repository Method Translation Table:
       `UsageStatRepository(db).get_bucket(...)` → `BillingRepository(db).stat_get_bucket(...)`
       `UsageStatRepository(db).get_user_stats(...)` → `BillingRepository(db).stat_get_user_stats(...)`
       `UsageStatRepository(db).get_all_stats(...)` → `BillingRepository(db).stat_get_all_stats(...)`
       `UsageStatRepository(db).list_usage_logs(...)` → `BillingRepository(db).stat_list_usage_logs(...)`
       `UsageStatRepository(db).list_analytics_logs(...)` → `BillingRepository(db).stat_list_analytics_logs(...)`
       `UsageStatRepository(db).list_logs_for_hour(...)` → `BillingRepository(db).stat_list_logs_for_hour(...)`
       `repo.add(bucket)` (UsageStat per-row insert) → `BillingRepository(db).session.add(bucket)` (no per-row helper exists per RESEARCH § Repository Method Translation Table note).
       Preserve verbatim:
       - `_build_usage_analytics_window(range_name, now_)`: dispatch for `"8h" | "24h" | "7d" | "30d"`.
       - Granularity branch in `get_usage_analytics`: `if start and end: granularity = "hour" if (end - start) <= timedelta(hours=48) else "day"`.
       - **Pitfall 8:** Do NOT add `error_code='invalid_model'` filtering at this service layer — `BillingRepository.stat_list_analytics_logs` / `stat_list_logs_for_hour` already embed `_exclude_invalid_model()` (verified `billing_repository.py:21-27, 263, 275`).

    Use the `log_event(logger, level, "eventName", k=v)` pattern (no string interpolation) for any logging added. Service classes are `@staticmethod` only, no instances, per `services/user-service/CLAUDE.md`.

    DO NOT add controllers or BalanceService yet — those are Task 2.
  </action>
  <verify>
    <automated>
      cd services/api-service &&
      python -c "from api_service.schemas.keys import ApiKeyItem, ApiKeyCreateData, ApiKeyCreateRequest, ApiKeyUpdateRequest; fields = ApiKeyItem.model_fields; assert 'key_prefix' in fields; assert 'key' not in fields; assert 'key_hash' not in fields; assert 'key' in ApiKeyCreateData.model_fields, 'ApiKeyCreateData must expose plaintext key once'; print('schemas.keys OK')" &&
      python -c "from api_service.schemas.billing import BalanceResponseData, BalanceTransactionItem, TopupOrderItem, UsageAnalyticsData, UsageAnalyticsRange, UsageStatItem, VoucherRedeemRequest, ApiCallLogItem; import inspect; assert hasattr(ApiCallLogItem, 'from_orm_instance')" &&
      python -c "from api_service.schemas.billing import BalanceResponseData; r = BalanceResponseData(balance=1000, frozen_amount=100, used_amount=50, total_requests=10, total_tokens=500); d = r.model_dump(); assert d['available_balance'] == 900, d" &&
      python -c "from api_service.services.api_key_service import ApiKeyService; assert all(hasattr(ApiKeyService, m) for m in ['list','create','update','disable','delete','verify_key_ownership','validate_by_hash','_refresh_status'])" &&
      python -c "from api_service.services.topup_order_service import TopupOrderService; assert hasattr(TopupOrderService, 'create_order') or hasattr(TopupOrderService, 'create')" &&
      python -c "from api_service.services.voucher_service import VoucherService; assert VoucherService.normalize_code('  ABC123 ') == 'abc123'" &&
      python -c "from api_service.services.usage_stat_service import UsageStatService" &&
      grep -c "BalanceTxRepository\\|TopupOrderRepository\\|UsageStatRepository\\|VoucherRedemptionCodeRepository\\|EmailCodeRepository\\|SessionRepository" services/api-service/api_service/services/*.py | { ! grep -v ":0$" ; } &&
      grep -c "from common\\.\\|from utils\\.\\|from gateways\\.\\|from repositories import \\|from schemas import \\|from core\\.config import\\|from services\\." services/api-service/api_service/services/api_key_service.py services/api-service/api_service/services/topup_order_service.py services/api-service/api_service/services/voucher_service.py services/api-service/api_service/services/usage_stat_service.py services/api-service/api_service/schemas/keys.py services/api-service/api_service/schemas/billing.py | { ! grep -v ":0$" ; } &&
      grep -q "for_update=True" services/api-service/api_service/services/voucher_service.py &&
      grep -q "stat_list_analytics_logs\\|stat_list_logs_for_hour" services/api-service/api_service/services/usage_stat_service.py &&
      cd services/api-service && pytest tests/ -x -q
    </automated>
  </verify>
  <acceptance_criteria>
    - `schemas/keys.py::ApiKeyItem` has `key_prefix` but NOT `key` or `key_hash` (security invariant — verified by `ApiKeyItem.model_fields`).
    - `schemas/keys.py::ApiKeyCreateData` has `key` field (plaintext returned once on create).
    - `schemas/billing.py::BalanceResponseData` exposes computed field `available_balance == balance - frozen_amount`.
    - `schemas/billing.py::UsageAnalyticsRange` is `Literal["8h", "24h", "7d", "30d"]`.
    - `schemas/billing.py::ApiCallLogItem.from_orm_instance` classmethod exists.
    - `services/api_key_service.py` defines `ApiKeyService` with staticmethods `list`, `create`, `update`, `disable`, `delete`, `verify_key_ownership`, `validate_by_hash`, `_refresh_status`. `delete` sets `api_key.deleted_at = now()` (soft — `grep -q "deleted_at" services/api-service/api_service/services/api_key_service.py`).
    - `services/topup_order_service.py` references `BillingRepository.topup_add`, `topup_list_for_user`, `topup_list_all` (NOT `TopupOrderRepository.*`).
    - `services/voucher_service.py` references `VoucherRepository` (class name) and `BillingRepository.add_tx` / `.exists_by_ref` (NOT `BalanceTxRepository.*` or `VoucherRedemptionCodeRepository.*`).
    - `services/voucher_service.py::VoucherService.normalize_code` returns `raw.strip().lower()` (Pitfall 10 — verified by assertion test in <verify>).
    - `services/voucher_service.py` uses `UserRepository(db).get_by_id(user_id, for_update=True)` inside `redeem` (SELECT FOR UPDATE).
    - `services/usage_stat_service.py` references `BillingRepository.stat_*` methods (NOT `UsageStatRepository.*`). Grep finds `stat_list_analytics_logs` and `stat_list_logs_for_hour`.
    - No service file under `services/api-service/api_service/services/{api_key,topup_order,voucher,usage_stat}_service.py` references the deleted classes (Pitfall 1 enforced — grep for `BalanceTxRepository\\|TopupOrderRepository\\|UsageStatRepository\\|VoucherRedemptionCodeRepository` returns 0 across all 4 new files).
    - No `from common\\.…` / `from utils\\.…` / `from gateways\\.…` / `from repositories import …` / `from schemas import …` / `from services\\.…` / `from core.config import …` imports in any new file (Import Rewrite Map fully enforced — grep returns 0 across keys.py, billing.py, and the 4 services).
    - All existing tests continue to pass: `pytest tests/ -x -q` exits 0.
  </acceptance_criteria>
  <done>
    schemas/keys.py + schemas/billing.py + 4 services (api_key, topup_order, voucher, usage_stat) ported. All repo-method renames per RESEARCH § Repository Method Translation Table applied. Security invariants (key_prefix only, SELECT FOR UPDATE on redeem, Pitfall 10 normalization) verified. No regression in existing tests. BalanceService + controllers come next.
  </done>
</task>

<task type="auto">
  <name>Task 2: BalanceService (largest service — 411 LOC with SELECT FOR UPDATE on every wallet mutation) + controllers/keys.py + controllers/billing.py + router mounts</name>
  <files>
    services/api-service/api_service/services/balance_service.py,
    services/api-service/api_service/controllers/keys.py,
    services/api-service/api_service/controllers/billing.py,
    services/api-service/api_service/core/router.py
  </files>
  <read_first>
    services/user-service/src/services/balance_service.py,
    services/user-service/src/controllers/keys.py,
    services/user-service/src/controllers/billing.py,
    services/api-service/api_service/repositories/billing_repository.py,
    services/api-service/api_service/repositories/user_repository.py,
    services/api-service/api_service/repositories/api_key_repository.py,
    services/api-service/api_service/repositories/voucher_repository.py,
    services/api-service/api_service/services/api_key_service.py,
    services/api-service/api_service/services/topup_order_service.py,
    services/api-service/api_service/services/voucher_service.py,
    services/api-service/api_service/services/usage_stat_service.py,
    services/api-service/api_service/schemas/keys.py,
    services/api-service/api_service/schemas/billing.py,
    services/api-service/api_service/schemas/common.py,
    services/api-service/api_service/core/router.py,
    services/api-service/api_service/core/db.py,
    services/api-service/api_service/core/policies.py,
    services/api-service/api_service/common/api/pagination.py,
    services/api-service/api_service/common/infra/db/query.py,
    services/api-service/api_service/common/core/exceptions.py,
    services/api-service/api_service/models/__init__.py,
    .planning/phases/04-user-domain-controllers/04-RESEARCH.md,
    .planning/phases/04-user-domain-controllers/04-PATTERNS.md
  </read_first>
  <action>
    Port the BalanceService and both controllers, then register the routers. Order:

    1. **services/balance_service.py:** Port from `services/user-service/src/services/balance_service.py` (~411 lines). Imports per RESEARCH § Import Translation Table:
       `from common.db import ListParams, PaginatedResult` → `from api_service.common.infra.db.query import ListParams, PaginatedResult`
       `from common.utils.timezone import now` → `from api_service.common.utils.timezone import now`
       `from common.observability import log_event` → `from api_service.common.observability import log_event`
       `from common.core.exceptions import ...` → `from api_service.common.core.exceptions import ...`
       `from repositories import ApiKeyRepository, BalanceTxRepository, TopupOrderRepository, UserRepository` → split into:
         `from api_service.repositories.api_key_repository import ApiKeyRepository`
         `from api_service.repositories.billing_repository import BillingRepository`
         `from api_service.repositories.user_repository import UserRepository`
       `from models import BalanceTransaction, User, UserApiKey, TopupOrder` → `from api_service.models import BalanceTransaction, User, UserApiKey, TopupOrder`

       Repo method renames per RESEARCH § Repository Method Translation Table — apply at EVERY call site:
       - `tx_repo = BalanceTxRepository(db)` → `billing_repo = BillingRepository(db)`
       - `tx_repo.exists_by_ref(tx_type=..., ref_type=..., ref_id=...)` → `billing_repo.exists_by_ref(...)` (same kwargs)
       - `tx_repo.add(BalanceTransaction(...))` → `billing_repo.add_tx(BalanceTransaction(...))`
       - `tx_repo.list_for_user(...)` → `billing_repo.list_tx_for_user(...)`
       - `tx_repo.list_all(...)` → `billing_repo.list_tx_all(...)`
       - `TopupOrderRepository(db).get_for_user_by_order_no(...)` → `BillingRepository(db).topup_get_for_user_by_order_no(...)`

       Preserve verbatim (these are security-critical):
       - `_get_user(db, user_id, for_update=False)` — calls `UserRepository(db).get_by_id(user_id, for_update=for_update)`.
       - **Every wallet mutation method (`consume_for_call_log`, `freeze`, `settle`, `refund`, `topup`, `redeem_code`, `admin_adjust`) starts with `user = await BalanceService._get_user(db, user_id, for_update=True)` BEFORE any mutation.** This is the SELECT FOR UPDATE row lock — Pitfall 2/4 in RESEARCH § Anti-Patterns.
       - **Every wallet mutation precedes the TX insert with an `exists_by_ref` idempotency check:**
         - `api_call` → `ref_id=request_id` (consume/freeze/settle/refund)
         - `topup_order` → `ref_id=order_no`
         - `voucher_code` → `ref_id=str(code.id)` (already implemented in voucher_service.py Task 1)
       - `_transaction_exists` helper.
       - All `await db.commit()` calls in their existing locations (service-layer explicit commit per project convention).

       Public read methods (used by `/billing/balance` and `/billing/transactions` controllers):
       - `get_balance(db, user_id) -> BalanceResponseData` (or whatever the source returns — port verbatim).
       - `list_transactions(db, user_id, *, params: ListParams, type_filter=None) -> PaginatedResult[BalanceTransaction]`.

    2. **controllers/keys.py:** Port from `services/user-service/src/controllers/keys.py` (107 lines). Imports per RESEARCH § Import Translation Table + Pitfall 3 (`get_db_session` → `get_db`):
       `from common.core.exceptions import ...` → `from api_service.common.core.exceptions import ...`
       `from core.dependencies import get_db_session` → `from api_service.core.db import get_db`
       `from core.policies import require_active_user` → `from api_service.core.policies import require_active_user`
       `from models import User` → `from api_service.models import User`
       `from schemas import ApiKeyCreateData, ApiKeyCreateRequest, ApiKeyItem, ApiKeyUpdateRequest, ApiResponse, ...` → `from api_service.schemas.keys import ApiKeyCreateData, ApiKeyCreateRequest, ApiKeyItem, ApiKeyUpdateRequest, ...` and `from api_service.schemas.common import ApiResponse`
       `from services.api_key_service import ApiKeyService` → `from api_service.services.api_key_service import ApiKeyService`

       Router declaration: `router = APIRouter(prefix="/keys", tags=["keys"])` (per source — keep prefix in router).

       5 endpoints — port verbatim with `Depends(get_db)` + `Depends(require_active_user)` on each:
       1. `GET ""` (list) — `ApiKeyService.list(db, int(current_user.id))` → wraps in `ApiResponse[list[ApiKeyItem]]`.
       2. `POST ""` (create) — `key, raw_key = await ApiKeyService.create(db, user_id=..., name=..., ...)` returning `{"code": 201, "message": "created", "data": {"key": raw_key, "item": ApiKeyItem.model_validate(key)}}` — plaintext key in response only here (per `ApiKeyCreateData`).
       3. `PATCH "/{key_id}"` (update) — pass `provided_fields=set(payload.model_fields_set)` so service can distinguish "field absent" from "field=None" (PATTERNS § controllers/keys.py — port verbatim).
       4. `POST "/{key_id}/disable"`.
       5. `DELETE "/{key_id}"` — `ApiKeyService.delete` (soft).

    3. **controllers/billing.py:** Port from `services/user-service/src/controllers/billing.py` (309 lines). Imports:
       `from common.api import PaginatedResponse` → `from api_service.common.api.pagination import PaginatedResponse`
       `from common.db import ListParams` → `from api_service.common.infra.db.query import ListParams`
       `from common.utils.timezone import now` → `from api_service.common.utils.timezone import now`
       `from core.dependencies import get_db_session` → `from api_service.core.db import get_db`  (Pitfall 3)
       `from core.policies import require_active_user` → `from api_service.core.policies import require_active_user`
       `from schemas import ApiCallLogItem, ApiResponse, BalanceResponseData, ...` → split into:
         `from api_service.schemas.common import ApiResponse`
         `from api_service.schemas.billing import (ApiCallLogItem, BalanceResponseData, BalanceTransactionItem, TopupOrderItem, UsageAnalyticsData, UsageAnalyticsRange, UsageStatItem, VoucherRedeemRequest, VoucherRedeemResponseData, VoucherRedemptionItem, ...)`
       `from services.balance_service import BalanceService` → `from api_service.services.balance_service import BalanceService`
       `from services.topup_order_service import TopupOrderService` → `from api_service.services.topup_order_service import TopupOrderService`
       `from services.voucher_service import VoucherService` → `from api_service.services.voucher_service import VoucherService`
       `from services.usage_stat_service import UsageStatService` → `from api_service.services.usage_stat_service import UsageStatService`
       `from services.api_key_service import ApiKeyService` → `from api_service.services.api_key_service import ApiKeyService`

       Router declaration: `router = APIRouter(prefix="/billing", tags=["billing"])`.

       `_build_list_params` helper — port verbatim (source lines 44-65, PATTERNS § controllers/billing.py).

       Define module-level constant `MAX_BILLING_RANGE_DAYS = 90` (source convention — verifies the 90-day cap referenced in `must_haves`).

       8 endpoints — port verbatim:
       1. `GET /balance` — `BalanceService.get_balance(db, int(current_user.id))`.
       2. `GET /transactions` — paginated via `_build_list_params`, type filter, calls `BalanceService.list_transactions`.
       3. `GET /topup-orders` — `TopupOrderService.list_orders`.
       4. `POST /topup-orders` — `TopupOrderService.create_order`.
       5. `POST /vouchers/redeem` — `VoucherService.redeem`.
       6. `GET /vouchers/redemptions` — `VoucherService.list_for_user`.
       7. `GET /usage` — `UsageStatService.get_user_stats` (paginated, time-bound).
       8. `GET /usage/analytics` — `UsageStatService.get_usage_analytics` (range or start/end).
       9. `GET /usage/logs` — `UsageStatService.list_usage_logs` returning `ApiCallLogItem.from_orm_instance(item)` per row (PATTERNS § controllers/billing.py — `api_key.name` eager-loaded).

       (Source has 8-9 endpoints depending on count of usage variants — port whatever the source defines, do not invent.)

       Key-ownership pre-check pattern (source lines 209-210, 240-241, 280-281 — port verbatim where present): `if api_key_id is not None: await ApiKeyService.verify_key_ownership(db, api_key_id, int(current_user.id))`.

       `try/except Exception: logger.exception(...); raise` pattern for each endpoint per PATTERNS § Error Handling.

    4. **core/router.py:** Append to the existing include_router block from 04-01:
       `from api_service.controllers import keys, billing`
       `api_router.include_router(keys.router)        # /keys — 5 endpoints (prefix in router)`
       `api_router.include_router(billing.router)     # /billing/* — 8 endpoints (prefix in router)`
       Place these AFTER the existing `auth.router` include. Do NOT include `model_catalog.router` — that mounts in 04-03.
  </action>
  <verify>
    <automated>
      cd services/api-service &&
      python -c "from api_service.services.balance_service import BalanceService; assert all(hasattr(BalanceService, m) for m in ['_get_user', 'consume_for_call_log', 'freeze', 'settle', 'refund', 'topup', 'redeem_code', 'admin_adjust', 'get_balance', 'list_transactions']), [m for m in dir(BalanceService) if not m.startswith('_')]" &&
      python -c "from api_service.controllers.keys import router as keys_router; paths = sorted([r.path for r in keys_router.routes]); print(paths); assert len(keys_router.routes) >= 5, f'expected ≥5 endpoints, got {len(keys_router.routes)}'" &&
      python -c "from api_service.controllers.billing import router as billing_router; paths = sorted([r.path for r in billing_router.routes]); print(paths); assert len(billing_router.routes) >= 8, f'expected ≥8 endpoints, got {len(billing_router.routes)}'" &&
      python -c "from api_service.core.router import api_router; routes = [r.path for r in api_router.routes]; assert any(p.endswith('/keys') or p.startswith('/api/v1/keys') for p in routes), routes; assert any('/billing' in p for p in routes), routes" &&
      grep -c "BalanceTxRepository\\|TopupOrderRepository\\|UsageStatRepository\\|VoucherRedemptionCodeRepository" services/api-service/api_service/services/balance_service.py | grep -q "^0$" &&
      grep -c "get_db_session" services/api-service/api_service/controllers/keys.py services/api-service/api_service/controllers/billing.py | { ! grep -v ":0$" ; } &&
      grep -c "for_update=True" services/api-service/api_service/services/balance_service.py | { read n; [ "$n" -ge 5 ] ; } &&
      grep -q "exists_by_ref" services/api-service/api_service/services/balance_service.py &&
      grep -q "add_tx" services/api-service/api_service/services/balance_service.py &&
      grep -q "MAX_BILLING_RANGE_DAYS" services/api-service/api_service/controllers/billing.py &&
      grep -q "ApiCallLogItem.from_orm_instance" services/api-service/api_service/controllers/billing.py &&
      grep -q "include_router(keys.router)" services/api-service/api_service/core/router.py &&
      grep -q "include_router(billing.router)" services/api-service/api_service/core/router.py &&
      cd services/api-service && python -c "from api_service.main import app; routes = [r.path for r in app.routes]; expected = ['/api/v1/keys','/api/v1/billing/balance','/api/v1/billing/transactions']; missing = [p for p in expected if not any(r == p or r.startswith(p) for r in routes)]; assert not missing, f'missing routes: {missing}, available: {sorted(routes)[:30]}'"
    </automated>
  </verify>
  <acceptance_criteria>
    - `services/balance_service.py::BalanceService` has staticmethods `_get_user`, `consume_for_call_log`, `freeze`, `settle`, `refund`, `topup`, `redeem_code`, `admin_adjust`, `get_balance`, `list_transactions`.
    - `services/balance_service.py` uses `for_update=True` on at least 5 distinct call sites (one per wallet-mutating method — Pitfall 2/4 enforced by grep count).
    - `services/balance_service.py` references `BillingRepository.add_tx` and `BillingRepository.exists_by_ref` (NOT `BalanceTxRepository.*` — grep returns 0).
    - `controllers/keys.py` defines `APIRouter(prefix="/keys", tags=["keys"])` with at least 5 routes, no `get_db_session` reference (Pitfall 3 — grep returns 0).
    - `controllers/billing.py` defines `APIRouter(prefix="/billing", tags=["billing"])` with at least 8 routes, references `MAX_BILLING_RANGE_DAYS = 90` and `ApiCallLogItem.from_orm_instance`.
    - `core/router.py` includes both `keys.router` and `billing.router` AFTER the `auth.router` line from 04-01.
    - `from api_service.main import app` produces an app whose routes include `/api/v1/keys`, `/api/v1/billing/balance`, `/api/v1/billing/transactions`.
    - No `BalanceTxRepository / TopupOrderRepository / UsageStatRepository / VoucherRedemptionCodeRepository` references anywhere in the new files (Pitfall 1 fully enforced).
  </acceptance_criteria>
  <done>
    BalanceService ported with all 7 wallet-mutating methods preserving SELECT FOR UPDATE + ref_id idempotency. controllers/keys.py and controllers/billing.py mounted at `/api/v1/keys/*` and `/api/v1/billing/*`. Router has 3 user routers wired (auth from 04-01 + keys + billing). Task 3 adds the test suite.
  </done>
</task>

<task type="auto">
  <name>Task 3: 7 integration/unit tests covering USER-04 (T-04-10..17) — keys, api_key_service, billing balance/tx/voucher/topup/usage, granularity</name>
  <files>
    services/api-service/tests/test_keys.py,
    services/api-service/tests/test_api_key_service.py,
    services/api-service/tests/test_billing_balance.py,
    services/api-service/tests/test_billing_tx.py,
    services/api-service/tests/test_voucher.py,
    services/api-service/tests/test_topup.py,
    services/api-service/tests/test_usage.py,
    services/api-service/tests/test_usage_stat_service.py
  </files>
  <read_first>
    services/api-service/tests/test_auth_dependencies.py,
    services/api-service/tests/conftest.py,
    services/api-service/tests/test_auth_register.py,
    services/api-service/tests/test_auth_me.py,
    services/api-service/api_service/services/api_key_service.py,
    services/api-service/api_service/services/balance_service.py,
    services/api-service/api_service/services/voucher_service.py,
    services/api-service/api_service/services/usage_stat_service.py,
    services/api-service/api_service/controllers/keys.py,
    services/api-service/api_service/controllers/billing.py,
    services/api-service/api_service/schemas/keys.py,
    services/api-service/api_service/schemas/billing.py,
    services/api-service/api_service/core/policies.py,
    .planning/phases/04-user-domain-controllers/04-VALIDATION.md,
    .planning/phases/04-user-domain-controllers/04-PATTERNS.md
  </read_first>
  <action>
    Create 8 test files covering VALIDATION slots T-04-10 through T-04-17, plus a topup test for completeness. Use the same mocking style as `tests/test_auth_dependencies.py:80-91` (`@pytest.mark.asyncio` + `@patch(...)` + `AsyncMock`). For integration tests of controllers, use `httpx.AsyncClient(app=app, base_url="http://test")` with `app.dependency_overrides[get_db] = lambda: mock_db` and `app.dependency_overrides[require_active_user] = lambda: mock_user`. Tear down overrides in each test.

    1. **tests/test_keys.py** — VALIDATION T-04-10 + T-04-11 (USER-04):
       - `test_create_returns_plaintext_once` (T-04-10): mock `ApiKeyService.create` to return `(api_key_obj, "sk-AAAA...")`; POST `/api/v1/keys` with `{"name": "test"}`; assert response.status == 201, `resp.json()["data"]["key"] == "sk-AAAA..."`, `resp.json()["data"]["item"]["key_prefix"] == "sk-AAAA"` (first 8 chars), and crucially `"key" not in resp.json()["data"]["item"]` (item never exposes plaintext).
       - `test_list_no_secrets` (T-04-11): mock `ApiKeyService.list` returning `[key1, key2]`; GET `/api/v1/keys`; assert no item in `resp.json()["data"]` has `key` or `key_hash` field. `for item in resp.json()["data"]: assert "key" not in item and "key_hash" not in item`.

    2. **tests/test_api_key_service.py** — unit tests for `ApiKeyService`:
       - `test_delete_is_soft` (T-04-12): mock `ApiKeyRepository.get_owned_key` to return a `MagicMock` `api_key`; await `ApiKeyService.delete(db, key_id=1, user_id=1)`; assert `api_key.deleted_at` was set (not None) AND `db.commit` was awaited. No row-delete SQL.
       - Optional: `test_refresh_status_expired`: pass a key with `expires_at` in the past, assert `_refresh_status` sets `STATUS_EXPIRED`.

    3. **tests/test_billing_balance.py** — VALIDATION T-04-13 (USER-04):
       - `test_balance_fields`: mock `BalanceService.get_balance` returning `BalanceResponseData(balance=1000, frozen_amount=100, used_amount=50, total_requests=10, total_tokens=500)`; GET `/api/v1/billing/balance`; assert int fields present and `data.available_balance == 900` (computed_field).

    4. **tests/test_billing_tx.py** — VALIDATION T-04-14 (USER-04):
       - `test_tx_filter_by_type`: mock `BalanceService.list_transactions` returning `PaginatedResult(items=[...], total=2, page=1, page_size=20)`; GET `/api/v1/billing/transactions?type=1&page=1&page_size=20`; assert `BalanceService.list_transactions.call_args.kwargs.get("type_filter") == 1` (or equivalent — verify against ported source signature); assert response body has `items` + `total`.

    5. **tests/test_voucher.py** — VALIDATION T-04-15 (USER-04):
       - `test_redeem_idempotent`: simulate the ref_id idempotency check. Mock `BillingRepository.exists_by_ref` returning `True` on the second call; mock `VoucherRepository.get_by_hash` returning the same code both times; mock `UserRepository.get_by_id` returning a `MagicMock` user with `balance=0` initially.
         First call: should credit the user. Second call: `exists_by_ref` short-circuits — `balance` MUST NOT be incremented again, `BillingRepository.add_tx` MUST NOT be called the second time. Assert `add_tx.call_count == 1` after both calls.

    6. **tests/test_topup.py** — supports USER-04 topup flow (no specific VALIDATION slot but completes USER-04 coverage):
       - `test_create_topup_order_within_bounds`: mock `TopupOrderService.create_order`; POST `/api/v1/billing/topup-orders` with `amount=settings.MIN_TOPUP_AMOUNT`; expect 201/200 and order_no in response.
       - `test_create_topup_order_below_min`: POST with `amount=settings.MIN_TOPUP_AMOUNT - 1`; expect 422 or 400 (whatever source returns — port-verify behavior).

    7. **tests/test_usage.py** — VALIDATION T-04-16 (USER-04):
       - `test_range_capped`: GET `/api/v1/billing/usage?start=2024-01-01T00:00:00&end=2024-06-01T00:00:00` (>90 days); expect 400 / `ValidationException` raised by `ListParams.validate_time_range` (`max_span_days=MAX_BILLING_RANGE_DAYS=90`).

    8. **tests/test_usage_stat_service.py** — VALIDATION T-04-17 (USER-04, unit test for granularity logic):
       - `test_granularity_switch`: directly call `UsageStatService._build_usage_analytics_window("24h", now)` and verify granularity is `"hour"`; with `range_name="30d"` verify `"day"`. Then call `UsageStatService.get_usage_analytics(db, user_id=1, start=t0, end=t0 + timedelta(hours=48))` (mock `BillingRepository.stat_list_logs_for_hour`/`stat_list_analytics_logs` as needed) and assert `granularity == "hour"` in the result. With `end = t0 + timedelta(hours=49)`, granularity must switch to `"day"`.

    Mock pattern for controller integration tests (template):
    ```
    from unittest.mock import AsyncMock, MagicMock, patch
    import pytest
    from httpx import AsyncClient
    from api_service.main import app
    from api_service.core.db import get_db
    from api_service.core.policies import require_active_user

    @pytest.mark.asyncio
    @patch("api_service.controllers.keys.ApiKeyService.create", new_callable=AsyncMock)
    async def test_create_returns_plaintext_once(mock_create, mock_user, mock_db):
        fake_key = MagicMock(id=1, key_prefix="sk-AAAA", name="test", status=1, quota_mode=0, quota_limit=0, quota_used=0, allowed_models=None, allow_ips=None, expires_at=None, last_used_at=None, created_at=..., updated_at=...)
        mock_create.return_value = (fake_key, "sk-AAAABBBB...")
        app.dependency_overrides[get_db] = lambda: mock_db
        app.dependency_overrides[require_active_user] = lambda: mock_user
        try:
            async with AsyncClient(app=app, base_url="http://test") as client:
                resp = await client.post("/api/v1/keys", json={"name": "test"})
            assert resp.status_code == 201
            data = resp.json()["data"]
            assert data["key"].startswith("sk-")
            assert "key" not in data["item"]
            assert data["item"]["key_prefix"] == "sk-AAAA"
        finally:
            app.dependency_overrides.clear()
    ```
    Use this template structure for every controller-level test.
  </action>
  <verify>
    <automated>
      cd services/api-service &&
      pytest tests/test_keys.py tests/test_api_key_service.py tests/test_billing_balance.py tests/test_billing_tx.py tests/test_voucher.py tests/test_topup.py tests/test_usage.py tests/test_usage_stat_service.py -x -q &&
      pytest tests/ -x -q
    </automated>
  </verify>
  <acceptance_criteria>
    - VALIDATION slots green: `pytest tests/test_keys.py tests/test_api_key_service.py tests/test_billing_balance.py tests/test_billing_tx.py tests/test_voucher.py tests/test_topup.py tests/test_usage.py tests/test_usage_stat_service.py -x -q` exits 0.
    - T-04-10 covered by `test_keys.py::test_create_returns_plaintext_once` — asserts `data["key"]` exists, `data["item"]` has `key_prefix` but no `key`/`key_hash`.
    - T-04-11 covered by `test_keys.py::test_list_no_secrets` — asserts no item in `data` exposes `key` or `key_hash`.
    - T-04-12 covered by `test_api_key_service.py::test_delete_is_soft` — asserts `api_key.deleted_at` set and `db.commit` awaited.
    - T-04-13 covered by `test_billing_balance.py::test_balance_fields` — asserts `available_balance == balance - frozen_amount`.
    - T-04-14 covered by `test_billing_tx.py::test_tx_filter_by_type` — asserts type filter passed through.
    - T-04-15 covered by `test_voucher.py::test_redeem_idempotent` — asserts `add_tx.call_count == 1` after duplicate redeem.
    - T-04-16 covered by `test_usage.py::test_range_capped` — asserts 90-day cap enforced.
    - T-04-17 covered by `test_usage_stat_service.py::test_granularity_switch` — asserts granularity flips at 48h.
    - Full test suite: `pytest tests/ -x -q` exits 0 (Wave 0 + 04-01 auth tests + 04-02 tests + Phase 3 regression tests all green).
    - No test depends on a real database, real Redis, or real ARQ pool — only `AsyncMock`/`MagicMock`/dependency_overrides.
  </acceptance_criteria>
  <done>
    8 test files in place. All VALIDATION T-04-10..17 slots green. Full test suite stays green. Plan 04-02 is complete; plan 04-03 (model catalog + email final integration tests) is unblocked.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → /keys | Authenticated via `require_active_user`; mutating endpoints (POST/PATCH/DELETE) require valid user_access_token cookie. |
| Browser → /billing | Authenticated via `require_active_user`; balance/transaction queries scoped by `int(current_user.id)`. |
| Service → DB (wallet) | Row-locked via `SELECT … FOR UPDATE` on `users` before every wallet mutation. |
| Service → DB (voucher) | Row-locked on both `voucher_redemption_codes` and `users`; ref_id idempotency on `balance_transactions`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-S3 | Spoofing | API key validation surface | mitigate (out of scope here) | `validate_by_hash` ported but not used until Phase 6 relay; key plaintext stored only in client memory after `POST /keys` response, hash-only in DB (`UserApiKey.key_hash` = sha256 of `sk-…`). |
| T-04-T3 | Tampering | Voucher double-redeem | mitigate | `BillingRepository.exists_by_ref(tx_type=VOUCHER_REDEEM, ref_type='voucher_code', ref_id=str(code.id))` short-circuits second call; `SELECT FOR UPDATE` on both voucher code row and user row (PATTERNS § voucher_service.py). Test T-04-15 asserts `add_tx.call_count == 1`. |
| T-04-T4 | Tampering | Wallet race (concurrent consume) | mitigate | Every `BalanceService` wallet method calls `_get_user(db, user_id, for_update=True)` BEFORE mutation (≥5 `for_update=True` occurrences verified by grep). |
| T-04-T5 | Tampering | Topup amount tampering | mitigate | `settings.MIN_TOPUP_AMOUNT` / `settings.MAX_TOPUP_AMOUNT` range validation in `TopupOrderService.create_order` (Wave 0 settings keys). |
| T-04-I4 | Information disclosure | API key plaintext leak via list/get | mitigate | `ApiKeyItem.model_fields` has `key_prefix` but neither `key` nor `key_hash`. Test T-04-11 asserts `"key" not in item and "key_hash" not in item`. |
| T-04-I5 | Information disclosure | Cross-user data leak in /billing/transactions | mitigate | All `BillingRepository.list_tx_for_user(user_id, …)` filter by `user_id`. Controllers always pass `int(current_user.id)`. No admin path here (Phase 5). |
| T-04-I6 | Information disclosure | API key in logs | mitigate | `log_event` discipline; never log raw `sk-…` — only `key_prefix` (RESEARCH § Security Domain). |
| T-04-D3 | Denial of service | Unbounded usage time range | mitigate | `ListParams.validate_time_range(max_span_days=MAX_BILLING_RANGE_DAYS=90)`. Test T-04-16 asserts cap enforced. |
| T-04-D4 | Denial of service | Unbounded `/keys` count | mitigate | `settings.MAX_API_KEYS_PER_USER=20` enforced in `ApiKeyService.create` via `count_for_user` check. |
| T-04-E2 | Elevation of privilege | `/keys/{id}` cross-user access | mitigate | Every mutating endpoint calls `verify_key_ownership(db, key_id, int(current_user.id))` which uses `ApiKeyRepository.get_owned_key(key_id, user_id)` — owner-scoped query. |
</threat_model>

<verification>
**Per-task verification** runs inside each `<verify><automated>` block.

**Plan-level gate** before merging to 04-03:
- `cd services/api-service && pytest tests/ -x -q` exits 0 (Wave 0 + 04-01 auth + 04-02 keys/billing + Phase 3 regression).
- Routes registered at app level: `/api/v1/keys` (5) + `/api/v1/billing/*` (≥8) + `/api/v1/auth/*` (10) from 04-01 = ≥23 routes (smoke test optional).
- No `from common\\.…` / `from utils\\.…` / `from gateways\\.…` / `from repositories import …` / `BalanceTxRepository` / `TopupOrderRepository` / `UsageStatRepository` / `VoucherRedemptionCodeRepository` references in any new file (Pitfall 1 + Pitfall 2 enforced).
- VALIDATION T-04-10 through T-04-17 all `✅` (8 of 23 slots — adds to 10 from 04-01, leaving 5 for 04-03).
</verification>

<success_criteria>
- USER-04 fully covered: balance, transactions, voucher redeem (idempotent), topup orders, usage stats / analytics / logs endpoints all functional with tests green.
- 13 endpoints mounted at `/api/v1/keys/*` (5) and `/api/v1/billing/*` (8).
- BalanceService preserves SELECT FOR UPDATE on every wallet mutation (≥5 `for_update=True` call sites verified by grep).
- VoucherService preserves ref_id idempotency (test asserts duplicate redeem doesn't credit twice).
- ApiKeyService preserves "plaintext returned once on create" + "soft delete" invariants (tests verify).
- All repo-method renames from RESEARCH § Repository Method Translation Table applied (no deleted class names anywhere — grep returns 0).
- Test suite green (10 new tests from 04-01 + 8 new tests from 04-02 + 5 Phase 3 baseline tests).
</success_criteria>

<output>
Create `.planning/phases/04-user-domain-controllers/04-02-SUMMARY.md` when done, summarizing:
- Files created/modified.
- VALIDATION slots covered: T-04-10..17 (8 slots in this plan).
- Requirement addressed: USER-04 (full).
- Decisions enacted: D-12 (relied on Wave 0 from 04-01).
- Pitfalls addressed: P1 (repo class translations applied across 4 services + balance_service), P3 (`get_db_session` → `get_db` in both controllers), P8 (no double-filter on `error_code='invalid_model'` in usage_stat_service), P10 (single normalization in VoucherService).
- Unblocked: 04-03 (model catalog + email integration tests).
</output>
</content>
</invoke>
---
phase: 04-user-domain-controllers
plan: 04-03
type: execute
wave: 3
depends_on: [04-01, 04-02]
files_modified:
  - services/api-service/api_service/schemas/model_catalog.py
  - services/api-service/api_service/schemas/__init__.py
  - services/api-service/api_service/services/model_catalog_service.py
  - services/api-service/api_service/controllers/model_catalog.py
  - services/api-service/api_service/core/router.py
  - services/api-service/tests/test_model_catalog.py
autonomous: true
requirements: [USER-05]
requirements_addressed: [USER-05]
tags: [model-catalog, cache, redis, public-api]

must_haves:
  truths:
    - "D-01: no `internal_*.py` files are in scope — the public catalog endpoints in this plan replace the old user-service HTTP-gateway-to-admin-internal pattern with direct in-process service calls"
    - "D-02: send_verification_email ARQ job semantics are owned by 04-01 (email_service migration); this plan only consumes the job-name pattern via cross-plan reference, no SMTP/email logic here"
    - "D-03: this plan introduces `schemas/model_catalog.py` (read-only subset of admin-service model_catalog schemas per D-06) — not a 1:1 user-service port because user-service had no local model_catalog schemas (only an HTTP gateway)"
    - "D-05: cache TTLs match source gateway: vendors=300s, categories=300s, models=120s, model_detail=300s — invalidation logic is deferred to Phase 5 (admin write path will issue `mc:*` deletes); Phase 4 cache is read-only-grow"
    - "D-08: this is plan 3 of exactly 3 plans for Phase 4 (model catalog + final wiring; depends on Wave 1 = 04-01 + Wave 2 = 04-02, finalizes router.include_router for the phase)"
    - "GET /api/v1/model-vendors returns active vendors only (D-04 `active_only=True`) and is publicly readable (no auth required)"
    - "GET /api/v1/models returns active models filtered by optional vendor + free-text `q`, with pagination"
    - "GET /api/v1/models/categories returns active categories only"
    - "GET /api/v1/models/{slug} returns 404 when the slug is missing; slug pattern `^[a-z0-9][a-z0-9._-]*$` enforced at route level"
    - "Every read path caches via `cache_get_or_fetch(key, fetch, ttl)` with key prefix `mc:` and source-matching TTLs: vendors=300s, categories=300s, models=120s, model_detail=300s"
    - "Cache is fail-open: a Redis error falls through to the live DB fetch (per `cache_get_or_fetch` semantics — verified in common/infra/cache.py:45-66)"
    - "Service class is named `ModelCatalogReadService` (NOT `ModelCatalogService` — leaves the latter free for Phase 5 admin variant per D-07)"
    - "Schemas in `schemas/model_catalog.py` cover ONLY read shapes (`ModelVendorItem`, `ModelVendorBrief`, `ModelCategoryItem`, `ModelCategoryBrief`, `SupportedModelItem`, `SupportedModelDetail`); admin write schemas (`*Create`, `*Update`, `*Response(AdminBaseResponse)`) are deferred to Phase 5 (D-06)"
    - "No HTTP gateway calls: the user-service `gateways/model_catalog.py` HTTP-to-admin-service pattern is fully replaced by direct repository access (one less network hop per request)"
    - "All 4 public read endpoints are mounted at `/api/v1/model-vendors`, `/api/v1/models/categories`, `/api/v1/models`, `/api/v1/models/{slug}`"
    - "`schemas/__init__.py` aggregator now re-exports model_catalog read schemas, finalizing the Phase 4 export surface"
  artifacts:
    - path: "services/api-service/api_service/schemas/model_catalog.py"
      provides: "ModelVendorItem, ModelVendorBrief, ModelCategoryItem, ModelCategoryBrief, SupportedModelItem, SupportedModelDetail (read-only subset of admin schemas — D-06)"
      contains: "SupportedModelItem"
    - path: "services/api-service/api_service/services/model_catalog_service.py"
      provides: "ModelCatalogReadService.list_vendors / list_categories / list_models / get_model_by_slug + cache wrapping via cache_get_or_fetch (mc: prefix)"
      contains: "class ModelCatalogReadService"
    - path: "services/api-service/api_service/controllers/model_catalog.py"
      provides: "4 public read endpoints (no auth) — /model-vendors, /models/categories, /models, /models/{slug}"
      contains: "ModelCatalogReadService"
    - path: "services/api-service/tests/test_model_catalog.py"
      provides: "T-04-18 cache_hits, T-04-19 filter, T-04-20 404 — covers USER-05 validation slots"
      contains: "test_cache_hits"
  key_links:
    - from: "services/api-service/api_service/services/model_catalog_service.py"
      to: "services/api-service/api_service/repositories/model_catalog_repository.py"
      via: "ModelVendorRepository / ModelCategoryRepository / ModelCatalogRepository — direct calls (NO HTTP)"
      pattern: "from api_service.repositories.model_catalog_repository import"
    - from: "services/api-service/api_service/services/model_catalog_service.py"
      to: "services/api-service/api_service/common/infra/cache.py"
      via: "cache_get_or_fetch(cache_key, _fetch, ttl)"
      pattern: "cache_get_or_fetch\\("
    - from: "services/api-service/api_service/controllers/model_catalog.py"
      to: "services/api-service/api_service/services/model_catalog_service.py"
      via: "await ModelCatalogReadService.list_vendors/list_categories/list_models/get_model_by_slug(db, ...)"
      pattern: "ModelCatalogReadService\\.(list_vendors|list_categories|list_models|get_model_by_slug)"
    - from: "services/api-service/api_service/core/router.py"
      to: "services/api-service/api_service/controllers/model_catalog.py"
      via: "api_router.include_router(model_catalog.router) — final include_router call"
      pattern: "include_router\\(model_catalog\\.router\\)"
---

<objective>
Port the 4 public model-catalog read endpoints into api-service, replacing the user-service HTTP gateway (`gateways/model_catalog.py`) with a direct read service that reuses Phase 3's `model_catalog_repository`. Add Redis caching layer (`mc:*` keys, source-matching TTLs), publish the final Phase 4 router wiring, and close out USER-05 with three integration tests.

Purpose: Complete the user-facing surface of Phase 4 (auth ✅ 04-01, keys/billing ✅ 04-02, model catalog now). Eliminates one HTTP hop on every model list/detail page load (D-04..07). Phase 5 will later extend `model_catalog_service` with admin CRUD and cache invalidation; this plan ships the read half only (D-05: cache is correct-up-to-TTL; admin invalidation deferred).

Output: 4 endpoints mounted at `/api/v1/model-vendors`, `/api/v1/models/categories`, `/api/v1/models`, `/api/v1/models/{slug}`; the new `ModelCatalogReadService`; the read-only schema subset; final `schemas/__init__.py` aggregator; final `core/router.py` include_router; 3 integration tests covering VALIDATION slots T-04-18, T-04-19, T-04-20.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-user-domain-controllers/04-CONTEXT.md
@.planning/phases/04-user-domain-controllers/04-RESEARCH.md
@.planning/phases/04-user-domain-controllers/04-PATTERNS.md
@.planning/phases/04-user-domain-controllers/04-VALIDATION.md
@.planning/phases/04-user-domain-controllers/04-01-PLAN.md
@.planning/phases/04-user-domain-controllers/04-02-PLAN.md

# Project conventions
@CLAUDE.md
@services/user-service/CLAUDE.md

# 04-01/04-02 outputs we build on
@services/api-service/api_service/core/router.py
@services/api-service/api_service/core/db.py
@services/api-service/api_service/schemas/__init__.py
@services/api-service/api_service/schemas/common.py
@services/api-service/api_service/controllers/__init__.py
@services/api-service/api_service/controllers/auth.py
@services/api-service/api_service/controllers/keys.py
@services/api-service/api_service/controllers/billing.py
@services/api-service/tests/conftest.py

# Phase 3 repositories (call signatures the executor needs)
@services/api-service/api_service/repositories/model_catalog_repository.py

# Cache layer (Phase 2)
@services/api-service/api_service/common/infra/cache.py
@services/api-service/api_service/common/api/pagination.py
@services/api-service/api_service/common/infra/db/query.py
@services/api-service/api_service/common/core/exceptions.py
@services/api-service/api_service/models/__init__.py

# Migration sources (truth-of-source per CONTEXT canonical_refs)
@services/user-service/src/controllers/model_catalog.py
@services/user-service/src/gateways/model_catalog.py
@services/admin-service/src/schemas/model_catalog.py
@services/admin-service/src/services/model_catalog_service.py

<interfaces>
<!-- Phase 3 model catalog repository — exact methods to call. Already provides eager-loading of vendor + category_links. -->

From api_service/repositories/model_catalog_repository.py (verified — Phase 3 D-01 renames applied):
- class ModelVendorRepository(db)
  - list_vendors(*, page: int, page_size: int, active_only: bool) -> tuple[list[ModelVendor], int]
  - get_by_slug(slug: str, *, active_only: bool = False) -> ModelVendor | None
- class ModelCategoryRepository(db)
  - list_categories(*, page: int, page_size: int, active_only: bool) -> tuple[list[ModelCategory], int]
- class ModelCatalogRepository(db)
  - list_models(*, page, page_size, vendors: list[str] | None, q: str | None, category: str | None, active_only: bool) -> tuple[list[ModelCatalog], int]
  - get_by_slug(slug: str, *, active_only: bool = False) -> ModelCatalog | None
  - _with_relationships() — internal: selectinload(vendor) + selectinload(category_links).selectinload(ModelCatalogCategoryMap.category) — used by both list_models and get_by_slug (verified)

Model attributes used by the serializer (per PATTERNS § services/model_catalog_service.py):
- ModelCatalog: id, slug, routing_slug, name, summary, description, sale_input_per_million, sale_output_per_million, sale_cached_input_per_million, capability_tags (list[str]), context_window, max_output_tokens, is_reasoning_model, is_active, sort_order, vendor (ModelVendor), category_links (list[ModelCatalogCategoryMap])
- ModelVendor: id, slug, name, logo_url, is_active, sort_order
- ModelCategory: id, key, name, sort_order, is_active
- ModelCatalogCategoryMap: category, sort_order

From api_service/common/infra/cache.py (verified):
- async cache_get_or_fetch(key: str, fetch: Callable[[], Awaitable[T]], ttl_seconds: int) -> T  # fail-open, JSON serialization

From api_service/schemas/common.py (04-01):
- ApiResponse[T], DateTimeModel

From api_service/common/api/pagination.py:
- class PaginatedResponse[T]  # NOT used here — gateway pattern returns plain dicts with items+total+page+page_size
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: schemas/model_catalog.py read-only subset (D-06 cross-domain copy from admin-service) + extend schemas/__init__.py</name>
  <files>
    services/api-service/api_service/schemas/model_catalog.py,
    services/api-service/api_service/schemas/__init__.py
  </files>
  <read_first>
    services/admin-service/src/schemas/model_catalog.py,
    services/api-service/api_service/schemas/common.py,
    services/api-service/api_service/schemas/__init__.py,
    services/api-service/api_service/common/api/pagination.py,
    services/api-service/api_service/common/utils/timezone.py,
    services/api-service/api_service/models/__init__.py,
    .planning/phases/04-user-domain-controllers/04-RESEARCH.md,
    .planning/phases/04-user-domain-controllers/04-PATTERNS.md
  </read_first>
  <action>
    Port the **read-only subset** of admin-service's `schemas/model_catalog.py` into `api-service` per D-06. Order:

    1. **Identify the read-only classes from `services/admin-service/src/schemas/model_catalog.py`** — per PATTERNS § schemas/model_catalog.py, port ONLY these classes verbatim:
       - `ModelVendorItem(DateTimeModel)` — id, slug, name, logo_url, is_active, sort_order, created_at, updated_at
       - `ModelVendorBrief(BaseModel)` — id, slug, name, logo_url
       - `ModelCategoryItem(DateTimeModel)` — id, key, name, sort_order, is_active, created_at, updated_at
       - `ModelCategoryBrief(BaseModel)` — key, name, sort_order
       - `SupportedModelItem(DateTimeModel)` — full shape per PATTERNS.md (id, slug, routing_slug, name, summary, description, sale_*, capability_tags, context_window, max_output_tokens, is_reasoning_model, is_active, sort_order, vendor: ModelVendorBrief, categories: list[ModelCategoryBrief])
       - `SupportedModelDetail(SupportedModelItem)` — `pass` (identical shape; subclass for API typing clarity)

       Use the source verbatim shape — DO NOT add/remove/rename fields.

    2. **DO NOT port** (admin write — Phase 5 territory per D-06):
       - `ModelVendorCreate`, `ModelVendorUpdate`
       - `ModelCategoryCreate`, `ModelCategoryUpdate`
       - `SupportedModelCreate`, `SupportedModelUpdate`
       - Any `*Response(AdminBaseResponse)` wrapper (AdminBaseResponse doesn't exist yet in Phase 4 — D-10 says Phase 5 will add it to the same schemas/common.py file)
       - Any `Reorder*` payload schemas

    3. **Imports rewrite** per RESEARCH § Import Translation Table:
       - `from schemas.common import AdminBaseResponse, DateTimeModel` → `from api_service.schemas.common import DateTimeModel` (drop AdminBaseResponse — not used by read-only subset)
       - `from common.api import PaginatedResponse` → `from api_service.common.api.pagination import PaginatedResponse` (only if any of the ported classes reference it — check source; if no read class uses it, omit the import)
       - All other `from common.*` or `from utils.*` imports → `api_service.*` equivalents.

    4. **Extend `schemas/__init__.py`** with model_catalog re-exports per PATTERNS § schemas/__init__.py § "Add in Wave 3":
       Append to the existing aggregator:
       `from api_service.schemas.model_catalog import (ModelCategoryBrief, ModelCategoryItem, ModelVendorBrief, ModelVendorItem, SupportedModelDetail, SupportedModelItem)`
       Update `__all__` accordingly. Preserve existing entries from 04-01 (auth + common) and 04-02 (keys + billing) — append only, do not delete.

    Verify the ported classes parse correctly: `ModelVendorBrief(id=1, slug="oai", name="OpenAI", logo_url=None)` constructs without raising; `SupportedModelItem` accepts the full source field list.
  </action>
  <verify>
    <automated>
      cd services/api-service &&
      python -c "from api_service.schemas.model_catalog import ModelVendorItem, ModelVendorBrief, ModelCategoryItem, ModelCategoryBrief, SupportedModelItem, SupportedModelDetail" &&
      python -c "from api_service.schemas.model_catalog import ModelVendorBrief; v = ModelVendorBrief(id=1, slug='oai', name='OpenAI', logo_url=None); assert v.slug == 'oai'" &&
      python -c "from api_service.schemas.model_catalog import SupportedModelItem; fields = set(SupportedModelItem.model_fields.keys()); required = {'id','slug','name','vendor','categories','is_active','sort_order','is_reasoning_model'}; assert required.issubset(fields), f'missing fields: {required - fields}; got {fields}'" &&
      python -c "import api_service.schemas.model_catalog as m; assert not hasattr(m, 'ModelVendorCreate'), 'admin write schemas must not be ported (D-06)'; assert not hasattr(m, 'ModelVendorUpdate'); assert not hasattr(m, 'SupportedModelCreate')" &&
      python -c "from api_service.schemas import (ModelVendorItem, ModelVendorBrief, ModelCategoryItem, ModelCategoryBrief, SupportedModelItem, SupportedModelDetail, ApiResponse, ApiKeyItem, BalanceResponseData, RegisterRequest); print('schemas aggregator OK')" &&
      grep -c "AdminBaseResponse" services/api-service/api_service/schemas/model_catalog.py | grep -q "^0$" &&
      grep -c "from common\\.\\|from schemas\\.\\|from utils\\." services/api-service/api_service/schemas/model_catalog.py | grep -q "^0$" &&
      cd services/api-service && pytest tests/ -x -q
    </automated>
  </verify>
  <acceptance_criteria>
    - `services/api-service/api_service/schemas/model_catalog.py` exports exactly 6 classes: `ModelVendorItem`, `ModelVendorBrief`, `ModelCategoryItem`, `ModelCategoryBrief`, `SupportedModelItem`, `SupportedModelDetail`.
    - `services/api-service/api_service/schemas/model_catalog.py` does NOT export any `*Create`, `*Update`, or any `*Response(AdminBaseResponse)` class (D-06 enforced — `grep -c "Create\\|Update\\|AdminBaseResponse" services/api-service/api_service/schemas/model_catalog.py` returns 0).
    - `SupportedModelItem.model_fields` includes at minimum `id`, `slug`, `name`, `vendor`, `categories`, `is_active`, `sort_order`, `is_reasoning_model` (PATTERNS § schemas/model_catalog.py shape).
    - `from api_service.schemas import (ModelVendorItem, ModelVendorBrief, ModelCategoryItem, ModelCategoryBrief, SupportedModelItem, SupportedModelDetail, ApiResponse, ApiKeyItem, BalanceResponseData, RegisterRequest)` succeeds — schemas/__init__.py aggregator includes Phase 4's entire export surface.
    - No `from common\\.*` or `from schemas\\.*` or `from utils\\.*` imports in `schemas/model_catalog.py` (Import Rewrite Map enforced).
    - All existing tests still pass: `pytest tests/ -x -q` exits 0.
  </acceptance_criteria>
  <done>
    Read-only schemas subset ported with admin write schemas explicitly excluded. schemas/__init__.py aggregator finalizes Phase 4 exports. Task 2 (service + controller) can build on these schemas.
  </done>
</task>

<task type="auto">
  <name>Task 2: ModelCatalogReadService (cache scaffolding from user-service gateway + read methods from admin-service service) + controllers/model_catalog.py + router include_router</name>
  <files>
    services/api-service/api_service/services/model_catalog_service.py,
    services/api-service/api_service/controllers/model_catalog.py,
    services/api-service/api_service/core/router.py
  </files>
  <read_first>
    services/user-service/src/controllers/model_catalog.py,
    services/user-service/src/gateways/model_catalog.py,
    services/admin-service/src/services/model_catalog_service.py,
    services/admin-service/src/schemas/model_catalog.py,
    services/api-service/api_service/schemas/model_catalog.py,
    services/api-service/api_service/repositories/model_catalog_repository.py,
    services/api-service/api_service/common/infra/cache.py,
    services/api-service/api_service/common/core/exceptions.py,
    services/api-service/api_service/core/router.py,
    services/api-service/api_service/core/db.py,
    services/api-service/api_service/models/__init__.py,
    .planning/phases/04-user-domain-controllers/04-RESEARCH.md,
    .planning/phases/04-user-domain-controllers/04-PATTERNS.md
  </read_first>
  <action>
    Build the composed service (cache wrap + read methods), port the controller (replacing HTTP gateway with direct service calls), and mount the final router. Order:

    1. **services/model_catalog_service.py — new module composed of two analogs per PATTERNS § services/model_catalog_service.py.**

       Module-level constants (port verbatim from `services/user-service/src/gateways/model_catalog.py:17-21`):
       `_CACHE_PREFIX = "mc:"`, `_VENDORS_TTL = 300`, `_CATEGORIES_TTL = 300`, `_MODELS_LIST_TTL = 120`, `_MODEL_DETAIL_TTL = 300`.

       Imports:
       `from api_service.common.infra.cache import cache_get_or_fetch`
       `from api_service.repositories.model_catalog_repository import (ModelVendorRepository, ModelCategoryRepository, ModelCatalogRepository)`
       `from api_service.schemas.model_catalog import (ModelVendorItem, ModelVendorBrief, ModelCategoryItem, ModelCategoryBrief, SupportedModelItem, SupportedModelDetail)`
       `from api_service.common.core.exceptions import NotFoundException`
       `from sqlalchemy.ext.asyncio import AsyncSession`

       Class `ModelCatalogReadService` (CLASS NAME LOCKED — NOT `ModelCatalogService`, per D-07 — leaves the latter for Phase 5 admin write variant):
       - `@staticmethod async def list_vendors(db: AsyncSession, *, page: int = 1, page_size: int = 100) -> dict`:
         cache_key = `f"{_CACHE_PREFIX}vendors:{page}:{page_size}"`. Inside `_fetch`: call `ModelVendorRepository(db).list_vendors(page=page, page_size=page_size, active_only=True)` and return `{"items": [ModelVendorItem.model_validate(v).model_dump() for v in vendors], "total": total, "page": page, "page_size": page_size}`. Outer call: `return await cache_get_or_fetch(cache_key, _fetch, _VENDORS_TTL)`.
       - `@staticmethod async def list_categories(db: AsyncSession, *, page: int = 1, page_size: int = 100) -> dict`: same pattern, key prefix `mc:categories:`, TTL `_CATEGORIES_TTL`, `ModelCategoryRepository.list_categories`. Use `ModelCategoryItem.model_validate(...)` for items.
       - `@staticmethod async def list_models(db: AsyncSession, *, page: int = 1, page_size: int = 100, vendors: list[str] | None = None, q: str | None = None, category: str | None = None) -> dict`: cache key includes a deterministic hash of the filter args. Use Python's `hashlib.md5(json.dumps({"page":page,"page_size":page_size,"vendors":sorted(vendors) if vendors else None,"q":q,"category":category}, sort_keys=True).encode()).hexdigest()[:12]` to produce a stable suffix → `cache_key = f"{_CACHE_PREFIX}models:{filter_hash}"`. Inside `_fetch`: call `ModelCatalogRepository(db).list_models(page=page, page_size=page_size, vendors=vendors, q=q, category=category, active_only=True)`; for each model build `SupportedModelItem` via the serializer helper described below; return `{"items": [...model_dump...], "total": total, "page": page, "page_size": page_size}`. Outer: `cache_get_or_fetch(cache_key, _fetch, _MODELS_LIST_TTL)`.
       - `@staticmethod async def get_model_by_slug(db: AsyncSession, slug: str) -> dict`: cache_key = `f"{_CACHE_PREFIX}model:{slug}"`. Inside `_fetch`: call `model = await ModelCatalogRepository(db).get_by_slug(slug, active_only=True)`; if `model is None`: raise `NotFoundException(detail=f"Model not found: {slug}")`. Build `SupportedModelDetail` via serializer; return `model.model_dump()`. Outer: `cache_get_or_fetch(cache_key, _fetch, _MODEL_DETAIL_TTL)`.

       Internal serializer helpers (port from `services/admin-service/src/services/model_catalog_service.py:44-106` per PATTERNS.md § services/model_catalog_service.py):
       - `@staticmethod def _vendor_item(vendor: ModelVendor) -> ModelVendorItem`: full vendor → `ModelVendorItem` mapping.
       - `@staticmethod def _category_item(category: ModelCategory) -> ModelCategoryItem`: full category mapping.
       - `@staticmethod def _model_item(model: ModelCatalog, *, detail: bool = False) -> SupportedModelItem | SupportedModelDetail`: iterate `model.category_links` sorted by `link.sort_order`, building `[ModelCategoryBrief(key=link.category.key, name=link.category.name, sort_order=link.sort_order) for link in sorted(...) if link.category is not None]`. Build `ModelVendorBrief(id=model.vendor.id, slug=model.vendor.slug, name=model.vendor.name, logo_url=model.vendor.logo_url)`. Compose full payload dict and return `SupportedModelDetail(**payload) if detail else SupportedModelItem(**payload)`.

       Notes:
       - `active_only=True` is hardcoded in every public read call per D-04 (user surface always filters to active).
       - Eager loading is handled inside `ModelCatalogRepository._with_relationships` — service does NOT need to add `selectinload(...)`.
       - **D-05 (cache invalidation deferred):** No `cache.delete(...)` calls in this service. Phase 5 admin writes will add invalidation. Add a TODO comment `# TODO(phase-5): admin writes invalidate mc:* keys (D-05 — currently correct-up-to-TTL)`.

    2. **controllers/model_catalog.py:** Port from `services/user-service/src/controllers/model_catalog.py` (54 lines) per PATTERNS § controllers/model_catalog.py.

       Imports:
       `from fastapi import APIRouter, Depends, Path, Query`
       `from fastapi.responses import JSONResponse`
       `from sqlalchemy.ext.asyncio import AsyncSession`
       `from api_service.core.db import get_db`
       `from api_service.services.model_catalog_service import ModelCatalogReadService`
       **DELETE** the source's `from gateways.model_catalog import model_catalog_gateway` import.

       Router declaration (per source line 11): `router = APIRouter(tags=["model-catalog"])` (NO prefix — paths in source are `/model-vendors`, `/models/categories`, `/models`, `/models/{slug}`; mounting `prefix="/api/v1"` happens at api_router level).

       4 public endpoints (no auth — public catalog per source behavior):

       1. `GET /model-vendors`:
          ```
          @router.get("/model-vendors", summary="List model vendors")
          async def list_model_vendors(page: int = Query(1, ge=1), page_size: int = Query(100, ge=1, le=200), db: AsyncSession = Depends(get_db)):
              payload = await ModelCatalogReadService.list_vendors(db, page=page, page_size=page_size)
              return JSONResponse(content=payload)
          ```

       2. `GET /models/categories`:
          ```
          @router.get("/models/categories", summary="List model categories")
          async def list_model_categories(page: int = Query(1, ge=1), page_size: int = Query(100, ge=1, le=200), db: AsyncSession = Depends(get_db)):
              payload = await ModelCatalogReadService.list_categories(db, page=page, page_size=page_size)
              return JSONResponse(content=payload)
          ```

       3. `GET /models`:
          ```
          @router.get("/models", summary="List supported models")
          async def list_supported_models(
              page: int = Query(1, ge=1),
              page_size: int = Query(50, ge=1, le=200),
              vendors: list[str] | None = Query(None, alias="vendor"),  # match source query param style
              q: str | None = Query(None, max_length=120),
              category: str | None = Query(None, max_length=120),
              db: AsyncSession = Depends(get_db),
          ):
              payload = await ModelCatalogReadService.list_models(db, page=page, page_size=page_size, vendors=vendors, q=q, category=category)
              return JSONResponse(content=payload)
          ```
          NOTE: verify the exact query param names in `services/user-service/src/controllers/model_catalog.py` and match them verbatim (the source may use `vendor` singular as the alias; `q` and `category` as plain str params).

       4. `GET /models/{slug}`:
          ```
          @router.get("/models/{slug}", summary="Get supported model by slug")
          async def get_supported_model(
              slug: str = Path(..., pattern=r"^[a-z0-9][a-z0-9._-]*$", max_length=120),
              db: AsyncSession = Depends(get_db),
          ):
              payload = await ModelCatalogReadService.get_model_by_slug(db, slug)
              return JSONResponse(content=payload)
          ```
          The slug pattern is sourced verbatim from PATTERNS § controllers/model_catalog.py.

       Use `try/except Exception: logger.exception(...); raise` around the service call if the source has it (port verbatim). `NotFoundException` raised in `get_model_by_slug` gets mapped to 404 by Phase 1's `register_exception_handlers`.

    3. **core/router.py — final include_router call:** Append AFTER the `keys.router` + `billing.router` lines from 04-02:
       `from api_service.controllers import model_catalog`
       `api_router.include_router(model_catalog.router)  # /model-vendors, /models/categories, /models, /models/{slug}`

       After this task the api_router includes all 4 user-domain controllers (`auth`, `keys`, `billing`, `model_catalog`), completing Phase 4's router surface.
  </action>
  <verify>
    <automated>
      cd services/api-service &&
      python -c "from api_service.services.model_catalog_service import ModelCatalogReadService, _CACHE_PREFIX, _VENDORS_TTL, _CATEGORIES_TTL, _MODELS_LIST_TTL, _MODEL_DETAIL_TTL; assert _CACHE_PREFIX == 'mc:'; assert _VENDORS_TTL == 300; assert _CATEGORIES_TTL == 300; assert _MODELS_LIST_TTL == 120; assert _MODEL_DETAIL_TTL == 300" &&
      python -c "from api_service.services.model_catalog_service import ModelCatalogReadService; assert all(hasattr(ModelCatalogReadService, m) for m in ['list_vendors', 'list_categories', 'list_models', 'get_model_by_slug'])" &&
      python -c "import api_service.services.model_catalog_service as m; assert not hasattr(m, 'ModelCatalogService'), 'class must be named ModelCatalogReadService per D-07'" &&
      python -c "from api_service.controllers.model_catalog import router as mc_router; paths = sorted([r.path for r in mc_router.routes]); print(paths); expected = {'/model-vendors', '/models/categories', '/models', '/models/{slug}'}; got = set(paths); assert expected.issubset(got), f'missing: {expected - got}, got: {got}'" &&
      python -c "from api_service.main import app; routes = sorted([r.path for r in app.routes]); expected = ['/api/v1/model-vendors', '/api/v1/models/categories', '/api/v1/models', '/api/v1/models/{slug}']; missing = [p for p in expected if p not in routes]; assert not missing, f'missing routes: {missing}'" &&
      grep -c "model_catalog_gateway\\|HTTP\\|httpx\\|admin-service" services/api-service/api_service/services/model_catalog_service.py services/api-service/api_service/controllers/model_catalog.py | { ! grep -v ":0$" ; } &&
      grep -q "cache_get_or_fetch" services/api-service/api_service/services/model_catalog_service.py &&
      grep -q "active_only=True" services/api-service/api_service/services/model_catalog_service.py &&
      grep -q "TODO(phase-5)" services/api-service/api_service/services/model_catalog_service.py &&
      grep -q "include_router(model_catalog.router)" services/api-service/api_service/core/router.py &&
      python -c "from api_service.main import app; auth = [r for r in app.routes if '/auth/' in getattr(r, 'path', '')]; keys = [r for r in app.routes if r.path.startswith('/api/v1/keys')]; billing = [r for r in app.routes if r.path.startswith('/api/v1/billing')]; mc = [r for r in app.routes if r.path.startswith('/api/v1/model') or 'models' in r.path]; print(f'auth={len(auth)} keys={len(keys)} billing={len(billing)} model_catalog={len(mc)}'); assert len(auth) >= 10; assert len(keys) >= 5; assert len(billing) >= 8; assert len(mc) >= 4"
    </automated>
  </verify>
  <acceptance_criteria>
    - `services/model_catalog_service.py` defines class `ModelCatalogReadService` (NOT `ModelCatalogService` — D-07 — verified by `grep -c "class ModelCatalogService\\b"` returns 0 in this file).
    - Module-level constants present with exact source values: `_CACHE_PREFIX = "mc:"`, `_VENDORS_TTL = 300`, `_CATEGORIES_TTL = 300`, `_MODELS_LIST_TTL = 120`, `_MODEL_DETAIL_TTL = 300`.
    - `ModelCatalogReadService` has 4 staticmethods: `list_vendors`, `list_categories`, `list_models`, `get_model_by_slug`. Each uses `cache_get_or_fetch(...)` (grep verifies).
    - Every read call passes `active_only=True` to its repository (D-04 — `grep -c "active_only=True" services/api-service/api_service/services/model_catalog_service.py` returns at least 4).
    - File contains TODO marker `TODO(phase-5)` referencing cache invalidation (D-05 deferred).
    - No `model_catalog_gateway`, `httpx`, `HTTP`, or `admin-service` references in the new service or controller (gateway eliminated — `grep -c` returns 0 across both files).
    - `controllers/model_catalog.py::router` has all 4 routes: `/model-vendors`, `/models/categories`, `/models`, `/models/{slug}`.
    - `core/router.py` includes `model_catalog.router` after `billing.router`. `grep -q "include_router(model_catalog.router)" services/api-service/api_service/core/router.py` succeeds.
    - When loaded into the FastAPI app, the routes `/api/v1/model-vendors`, `/api/v1/models/categories`, `/api/v1/models`, `/api/v1/models/{slug}` all resolve.
    - Endpoint counts at the app level: ≥10 auth, ≥5 keys, ≥8 billing, ≥4 model_catalog — meets the full Phase 4 user surface.
  </acceptance_criteria>
  <done>
    ModelCatalogReadService implements 4 read methods with cache + active_only filtering. controllers/model_catalog.py replaces user-service's HTTP gateway with direct service calls. Final `include_router` mounts the catalog routes. All 4 user-domain routers wired. Task 3 adds the test suite.
  </done>
</task>

<task type="auto">
  <name>Task 3: 3 integration tests for model catalog — T-04-18 cache_hits, T-04-19 filter, T-04-20 404</name>
  <files>
    services/api-service/tests/test_model_catalog.py
  </files>
  <read_first>
    services/api-service/tests/test_auth_dependencies.py,
    services/api-service/tests/conftest.py,
    services/api-service/tests/test_auth_register.py,
    services/api-service/tests/test_keys.py,
    services/api-service/api_service/services/model_catalog_service.py,
    services/api-service/api_service/controllers/model_catalog.py,
    services/api-service/api_service/schemas/model_catalog.py,
    services/api-service/api_service/common/infra/cache.py,
    services/api-service/api_service/common/core/exceptions.py,
    .planning/phases/04-user-domain-controllers/04-VALIDATION.md,
    .planning/phases/04-user-domain-controllers/04-PATTERNS.md
  </read_first>
  <action>
    Create `tests/test_model_catalog.py` covering VALIDATION T-04-18 (cache hits), T-04-19 (filter), T-04-20 (404). Use the established mocking style (`AsyncMock`/`MagicMock` + `httpx.AsyncClient(app=app, ...)` + `app.dependency_overrides[get_db]`).

    1. **`test_cache_hits` (T-04-18 — USER-05):** Verify second call to `ModelCatalogReadService.list_vendors` returns cached value without re-hitting the repository.
       - Patch `api_service.services.model_catalog_service.cache_get_or_fetch` to track calls.
       - OR (preferred) patch the repository call directly: `@patch("api_service.services.model_catalog_service.ModelVendorRepository")` returning an AsyncMock whose `list_vendors` returns `([vendor_mock], 1)`. Patch `api_service.services.model_catalog_service.get_cache_redis` (via cache.py) with a `fakeredis`-style AsyncMock that has `get`/`set` methods.
       - First call: assert `ModelVendorRepository(...).list_vendors.call_count == 1`. Second call (with the same page/page_size): the cache mock returns the cached JSON, so `list_vendors.call_count` MUST stay at 1.
       - Alternative simpler form: directly unit-test the cache path. Mock `cache_get_or_fetch` to call its `_fetch` callable on cache miss (return None) on first call, and return a cached value on second call. Assert the underlying fetch (i.e. the inner closure that calls the repo) is invoked only on the first call.
       - Use whichever form is more reliable; the goal is to assert that the cache layer is wired correctly so that `cache_get_or_fetch` is the gating call, not the repository.

    2. **`test_filter` (T-04-19 — USER-05):** GET `/api/v1/models?vendor=openai&q=gpt-4` and verify the service is called with the correct kwargs.
       - Patch `api_service.controllers.model_catalog.ModelCatalogReadService.list_models` with `AsyncMock` returning `{"items": [], "total": 0, "page": 1, "page_size": 50}`.
       - Override `get_db` with `mock_db`.
       - GET the endpoint with query params; assert response.status == 200.
       - Assert `ModelCatalogReadService.list_models.await_args.kwargs` includes `vendors=["openai"]` (or whatever the controller's query-param alias produces) AND `q="gpt-4"`.

    3. **`test_404` (T-04-20 — USER-05):** GET `/api/v1/models/nonexistent-slug`; service raises `NotFoundException`; assert response.status == 404.
       - Patch `api_service.controllers.model_catalog.ModelCatalogReadService.get_model_by_slug` with `AsyncMock(side_effect=NotFoundException(detail="Model not found: nonexistent-slug"))`.
       - Override `get_db` with `mock_db`.
       - GET `/api/v1/models/nonexistent-slug` and assert `resp.status_code == 404`.

    Each test must clean up `app.dependency_overrides.clear()` in a `finally` block to avoid leaking overrides between tests.

    Template (use as starting point — adapt to each case):
    ```
    from unittest.mock import AsyncMock, MagicMock, patch
    import pytest
    from httpx import AsyncClient
    from api_service.main import app
    from api_service.core.db import get_db
    from api_service.common.core.exceptions import NotFoundException

    @pytest.mark.asyncio
    @patch("api_service.controllers.model_catalog.ModelCatalogReadService.list_models", new_callable=AsyncMock)
    async def test_filter(mock_list, mock_db):
        mock_list.return_value = {"items": [], "total": 0, "page": 1, "page_size": 50}
        app.dependency_overrides[get_db] = lambda: mock_db
        try:
            async with AsyncClient(app=app, base_url="http://test") as client:
                resp = await client.get("/api/v1/models", params={"vendor": "openai", "q": "gpt-4"})
            assert resp.status_code == 200
            kwargs = mock_list.await_args.kwargs
            assert kwargs.get("q") == "gpt-4"
            assert kwargs.get("vendors") == ["openai"] or kwargs.get("vendors") == ("openai",)
        finally:
            app.dependency_overrides.clear()
    ```
  </action>
  <verify>
    <automated>
      cd services/api-service &&
      pytest tests/test_model_catalog.py -x -q &&
      pytest tests/ -x -q &&
      pytest tests/ --tb=short -q | tail -20
    </automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_model_catalog.py::test_cache_hits` passes (T-04-18 USER-05 covered) and asserts that the underlying repository call happens only once across two calls with identical inputs.
    - `tests/test_model_catalog.py::test_filter` passes (T-04-19 USER-05 covered) and asserts `vendors` + `q` kwargs flow through from query params to the service.
    - `tests/test_model_catalog.py::test_404` passes (T-04-20 USER-05 covered) and asserts response.status_code == 404 when `NotFoundException` is raised.
    - Full Phase 4 test suite stays green: `pytest tests/ -x -q` exits 0 (Wave 0 + 04-01 + 04-02 + 04-03 + Phase 3 baseline).
    - No test reads from a real DB / Redis / SMTP server — only `AsyncMock` / `MagicMock` / `app.dependency_overrides`.
    - Each test cleans up `app.dependency_overrides` in `finally` (grep for `dependency_overrides.clear()` returns ≥3 matches).
  </acceptance_criteria>
  <done>
    All 23 VALIDATION slots (T-04-01..23) green across the three plans. USER-05 fully verified. Phase 4 ready for `/gsd:verify-work`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → /model-vendors, /models, /models/{slug}, /models/categories | Public endpoints (NO auth) — readable by anyone reaching the api-service. Identical behavior to source (`services/user-service/src/controllers/model_catalog.py`). |
| Service → Redis cache | `mc:*` keys store JSON payloads of vendor/category/model listings. Redis is internal-network; payloads contain no secrets. |
| Service → DB | Read-only via `ModelCatalogRepository.list_models`/`get_by_slug` etc. No writes from this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-I7 | Information disclosure | Inactive models exposed via public catalog | mitigate | `active_only=True` hardcoded on every repository call (D-04 — verified by grep). Source already behaved this way; port preserves it. |
| T-04-I8 | Information disclosure | Cache stampede leaking partial data | accept | `cache_get_or_fetch` is fail-open and not distributed-locked. RESEARCH § Pattern 6 explicitly accepts this at the current load level (payloads <5KB, cache miss cost = 1 DB query). |
| T-04-T6 | Tampering | Slug injection in `/models/{slug}` | mitigate | Path-level regex `pattern=r"^[a-z0-9][a-z0-9._-]*$"` + `max_length=120` (PATTERNS § controllers/model_catalog.py). |
| T-04-T7 | Tampering | Free-text `q` filter SQL injection | mitigate | Parameterized via SQLAlchemy ORM `ilike(:q)` inside `ModelCatalogRepository.list_models` (Phase 3 verified). Controller validates `q: str | None = Query(None, max_length=120)`. |
| T-04-D5 | Denial of service | Unbounded page_size | mitigate | Controller-level `page_size: int = Query(100, ge=1, le=200)` — hard cap. |
| T-04-D6 | Denial of service | Cache key explosion via free-text `q` | mitigate | Cache key uses `md5(filter_args)[:12]` — 16M-key ceiling. Stale entries naturally expire at TTL (120s for models). |
| T-04-I9 | Information disclosure | Cache stale after admin update (D-05 deferred) | accept | Per D-05 / RESEARCH § Open Questions: admin invalidation deferred to Phase 5. Users may see up to 5min stale data; documented + accepted. |
</threat_model>

<verification>
**Per-task verification** runs inside each `<verify><automated>` block.

**Phase 4 final gate (run after Task 3 in this plan):**
- `cd services/api-service && pytest tests/ --cov=api_service --cov-report=term-missing --cov-fail-under=80` exits 0.
- `cd services/api-service && python -c "from api_service.main import app; assert sum(1 for r in app.routes if r.path.startswith('/api/v1')) >= 27"` (10 auth + 5 keys + 8 billing + 4 model_catalog = 27).
- `arq api_service.core.worker.WorkerSettings --check` exits 0 (manual smoke — worker boots in isolation per RESEARCH § Pitfall 5).
- VALIDATION slots T-04-01 through T-04-23 all `✅` (10 from 04-01 + 8 from 04-02 + 5 from 04-03 = 23 total).
- No `from common\\.…` / `from utils\\.…` / `from gateways\\.…` / `from repositories import …` imports anywhere in `services/api-service/api_service/{controllers,services,schemas}/` (final Import Rewrite Map sweep).
- `grep -rE "BalanceTxRepository|TopupOrderRepository|UsageStatRepository|VoucherRedemptionCodeRepository|EmailCodeRepository|SessionRepository|model_catalog_gateway|system_settings_gateway" services/api-service/api_service/` returns no matches (deleted Phase-3 sub-repos AND deleted gateways).
</verification>

<success_criteria>
- USER-05 fully covered: 4 public catalog endpoints (/model-vendors, /models, /models/categories, /models/{slug}) functional with cache + tests green.
- Phase 4 user surface complete: 27 endpoints under `/api/v1/*` (10 auth + 5 keys + 8 billing + 4 model_catalog), front-end paths unchanged (only host:port differs).
- All 4 USER-NN requirements covered across the three plans: USER-01 (04-01), USER-06 (04-01 partial + 04-03 finalization), USER-04 (04-02), USER-05 (04-03).
- All 12 user decisions enacted: D-01 (no internal_* migrated), D-02 (ARQ async email), D-03 (schemas 1:1 copy), D-04 (model catalog read filter active_only=True, no pool_model_configs JOIN), D-05 (cache TTL only, no invalidation), D-06 (read schemas only from admin), D-07 (`ModelCatalogReadService` class name), D-08 (3-plan split), D-09 (`settings.DEFAULT_USER_RPM`), D-10 (single schemas/common.py file), D-11 (inner db.commit preserved), D-12 (Wave 0 foundations landed first).
- All Pitfalls (P1-P10) addressed in code with grep-verifiable evidence.
- Full test suite green; coverage ≥80%; Phase 4 ready for `/gsd:verify-work`.
</success_criteria>

<output>
Create `.planning/phases/04-user-domain-controllers/04-03-SUMMARY.md` when done, summarizing:
- Files created/modified.
- VALIDATION slots covered: T-04-18, T-04-19, T-04-20 (3 slots in this plan; 23 total across phase).
- Requirement addressed: USER-05 (full).
- Decisions enacted: D-04 (active_only filter), D-05 (cache TTL only), D-06 (read schemas only), D-07 (ModelCatalogReadService class name).
- Pitfalls addressed: gateway deletion (model_catalog_gateway eliminated), Phase 3 D-04 model rename respected (ModelCatalog/ModelCatalogCategoryMap), cache fail-open preserved.
- Phase complete — all 4 user-domain requirements (USER-01/04/05/06) fully addressed; Phase 4 ready for `/gsd:verify-work` and merge to develop.
</output>
</content>
</invoke>
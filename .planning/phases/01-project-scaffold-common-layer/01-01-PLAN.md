---
phase: 1
plan: "01-01"
title: "Directory structure and pyproject.toml"
type: execute
wave: 1
depends_on: []
files_modified:
  - services/api-service/pyproject.toml
  - services/api-service/.env.example
  - services/api-service/api_service/__init__.py
  - services/api-service/api_service/common/__init__.py
  - services/api-service/api_service/common/api/__init__.py
  - services/api-service/api_service/common/core/__init__.py
  - services/api-service/api_service/common/infra/__init__.py
  - services/api-service/api_service/common/security/__init__.py
  - services/api-service/api_service/common/http/__init__.py
  - services/api-service/api_service/common/utils/__init__.py
  - services/api-service/api_service/core/__init__.py
  - services/api-service/api_service/controllers/__init__.py
  - services/api-service/api_service/services/__init__.py
  - services/api-service/api_service/models/__init__.py
  - services/api-service/api_service/repositories/__init__.py
  - services/api-service/api_service/schemas/__init__.py
  - services/api-service/migrations/.gitkeep
  - services/api-service/tests/__init__.py
autonomous: true
requirements:
  - INFRA-01
---

# Plan: Directory structure and pyproject.toml

## Objective

创建 api-service 的完整目录骨架和 pyproject.toml，使项目可被 `uv` 安装并通过 `ruff check` 验证。删除旧的 `src/` 目录结构（空目录），替换为决策 D-08 确定的 `api_service/` 包结构。

## Tasks

<task id="1">
<title>删除旧的 src/ 目录结构</title>
<read_first>
- services/api-service/src/ (确认目录为空或仅含空子目录)
</read_first>
<action>
删除 services/api-service/src/ 目录及其所有子目录。同时删除 services/api-service/config/ 目录（空目录，配置将通过 pydantic-settings 管理）。保留 services/api-service/migrations/ 目录。
</action>
<acceptance_criteria>
- `services/api-service/src/` 目录不存在
- `services/api-service/config/` 目录不存在
- `services/api-service/migrations/` 目录仍然存在
</acceptance_criteria>
</task>

<task id="2">
<title>创建 api_service/ 包目录结构</title>
<read_first>
- .planning/phases/01-project-scaffold-common-layer/01-RESEARCH.md (Section 3.2 Target Directory Structure)
- .planning/phases/01-project-scaffold-common-layer/01-CONTEXT.md (D-08 through D-13)
</read_first>
<action>
在 services/api-service/ 下创建以下目录结构，每个目录包含空 __init__.py：

api_service/
├── __init__.py (包含 __version__ = "1.0.0")
├── common/
│   ├── __init__.py
│   ├── api/
│   │   └── __init__.py
│   ├── core/
│   │   └── __init__.py
│   ├── infra/
│   │   └── __init__.py
│   ├── security/
│   │   └── __init__.py
│   ├── http/
│   │   └── __init__.py
│   └── utils/
│       └── __init__.py
├── core/
│   └── __init__.py
├── controllers/
│   └── __init__.py
├── services/
│   └── __init__.py
├── models/
│   └── __init__.py
├── repositories/
│   └── __init__.py
└── schemas/
    └── __init__.py

tests/
└── __init__.py

migrations/ (已存在，确保有 .gitkeep)
</action>
<acceptance_criteria>
- `services/api-service/api_service/__init__.py` 存在且包含 `__version__ = "1.0.0"`
- 以下 __init__.py 文件全部存在：api_service/common/__init__.py, api_service/common/api/__init__.py, api_service/common/core/__init__.py, api_service/common/infra/__init__.py, api_service/common/security/__init__.py, api_service/common/http/__init__.py, api_service/common/utils/__init__.py, api_service/core/__init__.py, api_service/controllers/__init__.py, api_service/services/__init__.py, api_service/models/__init__.py, api_service/repositories/__init__.py, api_service/schemas/__init__.py
- `services/api-service/tests/__init__.py` 存在
- `services/api-service/migrations/.gitkeep` 存在
</acceptance_criteria>
</task>

<task id="3">
<title>创建 pyproject.toml</title>
<read_first>
- .planning/phases/01-project-scaffold-common-layer/01-RESEARCH.md (Section 4 — 完整依赖列表和 tool 配置)
- services/user-service/pyproject.toml (参考现有格式)
</read_first>
<action>
在 services/api-service/pyproject.toml 创建项目配置：

[project] 部分：
- name = "eucal-ai-api-service"
- version = "1.0.0"
- description = "EucalAI unified API service: user + admin + relay"
- requires-python = ">=3.10"
- dependencies 列表来自 RESEARCH.md Section 4（包含 fastapi>=0.115.0, uvicorn[standard]>=0.34.0, pydantic>=2.5.0, pydantic-settings>=2.1.0, python-multipart>=0.0.6, email-validator>=2.1.0, httpx>=0.26.0, slowapi>=0.1.9, sqlalchemy[asyncio]>=2.0.25, aiomysql>=0.2.0, alembic>=1.14.0, python-jose[cryptography]>=3.3.1, passlib[bcrypt]>=1.7.4, bcrypt>=3.2.0,<4.0.0, cryptography>=42.0.0, snowflake-id>=1.0.0, nanoid>=2.0.0, redis>=5.0, arq>=0.26.0, openai>=1.40.0, anthropic>=0.34.0, cachetools>=5.0.0, python-dotenv>=1.0.0, tzdata>=2025.3）

[project.optional-dependencies] dev 部分：ruff>=0.4.0, mypy>=1.10.0, pytest>=8.0.0, pytest-asyncio>=0.23.0, httpx, coverage>=7.0

[build-system]: requires = ["hatchling"], build-backend = "hatchling.build"
[tool.hatch.build.targets.wheel]: packages = ["api_service"]
[[tool.uv.index]]: url = "https://pypi.tuna.tsinghua.edu.cn/simple", default = true
[tool.ruff]: target-version = "py310", line-length = 100, select = ["E", "F", "I", "N", "W", "UP", "B", "C4", "SIM"], ignore = ["E501"]
[tool.ruff.format]: quote-style = "double", indent-style = "space"
[tool.mypy]: python_version = "3.10", warn_return_any = true, warn_unused_configs = true, disallow_untyped_defs = true
</action>
<acceptance_criteria>
- `services/api-service/pyproject.toml` 存在
- `[project]` section 的 name 为 "eucal-ai-api-service"
- `[tool.hatch.build.targets.wheel]` 的 packages 为 ["api_service"]
- dependencies 列表包含 "fastapi>=0.115.0", "openai>=1.40.0", "anthropic>=0.34.0"
- dependencies 列表不包含 "litellm"
- `[[tool.uv.index]]` 的 url 为 "https://pypi.tuna.tsinghua.edu.cn/simple"
- `[tool.ruff]` 的 target-version 为 "py310"
</acceptance_criteria>
</task>

<task id="4">
<title>创建 .env.example</title>
<read_first>
- .planning/phases/01-project-scaffold-common-layer/01-RESEARCH.md (Section 7 — ApiServiceSettings 字段)
- services/user-service/src/common/config.py (BaseServiceSettings 必填字段: JWT_SECRET_KEY, INTERNAL_SECRET)
</read_first>
<action>
在 services/api-service/.env.example 创建环境变量示例文件，包含所有必填字段和关键可选字段：

必填（启动校验）：
- JWT_SECRET_KEY=your-secret-key-at-least-32-characters-long-here
- INTERNAL_SECRET=your-internal-secret-at-least-32-chars-here
- PROVIDER_SECRET_MASTER_KEY=your-aes256-master-key-base64-here

数据库：
- DATABASE_URL=mysql+aiomysql://root:password@localhost:3306/eucal_ai

Redis：
- REDIS_URL=redis://127.0.0.1:6379/0
- CACHE_REDIS_URL=redis://127.0.0.1:6379/2
- WORKER_QUEUE_REDIS_URL=redis://127.0.0.1:6379/1

服务：
- ENV=development
- DEBUG=true
- PORT=8000
- SNOWFLAKE_WORKER_ID=1

Inference：
- INFERENCE_SERVICE_URL=http://127.0.0.1:8004
- INFERENCE_SERVICE_SECRET=

SMTP（可选）：
- SMTP_HOST=
- SMTP_PORT=587
- SMTP_USER=
- SMTP_PASSWORD=
</action>
<acceptance_criteria>
- `services/api-service/.env.example` 存在
- 文件包含 `JWT_SECRET_KEY=` 行，值长度 >= 32 字符
- 文件包含 `INTERNAL_SECRET=` 行，值长度 >= 32 字符
- 文件包含 `DATABASE_URL=mysql+aiomysql://` 开头的行
- 文件包含 `REDIS_URL=redis://` 开头的行
</acceptance_criteria>
</task>

## Verification

```bash
cd services/api-service

# 1. 目录结构正确
find api_service -name "__init__.py" | wc -l  # 应为 13

# 2. pyproject.toml 语法正确
python -c "import tomllib; tomllib.load(open('pyproject.toml', 'rb'))"

# 3. 旧目录已删除
test ! -d src && echo "OK: src/ removed"

# 4. ruff 可解析（无语法错误）
ruff check api_service/ --select E999
```

## must_haves
- services/api-service/api_service/ 是 Python 包根目录（含 __init__.py 和 __version__）
- pyproject.toml 使用 hatchling 构建，packages 指向 api_service
- 不存在 litellm 依赖
- 旧的 src/ 目录结构已删除
- .env.example 包含所有启动必填的环境变量示例值

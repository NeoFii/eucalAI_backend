---
phase: 1
plan: "01-02"
title: "Common layer merge and Settings class"
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - services/api-service/api_service/common/config.py
  - services/api-service/api_service/common/observability.py
  - services/api-service/api_service/common/health.py
  - services/api-service/api_service/common/internal_logs.py
  - services/api-service/api_service/common/infra/redis.py
  - services/api-service/api_service/common/infra/cache.py
  - services/api-service/api_service/common/infra/db/base.py
  - services/api-service/api_service/common/infra/db/runtime.py
  - services/api-service/api_service/common/infra/db/repository.py
  - services/api-service/api_service/common/infra/db/query.py
  - services/api-service/api_service/common/infra/db/schema_version.py
  - services/api-service/api_service/common/infra/db/_env_shared.py
  - services/api-service/api_service/common/security/jwt.py
  - services/api-service/api_service/common/security/password.py
  - services/api-service/api_service/common/security/crypto.py
  - services/api-service/api_service/common/security/token_blacklist.py
  - services/api-service/api_service/common/http/internal_auth.py
  - services/api-service/api_service/common/http/request_context.py
  - services/api-service/api_service/common/utils/nanoid_uid.py
  - services/api-service/api_service/common/utils/snowflake.py
  - services/api-service/api_service/common/utils/timezone.py
  - services/api-service/api_service/common/api/pagination.py
  - services/api-service/api_service/common/core/exceptions.py
  - services/api-service/api_service/common/core/exception_handlers.py
  - services/api-service/api_service/core/config.py
autonomous: true
requirements:
  - INFRA-07
  - INFRA-08
---

# Plan: Common layer merge and Settings class

## Objective

将三个服务的 common 层按职责域重新组织并合并到 api_service/common/，遵循 D-02 决策的目录结构（infra/, security/, http/, utils/）。修正所有 import 路径为 `api_service.common.*` 格式。创建统一的 ApiServiceSettings 配置类，合并 user-service、admin-service、router-service 的所有配置项。

## Tasks

<task id="1">
<title>迁移 common/config.py (BaseServiceSettings)</title>
<read_first>
- services/user-service/src/common/config.py
</read_first>
<action>
将 services/user-service/src/common/config.py 复制到 services/api-service/api_service/common/config.py。文件内容保持不变（BaseServiceSettings 类无内部 import 需要修改，它只依赖 pydantic 和 pydantic_settings）。
</action>
<acceptance_criteria>
- `api_service/common/config.py` 包含 `class BaseServiceSettings(BaseSettings)` 定义
- 包含 `model_config = SettingsConfigDict(env_file=".env", ...)` 配置
- 包含 `validate_required_fields` model_validator 校验 JWT_SECRET_KEY 和 INTERNAL_SECRET
- `from pydantic_settings import BaseSettings, SettingsConfigDict` import 存在
</acceptance_criteria>
</task>

<task id="2">
<title>迁移 common/observability.py</title>
<read_first>
- services/user-service/src/common/observability.py
</read_first>
<action>
将 services/user-service/src/common/observability.py 复制到 services/api-service/api_service/common/observability.py。此文件无内部 common 层 import（只依赖标准库 + fastapi + starlette），内容保持不变。
</action>
<acceptance_criteria>
- `api_service/common/observability.py` 包含 `configure_logging` 函数
- 包含 `configure_logging_from_settings` 函数
- 包含 `install_observability` 函数
- 包含 `log_event` 函数
- 包含 `RingBufferHandler` 类
- 包含 `JsonLogFormatter` 类
- 无 `from common.` 或 `from src.` 开头的 import
</acceptance_criteria>
</task>

<task id="3">
<title>迁移 common/health.py</title>
<read_first>
- services/user-service/src/common/health.py
</read_first>
<action>
将 services/user-service/src/common/health.py 复制到 services/api-service/api_service/common/health.py。此文件只依赖 fastapi + sqlalchemy，无内部 import 需修改。
</action>
<acceptance_criteria>
- `api_service/common/health.py` 包含 `check_database_ready` 函数
- 包含 `build_readiness_response` 函数
- 无 `from common.` 开头的 import
</acceptance_criteria>
</task>

<task id="4">
<title>迁移 common/infra/redis.py</title>
<read_first>
- services/user-service/src/common/redis.py
</read_first>
<action>
将 services/user-service/src/common/redis.py 复制到 services/api-service/api_service/common/infra/redis.py。检查文件内部是否有 `from common.` import，如有则替换为 `from api_service.common.`。确保创建 `common/infra/__init__.py`。
</action>
<acceptance_criteria>
- `api_service/common/infra/redis.py` 存在
- `api_service/common/infra/__init__.py` 存在
- 包含 `init_redis` 函数（或等效的 Redis 连接初始化函数）
- 包含 `close_redis` 函数（或等效的关闭函数）
- 所有内部 import 使用 `api_service.common.` 前缀（如果有的话）
</acceptance_criteria>
</task>

<task id="5">
<title>迁移 common/infra/cache.py</title>
<read_first>
- services/user-service/src/common/cache.py
- services/api-service/api_service/common/infra/redis.py (task 4 产出)
</read_first>
<action>
将 services/user-service/src/common/cache.py 复制到 services/api-service/api_service/common/infra/cache.py。替换内部 import：
- `from common.redis import ...` → `from api_service.common.infra.redis import ...`
- `from common.observability import ...` → `from api_service.common.observability import ...`
</action>
<acceptance_criteria>
- `api_service/common/infra/cache.py` 存在
- 包含 `init_cache_redis` 函数
- 包含 `close_cache_redis` 函数
- 所有 `from common.` import 已替换为正确的 `from api_service.common.` 路径
</acceptance_criteria>
</task>

<task id="6">
<title>提取 internal.py 验签逻辑到 common/http/internal_auth.py</title>
<read_first>
- services/user-service/src/common/internal.py
</read_first>
<action>
从 services/user-service/src/common/internal.py 中**仅提取签名验证部分**到 services/api-service/api_service/common/http/internal_auth.py。确保创建 `common/http/__init__.py`。

只迁移以下内容：
1. `verify_internal_signature` 函数（HMAC 签名验证逻辑）
2. `build_internal_auth_dependency` 函数（FastAPI 依赖注入工厂，用于保护 internal 端点）
3. 相关的常量和辅助函数（如 HMAC 算法配置、时间戳校验窗口等）

**不迁移**以下内容（延迟到 Phase 6 InferenceClient）：
- `get_internal_client` 函数（httpx 连接池管理）
- `close_internal_clients` 函数
- 签名生成/发送逻辑（调用方逻辑）
- 熔断器/重试逻辑

替换 import：
- `from common.observability import ...` → `from api_service.common.observability import ...`
- `from common.core.exceptions import ...` → `from api_service.common.core.exceptions import ...`
</action>
<acceptance_criteria>
- `api_service/common/http/__init__.py` 存在
- `api_service/common/http/internal_auth.py` 存在
- 包含 `verify_internal_signature` 函数
- 包含 `build_internal_auth_dependency` 函数
- **不包含** `get_internal_client` 函数
- **不包含** `close_internal_clients` 函数
- 所有 `from common.` import 已替换为 `from api_service.common.` 对应路径
</acceptance_criteria>
</task>

<task id="7">
<title>迁移 common/internal_logs.py</title>
<read_first>
- services/user-service/src/common/internal_logs.py
</read_first>
<action>
将 services/user-service/src/common/internal_logs.py 复制到 services/api-service/api_service/common/internal_logs.py。替换所有 `from common.` import 为 `from api_service.common.`。
</action>
<acceptance_criteria>
- `api_service/common/internal_logs.py` 存在
- 包含 `build_internal_logs_router` 函数
- 所有 `from common.` import 已替换为 `from api_service.common.`
</acceptance_criteria>
</task>

<task id="8">
<title>迁移 admin-service 独有模块到对应职责域目录</title>
<read_first>
- services/admin-service/src/common/request_context.py
- services/admin-service/src/common/token_blacklist.py
</read_first>
<action>
1. 将 services/admin-service/src/common/request_context.py 复制到 services/api-service/api_service/common/http/request_context.py。替换所有 `from common.` import 为 `from api_service.common.` 对应路径。

2. 将 services/admin-service/src/common/token_blacklist.py 复制到 services/api-service/api_service/common/security/token_blacklist.py。替换 import：
   - `from common.redis import ...` → `from api_service.common.infra.redis import ...`
   - 其他 `from common.` → `from api_service.common.` 对应路径

确保创建 `common/security/__init__.py`。
</action>
<acceptance_criteria>
- `api_service/common/http/request_context.py` 存在，包含 ContextVar 定义（IP/UA 审计上下文）
- `api_service/common/security/__init__.py` 存在
- `api_service/common/security/token_blacklist.py` 存在，包含 JWT 黑名单相关函数
- 两个文件中所有 `from common.` import 已替换为正确的 `from api_service.common.` 路径
</acceptance_criteria>
</task>

<task id="9">
<title>迁移 common/api/pagination.py</title>
<read_first>
- services/user-service/src/common/api/pagination.py
</read_first>
<action>
将 services/user-service/src/common/api/pagination.py 复制到 services/api-service/api_service/common/api/pagination.py。替换所有 `from common.` import 为 `from api_service.common.`。
</action>
<acceptance_criteria>
- `api_service/common/api/pagination.py` 存在
- 包含分页相关的工具类或函数
- 所有 `from common.` import 已替换为 `from api_service.common.`
</acceptance_criteria>
</task>

<task id="10">
<title>迁移 common/core/exceptions.py 和 exception_handlers.py</title>
<read_first>
- services/user-service/src/common/core/exceptions.py
- services/user-service/src/common/core/exception_handlers.py
</read_first>
<action>
1. 将 services/user-service/src/common/core/exceptions.py 复制到 services/api-service/api_service/common/core/exceptions.py。替换所有 `from common.` import 为 `from api_service.common.`。

2. 将 services/user-service/src/common/core/exception_handlers.py 复制到 services/api-service/api_service/common/core/exception_handlers.py。替换 import：
   - `from common.core.exceptions import ...` → `from api_service.common.core.exceptions import ...`
   - `from common.observability import ...` → `from api_service.common.observability import ...`
</action>
<acceptance_criteria>
- `api_service/common/core/exceptions.py` 包含 `APIException` 基类定义
- `api_service/common/core/exception_handlers.py` 包含 `register_exception_handlers` 函数
- exception_handlers.py 中的 import 为 `from api_service.common.core.exceptions import ...`
- exception_handlers.py 中的 observability import 为 `from api_service.common.observability import ...`
</acceptance_criteria>
</task>

<task id="11">
<title>迁移 common/infra/db/ 模块（base, runtime, repository, query, _env_shared, schema_version）</title>
<read_first>
- services/user-service/src/common/db/base.py
- services/user-service/src/common/db/runtime.py
- services/user-service/src/common/db/repository.py
- services/user-service/src/common/db/query.py
- services/user-service/src/common/db/_env_shared.py
- services/admin-service/src/common/db/schema_version.py
</read_first>
<action>
1. 复制 user-service 的 db/base.py, db/runtime.py, db/repository.py, db/query.py, db/_env_shared.py 到 api_service/common/infra/db/。确保创建 `common/infra/db/__init__.py`。

2. 复制 admin-service 的 db/schema_version.py 到 api_service/common/infra/db/schema_version.py。

3. 在所有文件中替换 `from common.` import 为 `from api_service.common.` 对应路径。注意 db 模块内部互相引用需要更新为 `from api_service.common.infra.db.` 前缀。

4. 在 db/base.py 中添加共享的 DeclarativeBase 定义（如果不存在）：
   ```python
   from sqlalchemy.orm import DeclarativeBase
   class Base(DeclarativeBase):
       pass
   ```
   这是 RESEARCH.md Section 6.3 指出的必要修改。

5. 修改 schema_version.py 中的服务配置列表，从：
   ```python
   for service_name, package, db_env in [
       ("admin-service", "models", "ADMIN_DATABASE_URL"),
       ("user-service", "user_service", "USER_DATABASE_URL"),
   ]:
   ```
   改为：
   ```python
   for service_name, package, db_env in [
       ("api-service", "api_service", "DATABASE_URL"),
   ]:
   ```
</action>
<acceptance_criteria>
- `api_service/common/infra/db/__init__.py` 存在
- `api_service/common/infra/db/base.py` 包含 `class Base(DeclarativeBase)` 定义
- `api_service/common/infra/db/base.py` 包含 `TimestampMixin`, `SnowflakeIdMixin`, `SoftDeleteMixin`
- `api_service/common/infra/db/runtime.py` 包含 `ServiceDatabaseRuntime` 类
- `api_service/common/infra/db/repository.py` 包含 `BaseRepository` 泛型类
- `api_service/common/infra/db/query.py` 存在
- `api_service/common/infra/db/_env_shared.py` 存在
- `api_service/common/infra/db/schema_version.py` 包含 `"api-service"` 和 `"DATABASE_URL"` 字符串
- `api_service/common/infra/db/schema_version.py` 不包含 `"ADMIN_DATABASE_URL"` 或 `"USER_DATABASE_URL"`
- 所有文件中 `from common.` import 已替换为 `from api_service.common.` 对应路径
</acceptance_criteria>
</task>

<task id="12">
<title>迁移 common/security/ 和 common/utils/ 模块</title>
<read_first>
- services/user-service/src/common/utils/jwt.py
- services/user-service/src/common/utils/nanoid_uid.py
- services/user-service/src/common/utils/password.py
- services/user-service/src/common/utils/snowflake.py
- services/user-service/src/common/utils/timezone.py
- services/admin-service/src/common/utils/crypto.py
</read_first>
<action>
1. 迁移到 common/security/（按 D-02 职责域分组）：
   - user-service/src/common/utils/jwt.py → api_service/common/security/jwt.py
   - user-service/src/common/utils/password.py → api_service/common/security/password.py
   - admin-service/src/common/utils/crypto.py → api_service/common/security/crypto.py

2. 迁移到 common/utils/（保持工具类）：
   - user-service/src/common/utils/nanoid_uid.py → api_service/common/utils/nanoid_uid.py
   - user-service/src/common/utils/snowflake.py → api_service/common/utils/snowflake.py
   - user-service/src/common/utils/timezone.py → api_service/common/utils/timezone.py

3. 确保创建 `common/utils/__init__.py`。

4. 在所有文件中替换 `from common.` import 为 `from api_service.common.` 对应路径。

注意：snowflake.py 无内部 import（只依赖 snowflake 库）。jwt.py 可能有 `from common.config import ...` 需要替换为 `from api_service.common.config import ...`。crypto.py 可能有 `from common.observability import ...` 需要替换为 `from api_service.common.observability import ...`。
</action>
<acceptance_criteria>
- `api_service/common/security/jwt.py` 存在
- `api_service/common/security/password.py` 存在
- `api_service/common/security/crypto.py` 存在，包含 AES-256-GCM 加解密函数
- `api_service/common/utils/__init__.py` 存在
- `api_service/common/utils/nanoid_uid.py` 存在
- `api_service/common/utils/snowflake.py` 包含 `configure_snowflake` 函数和 `generate_snowflake_id` 函数
- `api_service/common/utils/timezone.py` 存在
- 所有文件中 `from common.` import 已替换为 `from api_service.common.` 对应路径
</acceptance_criteria>
</task>

<task id="13">
<title>创建统一 ApiServiceSettings 配置类</title>
<read_first>
- services/api-service/api_service/common/config.py (task 1 产出)
- services/user-service/src/core/config.py
- services/admin-service/src/core/config.py
- services/router-service/src/core/config.py
- .planning/phases/01-project-scaffold-common-layer/01-RESEARCH.md (Section 7 — 完整 Settings 设计)
</read_first>
<action>
在 services/api-service/api_service/core/config.py 创建 ApiServiceSettings 类：

1. 继承 `from api_service.common.config import BaseServiceSettings`
2. 包含以下分区字段（来自 RESEARCH.md Section 7.1）：
   - 基础: PROJECT_NAME="Eucal AI API Service", SERVICE_NAME="api-service", DESCRIPTION="Eucal AI Unified API Service", PORT=8000
   - Database: DATABASE_URL="mysql+aiomysql://root:password@localhost:3306/eucal_ai"
   - Redis: REDIS_URL, CACHE_REDIS_URL, WORKER_QUEUE_REDIS_URL
   - Inference: INFERENCE_SERVICE_URL, INFERENCE_SERVICE_SECRET
   - Admin: BOOTSTRAP_SUPERADMIN_ENABLED, BOOTSTRAP_SUPERADMIN_EMAIL, BOOTSTRAP_SUPERADMIN_PASSWORD, BOOTSTRAP_SUPERADMIN_NAME, PROVIDER_SECRET_MASTER_KEY, ADMIN_TOKEN_EXPIRE_MINUTES=480
   - User: SMTP_HOST, SMTP_PORT, SMTP_USER, SMTP_PASSWORD, SMTP_TLS, SMTP_FROM, MAX_API_KEYS_PER_USER=20, LOGIN_MAX_FAILURES=5, EMAIL_CODE_EXPIRE_MINUTES=5
   - Relay: CHANNEL_MAX_RETRIES=2, CHANNEL_COOLDOWN_SECONDS=30.0, CHANNEL_AUTO_DISABLE_ENABLED=True, CHANNEL_AUTO_DISABLE_FAILURE_THRESHOLD=5, RATE_LIMIT_ENABLED=True, RATE_LIMIT_DEFAULT_USER_RPM=20, RATE_LIMIT_GLOBAL_RPM=0, SDK_CLIENT_POOL_MAX_SIZE=64, ANTHROPIC_NATIVE_SLUGS=["anthropic"], CHANNEL_AFFINITY_ENABLED=False, CHANNEL_AFFINITY_TTL=3600
   - CORS: ALLOWED_HOSTS 默认值包含 localhost:5173, localhost:3000, localhost:3001, 127.0.0.1:5173, 127.0.0.1:3000
   - Logging: LOG_FILE_PREFIX="api"
   - Snowflake: SNOWFLAKE_WORKER_ID=1

3. 不包含 PROVIDER_SECRET_MASTER_KEY 的 model_validator（Phase 1 不需要加密功能，延迟到 Phase 5）

4. 底部添加：
   ```python
   @lru_cache
   def get_settings() -> ApiServiceSettings:
       return ApiServiceSettings()

   settings = get_settings()
   ```

5. 不使用 AliasChoices（决策 D-07）
6. 不包含已删除的配置项：USER_SERVICE_URL, ADMIN_SERVICE_URL, ROUTER_SERVICE_URL, CONFIG_REFRESH_INTERVAL_SECONDS, CALLLOG_FLUSH_INTERVAL, CALLLOG_MAX_BUFFER
</action>
<acceptance_criteria>
- `api_service/core/config.py` 包含 `class ApiServiceSettings(BaseServiceSettings)` 定义
- import 为 `from api_service.common.config import BaseServiceSettings`
- 包含 `SERVICE_NAME: str = "api-service"` 字段
- 包含 `DATABASE_URL` 字段，默认值含 `eucal_ai`（不是 eucal_ai_user 或 eucal_ai_admin）
- 包含 `INFERENCE_SERVICE_URL` 字段
- 包含 `CHANNEL_MAX_RETRIES` 字段
- 包含 `SMTP_HOST` 字段
- 包含 `BOOTSTRAP_SUPERADMIN_ENABLED` 字段
- 不包含 `USER_SERVICE_URL` 字段
- 不包含 `ADMIN_SERVICE_URL` 字段
- 不包含 `AliasChoices` import 或使用
- 底部有 `settings = get_settings()` 单例
- `get_settings` 使用 `@lru_cache` 装饰器
</acceptance_criteria>
</task>

<task id="14">
<title>验证 common 层 import 一致性</title>
<read_first>
- services/api-service/api_service/common/config.py
- services/api-service/api_service/core/config.py
</read_first>
<action>
运行 `ruff check api_service/` 验证所有文件无语法错误和 import 问题。检查是否有遗漏的 `from common.` 或 `from src.` import 未被替换。如果 ruff 报告 import 错误（F821 undefined name, E402 等），修复对应文件。

具体检查：
1. grep -r "from common\." api_service/ 应返回空（无遗漏）
2. grep -r "from src\." api_service/ 应返回空
3. grep -r "from core\." api_service/common/ 应返回空（common 层不应依赖 core 层）
</action>
<acceptance_criteria>
- `grep -r "from common\." api_service/` 返回空（无匹配）
- `grep -r "from src\." api_service/` 返回空（无匹配）
- `grep -r "from core\." api_service/common/` 返回空（common 不依赖 core）
- `ruff check api_service/ --select E999,F821` 无错误输出
</acceptance_criteria>
</task>

## Verification

```bash
cd services/api-service

# 1. 无遗漏的旧 import
grep -r "from common\." api_service/ && echo "FAIL: old imports remain" || echo "OK"
grep -r "from src\." api_service/ && echo "FAIL: src imports remain" || echo "OK"

# 2. common 层不依赖 core 层
grep -r "from core\." api_service/common/ && echo "FAIL: common depends on core" || echo "OK"

# 3. ruff 语法检查
ruff check api_service/ --select E999

# 4. Settings 可实例化（需要 .env 文件）
cp .env.example .env
python -c "from api_service.core.config import settings; print(settings.SERVICE_NAME)"
# 预期输出: api-service

# 5. 目录结构验证（D-02 职责域分组）
test -d api_service/common/infra/db && echo "OK: infra/db exists" || echo "FAIL"
test -d api_service/common/security && echo "OK: security exists" || echo "FAIL"
test -d api_service/common/http && echo "OK: http exists" || echo "FAIL"
test -d api_service/common/utils && echo "OK: utils exists" || echo "FAIL"

# 6. 不存在 gateway 目录（D-03: 不迁移 BaseGateway）
test -d api_service/common/gateway && echo "FAIL: gateway should not exist" || echo "OK"

# 7. internal_auth.py 不包含调用方逻辑（D-04）
grep -l "get_internal_client\|close_internal_clients" api_service/common/http/internal_auth.py && echo "FAIL: calling-side logic present" || echo "OK"

# 8. 文件数量验证
find api_service/common -name "*.py" -not -name "__init__.py" | wc -l
# 预期: 至少 19 个文件（不含 __init__.py）
```

## must_haves
- common 层按 D-02 职责域组织：infra/（db, redis, cache）、security/（jwt, password, crypto, token_blacklist）、http/（internal_auth, request_context）、utils/（nanoid, snowflake, timezone）
- observability.py、health.py、internal_logs.py 保持在 common/ 根目录
- 不存在 common/gateway/ 目录（D-03: 不迁移 BaseGateway）
- common/http/internal_auth.py 只包含验签逻辑（verify_internal_signature, build_internal_auth_dependency），不包含调用方逻辑（D-04）
- 所有 import 路径使用 `api_service.common.*` 格式，无遗留的 `from common.` 或 `from src.`
- common 层不依赖 core 层（单向依赖：core → common）
- ApiServiceSettings 合并了三个服务的所有配置项（取并集）
- ApiServiceSettings 不包含已废弃的跨服务 URL 配置（USER_SERVICE_URL 等）
- settings 单例使用 @lru_cache 模式
- infra/db/base.py 包含共享的 Base(DeclarativeBase) 类
- infra/db/schema_version.py 已更新为单一 api-service 配置

-- ============================================================
-- 迁移脚本：删除 users 表的 nickname 和 avatar_url 字段
-- ============================================================

-- 删除 nickname 字段
ALTER TABLE `users` DROP COLUMN `nickname`;

-- 删除 avatar_url 字段
ALTER TABLE `users` DROP COLUMN `avatar_url`;

-- ============================================================
-- 重要说明：
-- 1. 此操作不可逆，删除后数据无法恢复
-- 2. 建议在执行前备份数据库
-- 3. 确保应用代码已更新，不再依赖这些字段
-- ============================================================
